---
phase: 999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping
plan: "04"
type: execute
wave: 4
depends_on:
  - "999.9-01"
  - "999.9-02"
  - "999.9-03"
files_modified:
  - docs/architecture.md
  - docs/data-sources.md
  - README.md
autonomous: false
requirements:
  - PHASE-999.9-DOC
  - PHASE-999.9-VERIFY

must_haves:
  truths:
    - "docs/architecture.md describes the HQ/branch hub network + 135-row rate matrix + origin_zone × dest_zone lookup"
    - "docs/data-sources.md lists the 10 hubs with their addresses + zones (D-03 verbatim)"
    - "README.md mentions the new hub-aware origin capability in the differentiator narrative"
    - "End-to-end live demo: user picks a non-HQ hub via dropdown, asks for a surcharge, sees correct origin_zone in trace, surcharge_result reflects the cross-zone multiplier"
  artifacts:
    - path: "docs/architecture.md"
      provides: "Hub network architecture section + extended rate-table description + planner shortlist injection note"
      contains: "HQ/Branch"
    - path: "docs/data-sources.md"
      provides: "10-hub seed list with addresses and zones"
      contains: "hq-lat-krabang"
    - path: "README.md"
      provides: "Phase 999.9 differentiator (origin-aware pricing) in feature/highlights list"
      contains: "origin"
  key_links:
    - from: "docs/architecture.md"
      to: "data/raw/hubs.json"
      via: "documentation reference of seed data"
      pattern: "hubs.json"
    - from: "docs/data-sources.md"
      to: "data/raw/hubs.json"
      via: "10-hub enumeration matches the JSON file"
      pattern: "branch-bang-na"
---

<objective>
Close out Phase 999.9 with documentation updates so future readers (graders, classmates, future-you) understand the HQ/branch origin model + the new 135-row rate matrix, AND a final human-verify checkpoint that exercises the end-to-end live demo flow.

Purpose: Wave 1-3 ships the working capability; Wave 4 makes it discoverable + verified. Documentation aligns with v1.0 docs style (architecture.md mermaid + sections, data-sources.md hub list, README features section). The human-verify checkpoint catches any integration gap that the autonomous test suites missed (especially: real Google Maps geocoding of the new hub addresses, real Gemini extraction of origin_hub_id from prose, sessionStorage persistence across page reloads).

Output: Updated docs/architecture.md (HQ/Branch hub network section + planner shortlist callout + extended rate-table description), docs/data-sources.md (10-hub list verbatim D-03), README.md (one-paragraph addition to differentiator/highlights), and a passing human-verify checkpoint.
</objective>

<execution_context>
@/Users/pollot/Desktop/express-dynamic-workflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pollot/Desktop/express-dynamic-workflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping/999.9-CONTEXT.md
@.planning/phases/999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping/999.9-RESEARCH.md
@.planning/phases/999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping/999.9-UI-SPEC.md
@.planning/phases/999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping/999.9-01-SUMMARY.md
@.planning/phases/999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping/999.9-02-SUMMARY.md
@.planning/phases/999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping/999.9-03-SUMMARY.md

# Files modified by this plan
@docs/architecture.md
@docs/data-sources.md
@README.md

# References for the human-verify steps
@data/raw/hubs.json
@frontend/components/chat/HubPicker.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/architecture.md with HQ/Branch hub network section + extended rate-table description</name>
  <files>docs/architecture.md</files>
  <read_first>
    - docs/architecture.md (current — review entire file structure to find the right insertion points)
    - data/raw/hubs.json (Wave 1 — 10-hub seed)
    - .planning/phases/999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping/999.9-01-SUMMARY.md (multiplier matrix values)
    - .planning/phases/999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping/999.9-02-SUMMARY.md (planner shortlist + agent integration)
    - backend/agent/tools/lookup_rate.py (new signature for accuracy in docs)
    - data/scripts/generate_rate_table.py (ORIGIN_DEST_MULTIPLIER values for inclusion in docs)
  </read_first>
  <action>
    1. Read docs/architecture.md fully to map its current section structure.

    2. Add a NEW section titled "HQ/Branch Hub Network (Phase 999.9)" near the existing rate-table / pricing section. The section content (insert verbatim, adjust heading depth to match surrounding):

       ```markdown
       ### HQ/Branch Hub Network (Phase 999.9)

       Express ships from one HQ + 9 branches across Bangkok Metro, mirroring how Kerry / Flash / Thailand Post quote shipments — sender picks an origin branch, the system quotes a single-leg route from that branch to the destination.

       #### Hub seed

       Seeded from `data/raw/hubs.json` (mirrored to `frontend/data/hubs.json` for static-import on the UI). 10 hubs distributed across the three Bangkok Metro zones:

       - **central-1 (5 hubs):** `hq-lat-krabang` (HQ), `branch-bang-na`, `branch-nonthaburi`, `branch-pathum-thani`, `branch-samut-prakan`
       - **central-2 (3 hubs):** `branch-ayutthaya`, `branch-nakhon-pathom`, `branch-samut-sakhon`
       - **central-3 (2 hubs):** `branch-ratchaburi`, `branch-lop-buri`

       Distribution mirrors real Bangkok logistics density. Lat Krabang HQ is the canonical e-commerce / logistics cluster (Kerry, Flash, Lazada, Shopee all have facilities there).

       #### Origin × destination rate matrix

       The rate table grew from 45 rows (destination-zone-only) to **135 rows** (3 origin × 3 dest × 3 ship × 5 weight tiers). Lookup key: `(shipping_type, origin_zone, dest_zone, weight_kg)`.

       Multiplier matrix (`ORIGIN_DEST_MULTIPLIER` in `data/scripts/generate_rate_table.py`):

       | M | central-1 | central-2 | central-3 |
       |---|-----------|-----------|-----------|
       | **central-1** | 1.00 | 1.25 | 1.70 |
       | **central-2** | 1.25 | 1.00 | 1.45 |
       | **central-3** | 1.70 | 1.45 | 1.00 |

       Symmetric: `M[origin][dest] == M[dest][origin]`. Diagonal = 1.00 preserves v1.0 central-1 → central-1 rates byte-for-byte (e.g., `bounce 0-5kg = 55 THB`). Off-diagonal scales with zone distance.

       #### Origin capture: hybrid dropdown + prose

       1. **Dropdown (HubPicker):** persistent default per browser tab. Cold start = `hq-lat-krabang`. SessionStorage key: `express_origin_hub_id`.
       2. **Prose extraction:** Planner (Gemini 2.0 Flash) reads a 10-hub shortlist injected into its SYSTEM_PROMPT and extracts `origin_hub_id` from the user's message. Allowlist-validated against `_HUB_INDEX`; invalid emissions fall back to the dropdown's value.
       3. **Silent default:** when neither dropdown nor prose specifies a hub, `pricing_agent_node` defaults to `hq-lat-krabang` and emits a "Origin unspecified — defaulted to HQ Lat Krabang." bullet in its reasoning trace (D-09).

       #### Single-leg routing (D-04)

       Every chat turn makes ONE Google Maps Directions call: chosen hub → destination. The internal HQ→branch transfer is operational cost, NOT customer-facing pricing — matches Kerry / Flash / Thailand Post quoting behaviour.

       Cache key: `(origin_hub_id, destination)` — different hubs to the same destination are independent cache entries.
       ```

    3. UPDATE the existing rate-table / lookup_rate description elsewhere in the file (search for `lookup_rate` references) — the old description likely says `lookup_rate(shipping_type, zone, weight_kg)`. Change it to:
       ```
       lookup_rate(shipping_type, origin_zone, dest_zone, weight_kg)
       ```
       (Phase 999.9 D-05 signature.)

    4. UPDATE any zone enumeration that listed only `central-1, central-2, central-3` as DESTINATION zones — clarify these zones now apply to BOTH origin and destination dimensions of the rate matrix.

    5. If the file has a Mermaid diagram of the agent graph, leave it as-is (graph topology is unchanged). If it has an "Agent Inputs / State Schema" subsection, append `origin_hub_id: Optional[str] (Phase 999.9 D-08)` to the AgentState field list.
  </action>
  <verify>
    <automated>grep -q "HQ/Branch Hub Network" docs/architecture.md && grep -q "ORIGIN_DEST_MULTIPLIER" docs/architecture.md && grep -q "origin_hub_id" docs/architecture.md && grep -q "135 rows" docs/architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/architecture.md contains literal `HQ/Branch Hub Network`
    - docs/architecture.md contains literal `135 rows`
    - docs/architecture.md contains literal `ORIGIN_DEST_MULTIPLIER` OR a markdown table with the symmetric multiplier values (1.00 / 1.25 / 1.70 / 1.45)
    - docs/architecture.md contains all 10 hub_ids: hq-lat-krabang, branch-bang-na, branch-nonthaburi, branch-pathum-thani, branch-samut-prakan, branch-ayutthaya, branch-nakhon-pathom, branch-samut-sakhon, branch-ratchaburi, branch-lop-buri
    - docs/architecture.md contains literal `lookup_rate(shipping_type, origin_zone, dest_zone, weight_kg)` (new signature documented)
    - docs/architecture.md contains literal `origin_hub_id` (referenced as a state field)
    - docs/architecture.md contains literal `Origin unspecified — defaulted to HQ Lat Krabang` (D-09 narration documented)
  </acceptance_criteria>
  <done>
    docs/architecture.md has a complete HQ/Branch Hub Network section with hub list, multiplier matrix, hybrid dropdown+prose capture flow, silent-default narration, and single-leg routing rationale. Existing references to lookup_rate / zone updated to reflect the new signature.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/data-sources.md and README.md</name>
  <files>docs/data-sources.md, README.md</files>
  <read_first>
    - docs/data-sources.md (current — review structure; existing zone-definitions table is a good model for the hubs table)
    - README.md (current — find the Highlights / Features / Differentiators section)
    - data/raw/hubs.json (canonical 10-hub list with addresses)
    - .planning/phases/999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping/999.9-01-SUMMARY.md
  </read_first>
  <action>
    1. EDIT docs/data-sources.md — APPEND a new section after the existing zone definitions section:

       ```markdown
       ## HQ/Branch Hub Network (Phase 999.9)

       Source: `data/raw/hubs.json` (also mirrored to `frontend/data/hubs.json` for static-import on the UI).

       The Express network consists of 1 HQ + 9 branches across Bangkok Metro:

       | hub_id | Display name | Address (Google-geocodable) | Zone |
       |--------|--------------|------------------------------|------|
       | `hq-lat-krabang` | Express HQ — Lat Krabang Industrial Estate, Bangkok | Lat Krabang Industrial Estate, Bangkok | central-1 |
       | `branch-bang-na` | Express Branch — Bang Na, Bangkok | Bang Na, Bangkok | central-1 |
       | `branch-nonthaburi` | Express Branch — Mueang Nonthaburi | Mueang Nonthaburi, Nonthaburi | central-1 |
       | `branch-pathum-thani` | Express Branch — Mueang Pathum Thani | Mueang Pathum Thani, Pathum Thani | central-1 |
       | `branch-samut-prakan` | Express Branch — Mueang Samut Prakan | Mueang Samut Prakan, Samut Prakan | central-1 |
       | `branch-ayutthaya` | Express Branch — Phra Nakhon Si Ayutthaya | Phra Nakhon Si Ayutthaya, Ayutthaya | central-2 |
       | `branch-nakhon-pathom` | Express Branch — Mueang Nakhon Pathom | Mueang Nakhon Pathom, Nakhon Pathom | central-2 |
       | `branch-samut-sakhon` | Express Branch — Mueang Samut Sakhon | Mueang Samut Sakhon, Samut Sakhon | central-2 |
       | `branch-ratchaburi` | Express Branch — Mueang Ratchaburi | Mueang Ratchaburi, Ratchaburi | central-3 |
       | `branch-lop-buri` | Express Branch — Mueang Lop Buri | Mueang Lop Buri, Lop Buri | central-3 |

       **Distribution:** central-1 = 5 hubs, central-2 = 3 hubs, central-3 = 2 hubs. Mirrors real Bangkok logistics density; Lat Krabang HQ is the canonical e-commerce / logistics cluster.

       **Origin zone derivation:** at lookup time, `pricing_agent_node` resolves `origin_hub_id` -> `origin_zone` via the `origin_zone_for(hub_id)` helper in `backend/agent/tools/hubs.py`. The zone is then passed to `lookup_rate(shipping_type, origin_zone, dest_zone, weight_kg)`.

       **Refreshing:** the SQLite `hubs` table is rebuilt on every run of `python data/scripts/seed_database.py` via `if_exists="replace"` semantics — the script is idempotent, re-running produces the same 10-row table.
       ```

    2. EDIT README.md — find the Features / Differentiators / Highlights section (search for terms like "Highlights", "Features", "What it does"). APPEND a new bullet describing the Phase 999.9 capability:

       ```markdown
       - **HQ/Branch origin model.** Sender picks an origin hub from a 10-hub network (1 HQ in Lat Krabang + 9 branches across Bangkok Metro) via dropdown OR inline prose ("ship from Bang Na to Nonthaburi"). The agent calculates route + surcharge from the chosen hub, using a 135-row origin × destination rate matrix. Matches Kerry/Flash/Thailand Post quoting behaviour.
       ```

       If README.md does NOT have an obvious Features/Highlights bullet list, append the same content to a new section titled `### Phase 999.9 — HQ/Branch Origin Model` placed near the top architecture/feature description.

    3. Verify both files render correctly by checking the markdown structure (no broken tables, no extra heading levels).
  </action>
  <verify>
    <automated>grep -q "branch-lop-buri" docs/data-sources.md && grep -q "branch-bang-na" docs/data-sources.md && grep -q "HQ/Branch" docs/data-sources.md && grep -q "origin" README.md && grep -qi "hub" README.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/data-sources.md contains all 10 hub_ids verbatim (hq-lat-krabang through branch-lop-buri)
    - docs/data-sources.md contains the markdown table header `| hub_id | Display name | Address (Google-geocodable) | Zone |`
    - docs/data-sources.md contains literal `central-1 = 5 hubs, central-2 = 3 hubs, central-3 = 2 hubs`
    - docs/data-sources.md contains literal `origin_zone_for(hub_id)`
    - README.md contains literal `HQ/Branch` OR `HQ/branch` OR `hub network` (bullet describing the new capability)
    - README.md contains literal `Lat Krabang` (anchoring the canonical HQ name)
    - README.md contains literal `135-row` OR `135 rows` (rate matrix size called out)
  </acceptance_criteria>
  <done>
    docs/data-sources.md has the complete 10-hub table with addresses and zones; README.md mentions the HQ/Branch origin capability in the Highlights/Features section.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN-VERIFY — End-to-end live demo of HQ/Branch origin model</name>
  <files>(no file changes — manual verification of live system)</files>
  <read_first>
    - .planning/phases/999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping/999.9-UI-SPEC.md §"Interaction Contracts"
    - .planning/phases/999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping/999.9-CONTEXT.md (D-08 / D-09 / D-10 behavior)
  </read_first>
  <what-built>
    - Wave 1: 10-hub seed in data/raw/hubs.json + frontend/data/hubs.json; 135-row rate matrix in data/raw/express_rate_table.csv; SQLite hubs table; backend/agent/tools/hubs.py helper
    - Wave 2: AgentState.origin_hub_id; calculate_route(hub_id, dest); lookup_rate(ship, origin_zone, dest_zone, weight); pricing_agent D-09 narration; planner SYSTEM_PROMPT shortlist + allowlist validation + 999.1 merge + Phase 5 D-08 inheritance; ChatRequest.origin_hub_id; chat handler initial_state seeding (Pitfall 1 default)
    - Wave 3: HubPicker component (UI-SPEC styling); ChatColumn input-row composition (HubPicker above ChatInput); ChatApp lifted state + sessionStorage persistence; useChatStream.send forwards origin_hub_id; ChatInput placeholder updated
    - Wave 4 Tasks 1-2: docs/architecture.md + docs/data-sources.md + README.md updated
  </what-built>
  <how-to-verify>
    Run the live system and exercise these four flows. Each step lists the EXACT input + the EXACT expected outcome. Mark each as PASS or FAIL.

    **Setup (one-time per session):**
    1. From repo root: `python data/scripts/seed_database.py` — confirm stdout reports `135 rate table rows` AND `10 hubs`.
    2. Start the backend in one terminal: `uvicorn backend.api.main:app --reload --port 8000` — wait for "Application startup complete".
    3. Start the frontend in another terminal: `cd frontend && npm run dev` — wait for "Ready in XXXms".
    4. Open http://localhost:3000 in a fresh browser tab (NOT a tab with prior sessionStorage).

    **Flow 1: Cold-start default + dropdown rendering**
    Steps:
    a. Confirm the HubPicker is visible above the chat input textarea.
    b. The "From:" label is present in font-semibold.
    c. The selected option is `Express HQ — Lat Krabang Industrial Estate, Bangkok (central-1)`.
    d. Click the dropdown — confirm 10 options are listed in this order: HQ first, then 4 more central-1 branches, then 3 central-2, then 2 central-3.
    e. Browser DevTools > Application > Session Storage > http://localhost:3000 — confirm `express_origin_hub_id` does NOT exist yet (cold start hasn't yet written it). After any picker interaction (Flow 2), it should appear.

    Expected: All five points pass. Default = HQ. 10 options in D-03 order.

    **Flow 2: Dropdown override → backend reads origin_zone correctly**
    Steps:
    a. Pick `Express Branch — Phra Nakhon Si Ayutthaya (central-2)` from the dropdown.
    b. DevTools > Application > Session Storage — confirm `express_origin_hub_id = branch-ayutthaya` is now stored.
    c. Type into the textarea: `Ship 5kg bounce to Nonthaburi` (no hub mentioned in prose).
    d. Click Send.
    e. Watch the trace panel on the right. The Pricing Agent step's reasoning should NOT contain the literal "Origin unspecified — defaulted to HQ Lat Krabang." (because dropdown supplied the hub).
    f. Look at the final answer's surcharge_result.total. Compare against an HQ-origin same shipment by switching dropdown back to HQ and re-asking — the Ayutthaya origin total should be HIGHER (multiplier 1.25 vs 1.00 for central-2 → central-1 vs central-1 → central-1).

    Expected: sessionStorage updated, NO default-to-HQ bullet, total visibly higher than HQ-origin baseline.

    **Flow 3: Prose override beats dropdown**
    Steps:
    a. Set dropdown to `Express HQ — Lat Krabang Industrial Estate, Bangkok (central-1)` (HQ).
    b. Type into the textarea: `Ship 5kg bounce from Bang Na to Nonthaburi`.
    c. Click Send.
    d. Watch the planner trace step's tool_output JSON. The `origin_hub_id` field should be `"branch-bang-na"` (extracted from prose "from Bang Na").
    e. The Pricing Agent's surcharge_result reflects origin_zone=central-1 (Bang Na's zone — same as HQ's, so total may equal Flow 2 HQ-baseline; that's correct).
    f. Confirm the dropdown is STILL set to HQ (prose override is per-turn, dropdown is the persistent default — UI-SPEC §Interaction Contracts).

    Expected: Planner extracted branch-bang-na from prose; dropdown unchanged; trace JSON shows the override.

    **Flow 4: Default-to-HQ silent narration (when both dropdown AND prose miss)**
    Steps:
    a. Open browser DevTools > Application > Session Storage > clear `express_origin_hub_id` (simulates a state where sessionStorage is empty AND the cold-start default hasn't yet written).
    b. Hard-reload the page (Cmd+Shift+R / Ctrl+F5). The dropdown should be at HQ (default).
    c. Type: `What surcharge for 5kg bounce to Nonthaburi?` (no hub in prose; dropdown is HQ).
    d. The Pricing Agent will use HQ via the API-boundary default — surcharge_result computed against origin_zone=central-1.
    e. Note: the D-09 "Origin unspecified — defaulted to HQ Lat Krabang." bullet is NOT expected to appear in this flow because the API boundary always seeds 'hq-lat-krabang' (Pitfall 1). The bullet only fires when a request arrives with origin_hub_id explicitly omitted at the dispatch level — which the frontend cannot trigger. This is expected behaviour; it is documented in 999.9-02-SUMMARY.md.

    Expected: Surcharge computed correctly; no D-09 bullet (Pitfall 1 mitigation in effect — confirms it's working).

    **Flow 5: Page-reload sessionStorage persistence**
    Steps:
    a. Pick `Express Branch — Bang Na, Bangkok (central-1)` from the dropdown.
    b. Refresh the page (Cmd+R / F5).
    c. Confirm the dropdown comes back showing Bang Na as the selected option (post-mount sessionStorage read seeded the state).
    d. Open a NEW browser tab to http://localhost:3000 (NOT a copy of the existing tab — a brand new tab/window).
    e. The new tab's dropdown should be HQ (sessionStorage scope = single tab; new tab starts fresh).

    Expected: Same-tab refresh preserves selection. New tab/window starts at HQ default.

    **Flow 6 (OPTIONAL): Production build smoke**
    Run `cd frontend && npm run build` — confirm exit code 0 and no TypeScript or lint errors. The build artifact is what the demo recording (Phase 5 Plan 05-07 Task 4) would use.

    **Failure handling:** if any flow fails:
    - Capture the failing browser console errors (DevTools > Console)
    - Capture the backend logs (uvicorn stdout)
    - Capture the network request/response in DevTools > Network
    - Reply with: `FAIL Flow N: [paste console/logs/network details]`
    - DO NOT mark this task as approved.
  </how-to-verify>
  <resume-signal>Reply "approved" if all 5 (or 6) flows passed. Reply "failed Flow N: <details>" otherwise.</resume-signal>
  <action>Pause autonomous execution and surface the &lt;how-to-verify&gt; flows above to the user. The user runs the live demo (uvicorn + npm run dev), exercises Flow 1 through Flow 5 (Flow 6 optional) in their browser, and replies with a resume-signal. Do NOT proceed past this gate without an &quot;approved&quot; response.</action>
  <verify>
    <automated>echo "Manual checkpoint — verification deferred to human reply per resume-signal contract."</automated>
  </verify>
  <done>User replies &quot;approved&quot; (all flows PASS) or &quot;failed Flow N: ...&quot; (with details for the orchestrator to triage as a gap).</done>
</task>

</tasks>

<verification>
- All three doc files updated and committed
- Human-verify checkpoint passed (5 flows)
- No regressions in existing test suite (final run: `cd backend && pytest -q && cd ../frontend && npm test -- --run`)
</verification>

<success_criteria>
- docs/architecture.md has the HQ/Branch Hub Network section with hub list, multiplier matrix, hybrid capture flow, single-leg routing rationale
- docs/data-sources.md has the 10-hub table with addresses and zones
- README.md mentions the HQ/Branch origin capability
- Live demo passes all 5 flows in Task 3
- Phase 999.9 SUMMARY.md (cumulative — across all 4 plans) ready for commit
</success_criteria>

<output>
After completion, create `.planning/phases/999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping/999.9-04-SUMMARY.md` documenting:
- Doc files updated + section anchors
- Human-verify outcome (PASS/FAIL per flow with any deviations)
- Phase 999.9 cumulative state delta (test count delta across all 4 plans, file count, line count if convenient)
- Demo prompts that worked + any that produced unexpected behavior (HITL fires more often per Pitfall 7 — log if applicable)
- Recommended demo script for the v1.1 milestone recording (3 prompts that exercise dropdown / prose / cross-zone)
- Any deviations from the plan (and why)

Then update STATE.md:
- Mark Phase 999.9 as complete
- Add cumulative decisions list ([Phase 999.9]: ...)
- Update progress metrics
</output>
