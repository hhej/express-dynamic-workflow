---
phase: 999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping
plan: "01"
subsystem: database
tags: [hubs, sqlite, rate-table, lookup-rate, pandas, json, schema-migration]

# Dependency graph
requires:
  - phase: 02
    provides: lookup_rate v1 + 3-arg signature, RateResult model, sentinel weight_max_kg=999, half-open weight tiers
  - phase: 01
    provides: generate_rate_table.py + seed_database.py base (CSV → SQLite seeder pattern), zone_definitions.json shape template
provides:
  - 10-hub seed dataset (data/raw/hubs.json) — 1 HQ + 9 branches with hub_id PK
  - hubs.py loader module — _HUB_INDEX, origin_string_for, origin_zone_for helpers (mirrors _ZONE_INDEX pattern)
  - 135-row origin × destination rate matrix (3 origin × 3 dest × 3 ship × 5 weight)
  - ORIGIN_DEST_MULTIPLIER 3×3 symmetric matrix (D-06) replacing legacy single-zone multipliers
  - lookup_rate v2 — 4-arg signature (shipping_type, origin_zone, dest_zone, weight_kg)
  - hubs SQLite table (hub_id PK, name, address, zone) seeded by data/scripts/seed_database.py
  - mock_hubs_json conftest fixture for Wave 2 isolation
affects: [999.9-02 wave 2 (calculate_route hub_id signature, pricing_agent origin_zone resolution, planner hub_id extraction), 999.9-03 wave 3 (frontend HubPicker reads same hubs.json)]

# Tech tracking
tech-stack:
  added: []  # No new libraries — additive Python stdlib + pandas only
  patterns:
    - "Module-import-time lazy index pattern (mirrors _ZONE_INDEX in calculate_route.py)"
    - "Symmetric multiplier matrix with documented diagonal=1.0 invariant (preserves v1.0 byte-for-byte)"
    - "Schema migration via if_exists='replace' on rate_table + DELETE+INSERT on hubs (Phase 1 seeder convention)"

key-files:
  created:
    - data/raw/hubs.json
    - backend/agent/tools/hubs.py
    - backend/tests/test_hubs.py
  modified:
    - data/raw/express_rate_table.csv (45 → 135 rows; new schema)
    - data/scripts/generate_rate_table.py (REPLACED ZONE_MULTIPLIERS with ORIGIN_DEST_MULTIPLIER)
    - data/scripts/seed_database.py (added HUBS_JSON + seed_hubs)
    - backend/agent/tools/lookup_rate.py (3-arg → 4-arg signature)
    - backend/tests/conftest.py (added mock_hubs_json fixture)
    - backend/tests/test_rate_table.py (schema migrated; +6 999.9 tests)
    - backend/tests/test_lookup_rate.py (signature migrated; +3 999.9 tests; 1 renamed)
    - backend/tests/test_seed_database.py (135-row migration; +4 999.9 hubs tests)

key-decisions:
  - "Pitfall 3 honored: ORIGIN_DEST_MULTIPLIER diagonal = 1.0 preserves v1.0 central-1 rates byte-for-byte (bounce 0-5=55, retail_standard 0-5=50, retail_fast 0-5=65). Avoided the RESEARCH-flagged trap of stacking a 0.9x intra-zone multiplier on top, which would have silently re-priced every Phase 1-8 cached estimate."
  - "Off-diagonal multipliers calibrated symmetrically: one-zone-apart=1.25 (c1↔c2), two-zones-apart=1.70 (c1↔c3), c2↔c3=1.45 — D-06 symmetric matrix invariant verified by test_multiplier_symmetry across all 45 ship×weight combos."
  - "hubs.py mirrors _ZONE_INDEX pattern: _HUB_INDEX built once at module import time. No runtime invalidation — uvicorn restart picks up hubs.json edits. Trade-off: simplicity over hot-reload, matches Phase 2 D-08 baseline cache philosophy."
  - "lookup_rate signature change is additive in semantics but breaking in arity: existing pricing_agent callers will TypeError until Wave 2 Plan 999.9-02 Task 1 lands. PLAN.md verification block explicitly documents this as the planned inter-wave breakage."
  - "Hub display strings (the 'name' field in hubs.json) are FULL strings from UI-SPEC §Hub display strings — frontend renders verbatim with zero concatenation. UI-SPEC locks these literals."
  - "seed_hubs uses CREATE TABLE IF NOT EXISTS + DELETE + INSERT (rather than DROP + CREATE) so concurrent connections never see an empty table mid-reseed; idempotent reseed test confirms 10 rows persist across re-runs."
  - "ZONE_MULTIPLIERS literal token completely removed from generate_rate_table.py — comment phrasing changed to 'legacy single-zone multiplier dict' so test_no_zone_multipliers_constant_remains AND a literal grep both pass; zero risk of double-multiplication regressions."

patterns-established:
  - "Hub lookup helper module: _HUB_INDEX = _load_hub_index() at import; resolver functions raise ValueError with sorted allowed-list on unknown hub_id"
  - "Symmetric distance matrix as 3×3 nested dict with diagonal=1.0 (preserves baseline) — pattern reusable for future origin/dest pricing axes (e.g., per-vehicle-type pricing in v2)"
  - "Database schema migration: change CSV header order + run generator + run seeder; if_exists='replace' silently picks up new columns without explicit ALTER TABLE. Tests must update column names AND row counts in lockstep."

requirements-completed:
  - HUB-01  # 10 hubs seeded from data/raw/hubs.json with central-1=5 / central-2=3 / central-3=2 distribution
  - HUB-05  # lookup_rate extended to (shipping_type, origin_zone, dest_zone, weight_kg) signature
  - HUB-06  # Rate table extended to 135-row origin × destination matrix via extended generate_rate_table.py

# Metrics
duration: 9min
completed: 2026-05-10
---

# Phase 999.9 Plan 01: HQ/Branch Data Foundation Summary

**10-hub seed dataset + 135-row origin × destination rate matrix + symmetric ORIGIN_DEST_MULTIPLIER + 4-arg lookup_rate signature; v1.0 central-1 rates preserved byte-for-byte (Pitfall 3 honoured).**

## Performance

- **Duration:** 9 min
- **Started:** 2026-05-10T03:49:59Z
- **Completed:** 2026-05-10T03:59:44Z
- **Tasks:** 3 (all autonomous, all TDD)
- **Files modified:** 9 (3 created, 6 modified)
- **Tests added:** +13 net new (7 hub tests, 6 rate-table tests + monotonic-replaced-with-diagonal-cheapest, 3 lookup_rate tests, 4 seed_database hubs tests; 9 existing lookup_rate tests + 7 existing seed_database tests + 1 generator-docstring test updated for the new schema)
- **Wave 1 verification gate:** 57/57 green (`pytest tests/test_hubs.py tests/test_rate_table.py tests/test_lookup_rate.py tests/test_seed_database.py`)

## Accomplishments

- **10-hub seed (D-01/D-02/D-03):** `data/raw/hubs.json` ships 10 entries verbatim from CONTEXT D-03 — `hq-lat-krabang`, `branch-bang-na`, `branch-nonthaburi`, `branch-pathum-thani`, `branch-samut-prakan`, `branch-ayutthaya`, `branch-nakhon-pathom`, `branch-samut-sakhon`, `branch-ratchaburi`, `branch-lop-buri`. Zone distribution central-1=5, central-2=3, central-3=2.
- **Hub loader module (D-04):** `backend/agent/tools/hubs.py` exposes `_HUB_INDEX`, `origin_string_for(hub_id)`, `origin_zone_for(hub_id)` mirroring the `_ZONE_INDEX` pattern from `calculate_route.py` (single load at import time). Unknown `hub_id` raises `ValueError` with sorted allowed-list.
- **135-row rate matrix (D-05/D-07):** `data/raw/express_rate_table.csv` regenerated via extended `generate_rate_table.py`; CSV header is `shipping_type,origin_zone,dest_zone,weight_min_kg,weight_max_kg,base_rate_thb` (origin_zone BEFORE dest_zone). Rate range 50–765 THB.
- **Symmetric ORIGIN_DEST_MULTIPLIER matrix (D-06):** Replaces legacy single-zone multiplier dict. Diagonal=1.0 (preserves v1.0 central-1 base rates byte-for-byte). Off-diagonals: c1↔c2=1.25, c1↔c3=1.70, c2↔c3=1.45.
- **lookup_rate signature extended (D-05):** `lookup_rate(shipping_type, origin_zone, dest_zone, weight_kg)` — SQL WHERE adds both zone clauses; ValueError messages surface both zones for descriptive failure modes.
- **hubs SQLite table (D-01):** `seed_database.py` gains `HUBS_JSON` constant + `seed_hubs()` helper. `data/express.db` re-seeded — `rate_table` has 135 rows, `hubs` has 10 rows, idempotent across re-runs.

## Task Commits

Each task was committed atomically:

1. **Task 1: 10-hub seed + hubs.py + test_hubs.py + conftest fixture** — `6dbc444` (feat)
2. **Task 2: Regenerate rate table CSV with ORIGIN_DEST_MULTIPLIER + extend test_rate_table.py** — `9d30c7e` (feat)
3. **Task 3: Extend lookup_rate signature + seed_hubs + end-to-end re-seed** — `9e9ad27` (feat)

**Plan metadata commit:** Pending (this SUMMARY.md + STATE.md + ROADMAP.md update).

## Files Created/Modified

### Created

- `data/raw/hubs.json` — 10 hubs (1 HQ + 9 branches) with `{name, address, zone}` per hub_id
- `backend/agent/tools/hubs.py` — `_HUB_INDEX` index + `origin_string_for` + `origin_zone_for` helpers
- `backend/tests/test_hubs.py` — 7 tests covering D-01/D-02/D-03/D-04

### Modified

- `data/raw/express_rate_table.csv` — schema migrated (45 → 135 rows, new header)
- `data/scripts/generate_rate_table.py` — `ORIGIN_DEST_MULTIPLIER` 3×3 symmetric matrix replaces legacy single-zone dict; main() loops origin × dest × ship × weight
- `data/scripts/seed_database.py` — `HUBS_JSON` constant + `seed_hubs()` helper; main() reports rate/fuel/zones/hubs counts
- `backend/agent/tools/lookup_rate.py` — 3-arg → 4-arg signature; SQL WHERE adds origin_zone/dest_zone clauses; legacy single-zone WHERE removed
- `backend/tests/conftest.py` — `mock_hubs_json` fixture (3-hub minimal test set) for Wave 2 isolation
- `backend/tests/test_rate_table.py` — schema migrated to origin/dest; row count 45→135; replaced zone-monotonic test with diagonal-cheapest assertion (D-06 symmetry breaks v1.0 monotonic); +6 new tests under `TestPhase999_9Schema`
- `backend/tests/test_lookup_rate.py` — every existing call site updated to 4-arg form; `test_unknown_zone_raises` renamed to `test_unknown_dest_zone_raises`; +3 new D-05/D-06 tests
- `backend/tests/test_seed_database.py` — every assertion migrated from 45→135 + zone→origin_zone/dest_zone; +4 new tests under `TestPhase999_9SeedHubs`

## Decisions Made

### ORIGIN_DEST_MULTIPLIER — Final calibration

| origin \ dest | central-1 | central-2 | central-3 |
| ------------- | --------- | --------- | --------- |
| central-1     | **1.00**  | 1.25      | 1.70      |
| central-2     | 1.25      | **1.00**  | 1.45      |
| central-3     | 1.70      | 1.45      | **1.00**  |

**Symmetry verified:** `M[a][b] == M[b][a]` for all 9 cells (test_multiplier_symmetry runs 3 ship × 5 weight × 3 mirror pairs = 45 assertions).

**Diagonal preservation evidence (Pitfall 3):**

| Shipping type   | weight_min_kg | weight_max_kg | central-1 → central-1 base rate (THB) | Matches v1.0? |
| --------------- | ------------- | ------------- | ------------------------------------- | ------------- |
| bounce          | 0             | 5             | 55                                    | ✅ byte-for-byte |
| retail_standard | 0             | 5             | 50                                    | ✅ byte-for-byte |
| retail_fast     | 0             | 5             | 65                                    | ✅ byte-for-byte |
| bounce          | 5             | 10            | 85                                    | ✅ byte-for-byte |
| bounce          | 10            | 20            | 130                                   | ✅ byte-for-byte |
| bounce          | 20            | 50            | 220                                   | ✅ byte-for-byte |
| bounce          | 50            | 999           | 380                                   | ✅ byte-for-byte |

### hubs SQLite table schema

```sql
CREATE TABLE IF NOT EXISTS hubs (
  hub_id TEXT PRIMARY KEY,
  name TEXT,
  address TEXT,
  zone TEXT
);
```

10 rows after `seed_database.py` runs:

```
$ sqlite3 data/express.db "SELECT hub_id, zone FROM hubs ORDER BY zone, hub_id;"
branch-bang-na|central-1
branch-nonthaburi|central-1
branch-pathum-thani|central-1
branch-samut-prakan|central-1
hq-lat-krabang|central-1
branch-ayutthaya|central-2
branch-nakhon-pathom|central-2
branch-samut-sakhon|central-2
branch-lop-buri|central-3
branch-ratchaburi|central-3
```

### Test count delta

- **+7 hub tests:** test_load_hubs_returns_10_entries, test_zone_distribution, test_all_expected_hub_ids_present, test_origin_string_for_known_hub, test_origin_zone_for_known_hub, test_unknown_hub_id_raises_value_error, test_required_keys_per_hub
- **+6 rate-table tests:** test_135_rows_after_regeneration, test_diagonal_preserves_v1_central1_rates, test_multiplier_symmetry, test_origin_zone_column_before_dest_zone, test_origin_dest_multiplier_values, test_no_zone_multipliers_constant_remains
- **+3 lookup_rate tests:** test_lookup_rate_origin_dest_signature, test_origin_dest_central_1_to_central_3_returns_higher_rate, test_unknown_origin_zone_raises
- **+4 seed_database tests:** test_seed_hubs_inserts_10_rows, test_135_rows_after_seed, test_idempotent_reseed, test_hubs_schema
- **Updated existing:** test_unknown_zone_raises → test_unknown_dest_zone_raises (rename); 9 existing lookup_rate calls migrated to 4-arg form; 7 seed_database row-count assertions migrated 45→135; 1 generator-docstring test pinned to "ORIGIN_DEST_MULTIPLIER" string instead of pre-999.9 phrasing.

## Deviations from Plan

None — plan executed exactly as written.

One micro-adjustment worth noting (NOT a deviation):
- The plan's <action> step 1 for Task 2 included a documentation comment `# Phase 999.9 D-06: REPLACES legacy ZONE_MULTIPLIERS dict (Pitfall 3 — ...)`. This was rephrased to `# Phase 999.9 D-06: REPLACES legacy single-zone multiplier dict (Pitfall 3 — ...)` so the literal acceptance criterion `does NOT contain literal 'ZONE_MULTIPLIERS'` passes a strict grep. The semantic meaning is preserved; the historical token is removed. The new `test_no_zone_multipliers_constant_remains` test continues to enforce the invariant by checking `hasattr(module, 'ZONE_MULTIPLIERS')`.

## Issues Encountered

None — every test passed first run after each task's GREEN-phase implementation. The only minor friction:
- `git add data/express.db` initially failed because the SQLite DB is gitignored. Resolved by excluding it from the staged list — the DB is a generated artifact reproducible from `data/raw/express_rate_table.csv` + `data/raw/hubs.json` + `data/scripts/seed_database.py`, so committing it would couple the repo to a binary blob. The Phase 1-8 baseline already follows this convention.

## User Setup Required

None — no external service configuration required. Wave 2 will exercise the new schema end-to-end against the existing Google Maps + Gemini integrations.

## Next Phase Readiness

### Ready for Wave 2 (Plan 999.9-02)

- ✅ `data/raw/hubs.json` exists at module-import path used by `backend/agent/tools/hubs.py`
- ✅ `_HUB_INDEX` populated; downstream `pricing_agent_node` can call `origin_zone_for(hub_id)` directly
- ✅ `lookup_rate` accepts the new 4-arg signature
- ✅ `data/express.db` `rate_table` has 135 rows + `hubs` has 10 rows
- ✅ `mock_hubs_json` fixture available for downstream Wave 2 tests
- ✅ `data/scripts/generate_rate_table.py` exposes `ORIGIN_DEST_MULTIPLIER` for any test that needs to verify expected rates without re-implementing the math

### Known Wave 2 Pre-Condition (Documented in PLAN.md `<verification>`)

- ⚠️ `backend/agent/nodes/pricing_agent.py:460` still calls 3-arg `lookup_rate(shipping_type, zone, weight_kg)`. Any test that exercises pricing_agent end-to-end will fail with `TypeError: lookup_rate() missing 1 required positional argument: 'weight_kg'` until Wave 2 Plan 999.9-02 Task 1 (pricing_agent_node + calculate_route signature) lands. **This is expected and explicitly documented in PLAN.md `<verification>` block** — Wave 2 must run pricing_agent update FIRST so the existing test suite returns to all-green within one merge window.

### Wave 1 Test Suite Health

- `pytest tests/test_hubs.py tests/test_rate_table.py tests/test_lookup_rate.py tests/test_seed_database.py` → 57/57 green
- Full backend suite NOT re-run intentionally — `pricing_agent.py` 3-arg call site is the documented Wave 1 → Wave 2 inter-wave breakage. Re-running full suite now would surface known failures unrelated to Plan 999.9-01 acceptance.

## Self-Check: PASSED

All 12 declared file paths exist on disk; all 3 task commit hashes (6dbc444, 9d30c7e, 9e9ad27) exist in `git log --oneline --all`. Wave 1 verification gate `pytest tests/test_hubs.py tests/test_rate_table.py tests/test_lookup_rate.py tests/test_seed_database.py` returned 57/57 green at end of Task 3.

---
*Phase: 999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping*
*Plan: 01*
*Completed: 2026-05-10*
