---
phase: 999.9-hq-branch-origin-model-real-world-hub-to-destination-shipping
verified: 2026-05-12T13:00:00Z
status: passed
score: 6/6 must-haves verified
re_verification:
  is_re_verification: true
  reason: "Milestone v1.1 audit on 2026-05-12 flagged phase as unverified; retroactive VERIFICATION.md produced"
  previous_status: "N/A — no prior VERIFICATION.md existed"
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 9 (999.9): HQ/Branch Origin Model Verification Report

**Phase Goal:** Replace the implicit-origin model with explicit hub-based origin selection. Sender picks HQ or one of 9 branches via dropdown OR prose, and the agent calculates route + surcharge from the actual chosen hub to the destination — matching how Kerry/Flash/Thailand Post quote shipments. Bangkok Metro scope only.

**Verified:** 2026-05-12T13:00:00Z
**Status:** passed
**Re-verification:** Yes — retroactive verification after phase already shipped (2026-05-10). Milestone v1.1 audit on 2026-05-12 identified this phase as unverified.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `data/express.db` has 10 seeded hubs + 135-row rate table; `seed_database.py` is idempotent | ✓ VERIFIED | `sqlite3 express.db` returns `10` for hubs, `135` for rate_table; live run of `seed_database.py` reports "10 hubs" and "135 rate table rows" |
| 2 | `lookup_rate(shipping_type, origin_zone, dest_zone, weight_kg)` returns higher rates for cross-zone than intra-zone; multiplier matrix is symmetric | ✓ VERIFIED | `bounce/central-1/central-1/3kg = 55 THB`, `bounce/central-1/central-3/3kg = 94 THB`; `bounce/central-3/central-1/3kg = 94 THB` (symmetric); `ORIGIN_DEST_MULTIPLIER` diagonal=1.0, off-diagonals 1.25/1.45/1.70 |
| 3 | User picks origin via HubPicker dropdown; cold-start default = HQ Lat Krabang; choice persists via sessionStorage | ✓ VERIFIED | `HubPicker.tsx` is a substantive controlled `<select>` component (56 lines); `DEFAULT_HUB_ID='hq-lat-krabang'` in `constants.ts`; `HUB_PICKER_STORAGE_KEY='express_origin_hub_id'`; post-hydration `useEffect` in `ChatApp.tsx` reads and writes sessionStorage; 6 vitest tests pass |
| 4 | Prose override wins for a single turn; dropdown not mutated | ✓ VERIFIED | Planner `followup_query` token-detection block (planner.py ~line 451-484) nulls `parsed.origin_hub_id` when hub address tokens absent from user message; prose extraction tests `test_planner_extracts_origin_hub_id_from_prose` + `test_planner_followup_explicit_hub_in_prose_wins` pass; `useChatStream.send` accepts `originHubId?` param (dropdown value) but planner prose extraction overrides per turn |
| 5 | When no hub specified, agent silently defaults to HQ + Pricing Agent emits "Origin unspecified — defaulted to HQ Lat Krabang" bullet | ✓ VERIFIED | `pricing_agent_node` captures `raw_origin_hub_id = state.get("origin_hub_id")`; `origin_hub_was_unspecified = raw_origin_hub_id is None`; `_build_bullets` prepends "Origin unspecified — defaulted to HQ Lat Krabang." when flag is True (lines 213-216); `test_pricing_agent_default_to_hq_when_none` passes |
| 6 | Follow-up turn inherits `origin_hub_id`; route TTL cache key includes `origin_hub_id` | ✓ VERIFIED | `merged_origin_hub_id = parsed.origin_hub_id or state.get("origin_hub_id")` (planner.py line 502); `calculate_route` uses `key = (origin_hub_id, destination)` tuple (line 130); `_route_matches` compares `rd_hub == state_hub` when both present (lines 119-121); `test_planner_followup_inherits_origin_hub_id` and `test_calculate_route_cache_key_includes_hub_id` pass |

**Score: 6/6 truths verified**

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `data/raw/hubs.json` | 10 hubs (1 HQ + 9 branches), zone distribution 5/3/2 | ✓ VERIFIED | Present; 10 keys; c1=5, c2=3, c3=2 confirmed |
| `backend/agent/tools/hubs.py` | `_load_hub_index()`, `origin_zone_for()`, `hub_label_for()`, `origin_string_for()` | ✓ VERIFIED | 105 lines; all four helpers substantive; ValueError with sorted allowlist on unknown hub_id |
| `data/scripts/seed_database.py` | Idempotent seed of 10 hubs + 135-row rate table | ✓ VERIFIED | Reports "10 hubs" and "135 rate table rows" on re-run |
| `data/scripts/generate_rate_table.py` | `ORIGIN_DEST_MULTIPLIER` 3x3 symmetric matrix | ✓ VERIFIED | Matrix confirmed symmetric via programmatic check; diagonal=1.0, off-diagonals as documented |
| `backend/agent/tools/lookup_rate.py` | 4-arg signature `(shipping_type, origin_zone, dest_zone, weight_kg)` | ✓ VERIFIED | SQL WHERE includes both `origin_zone = ?` and `dest_zone = ?` (lines 66-69) |
| `backend/agent/state.py` | `origin_hub_id: Optional[str]` field | ✓ VERIFIED | Present at line 131 with documented Pitfall 1 note |
| `backend/agent/tools/models.py` | `RouteData.origin_hub_id Optional[str]` | ✓ VERIFIED | Field exists; `calculate_route` populates it at construction (line 172) |
| `backend/agent/tools/calculate_route.py` | `(origin_hub_id, destination)` signature; `(origin_hub_id, destination)` cache key | ✓ VERIFIED | Line 104 signature; line 130 tuple key |
| `backend/agent/nodes/route_agent.py` | Reads `state.origin_hub_id`, defaults to HQ, validates allowlist, forwards to `calculate_route` | ✓ VERIFIED | Lines 117-131 substantive; allowlist guard raises on unknown hub_id |
| `backend/agent/nodes/pricing_agent.py` | `origin_zone_for(hub_id)` + 4-arg `lookup_rate` + silent-default bullet | ✓ VERIFIED | Lines 509-514; `_build_bullets` emits D-09 bullet when `origin_hub_was_unspecified=True` |
| `backend/agent/nodes/planner.py` | Allowlist validation; 999.1 null-only merge; D-08 follow-up token detection; `_route_matches` hub-key compare | ✓ VERIFIED | Lines 383-395 (allowlist); 502 (merge); 451-484 (token detection); 118-126 (`_route_matches`) |
| `backend/agent/prompts/planner.py` | 10-hub shortlist injected in SYSTEM_PROMPT; `origin_hub_id` in JSON schema | ✓ VERIFIED | All 10 hub_ids present in SYSTEM_PROMPT; `"origin_hub_id"` in schema |
| `backend/api/models.py` | `ChatRequest.origin_hub_id Optional[str]` | ✓ VERIFIED | Lines 51-58; description documents allowlist validation |
| `backend/api/routes/chat.py` | API-boundary default `hq-lat-krabang`; seeds `initial_state["origin_hub_id"]` | ✓ VERIFIED | Lines 241-248 validate + default; line 259 seeds initial_state |
| `frontend/data/hubs.json` | Mirror of `data/raw/hubs.json` | ✓ VERIFIED | Byte-for-byte identical content confirmed |
| `frontend/components/chat/HubPicker.tsx` | Controlled `<select>` with glass-morphism styling, `aria-label="Origin hub"`, `htmlFor` | ✓ VERIFIED | 56 lines; substantive controlled component; ARIA contract honored |
| `frontend/components/ChatApp.tsx` | `originHubId` state; post-hydration sessionStorage seed; persistence; `handleSend` forwards hub | ✓ VERIFIED | Lines 44-68 (state + effects); `chat.send(message, originHubId)` called in `handleSend` |
| `frontend/hooks/useChatStream.ts` | `send(message, originHubId?)` forwards `origin_hub_id` in POST body | ✓ VERIFIED | Lines 130-147; `origin_hub_id: originHubId` included in `api.postChat` payload |
| `frontend/components/chat/ChatColumn.tsx` | HubPicker rendered as first child of input-row wrapper | ✓ VERIFIED | Lines 99-111; `<HubPicker>` is first child inside `flex flex-col gap-2` wrapper |
| `data/raw/zone_definitions.json` | Geocode aliases for `Lopburi` and `Phra Nakhon Si Ayutthaya` | ✓ VERIFIED | `central-3` includes both "Lop Buri" and "Lopburi"; `central-2` includes both "Ayutthaya" and "Phra Nakhon Si Ayutthaya" |
| `backend/config.py` | `BASELINE_DIESEL_PRICE` computed via `_compute_rolling_baseline(90)` (not static 29.94) | ✓ VERIFIED | `_compute_rolling_baseline` defined in config.py; env override preserved; current value = 36.4 THB/L |
| `frontend/lib/constants.ts` | `HUB_PICKER_STORAGE_KEY`, `DEFAULT_HUB_ID`, 6 hub-aware `EXAMPLE_PROMPTS` | ✓ VERIFIED | All three present; prompts include Bang Na, Ayutthaya, Samut Prakan, Lop Buri references |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `ChatApp.tsx` | `useChatStream.send` | `chat.send(message, originHubId)` in `handleSend` | ✓ WIRED | `originHubId` state forwarded on every send |
| `useChatStream` | `POST /api/chat` | `origin_hub_id: originHubId` in `api.postChat` body | ✓ WIRED | Present at line 146; undefined drops from JSON.stringify |
| `chat.py` `_fresh_stream` | `AgentState` | `initial_state["origin_hub_id"] = resolved_hub_id` | ✓ WIRED | API-boundary default `hq-lat-krabang` lands before state construction |
| `planner_node` | `merged_origin_hub_id` | `parsed.origin_hub_id or state.get("origin_hub_id")` (null-only merge) | ✓ WIRED | Line 502; follow-up inheritance confirmed by test |
| `planner_node` | `route_agent_node` | `origin_hub_id` in state returned by planner | ✓ WIRED | `merged_origin_hub_id` written to return state |
| `route_agent_node` | `calculate_route` | `calculate_route(origin_hub_id, destination)` | ✓ WIRED | Line 145; hub address resolved via `origin_string_for` |
| `calculate_route` | `_route_cache` | `key = (origin_hub_id, destination)` tuple | ✓ WIRED | Line 130; `_route_matches` in planner also uses hub-key compare |
| `pricing_agent_node` | `lookup_rate` | `origin_zone_for(origin_hub_id)` then `lookup_rate(shipping_type, origin_zone, dest_zone, weight_kg)` | ✓ WIRED | Lines 509-525; 4-arg call verified |
| `_build_bullets` | narration | `hub_label_for(origin_hub_id)` + `origin_zone_for(origin_hub_id)` in bullet 1 text | ✓ WIRED | Lines 218-223; hub_label surfaces in chat answer |
| `HubPicker.tsx` | `ChatColumn.tsx` | `hubs`, `value`, `onChange`, `disabled` props | ✓ WIRED | Lines 100-105 of ChatColumn; `onHubChange` wired to `handleHubChange` in ChatApp |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `HubPicker.tsx` | `hubs: Hub[]` | `frontend/data/hubs.json` via static-import in `ChatApp.tsx` | Yes — `HUBS_LIST` built from real JSON at module level | ✓ FLOWING |
| `pricing_agent_node` | `rate.base_rate` | `lookup_rate(shipping_type, origin_zone, dest_zone, weight_kg)` → SQLite `rate_table` (135 rows) | Yes — SQL WHERE on both origin and dest zone | ✓ FLOWING |
| `pricing_agent_node` | `origin_zone` | `origin_zone_for(origin_hub_id)` → `_HUB_INDEX` (hubs.json loaded at import) | Yes — real JSON data, 10 hubs | ✓ FLOWING |
| `planner_node` | `merged_origin_hub_id` | prose extraction (LLM) or sessionStorage via `ChatRequest.origin_hub_id` | Yes — LLM extracts from real user message; API-boundary validates against live `_HUB_INDEX` | ✓ FLOWING |
| `_build_bullets` narration | `hub_label` | `hub_label_for(origin_hub_id)` → `_HUB_INDEX` | Yes — narration text uses real hub name | ✓ FLOWING |
| `config.py` `BASELINE_DIESEL_PRICE` | rolling 90d mean | `_compute_rolling_baseline(90)` reads `data/raw/eppo_diesel_prices.csv` | Yes — 36.4 THB/L (dynamic); static fallback only when CSV missing | ✓ FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command / Check | Result | Status |
|----------|----------------|--------|--------|
| 10 hubs with correct zone distribution | `_HUB_INDEX` count + zone breakdown | 10 hubs (c1=5, c2=3, c3=2) | ✓ PASS |
| Cross-zone rate > intra-zone at same weight tier | `lookup_rate('bounce','central-1','central-3',3.0)` vs `lookup_rate('bounce','central-1','central-1',3.0)` | 94 > 55 THB | ✓ PASS |
| Multiplier matrix is symmetric | `ORIGIN_DEST_MULTIPLIER[a][b] == ORIGIN_DEST_MULTIPLIER[b][a]` for all pairs | True (all 9 cells) | ✓ PASS |
| `AgentState` and `ChatRequest` have `origin_hub_id` | annotation + instantiation check | Both confirmed | ✓ PASS |
| API-boundary default resolves unknown hub to HQ | Allowlist check logic | `'hq-lat-krabang'` returned for invalid input | ✓ PASS |
| All 10 hub IDs in planner SYSTEM_PROMPT | String membership check | True | ✓ PASS |
| `seed_database.py` idempotent | Re-run output | "135 rate table rows, 10 hubs" | ✓ PASS |
| Backend pytest suite | `python3 -m pytest backend/tests/ -q` | **358 passed** in 10.76s | ✓ PASS |
| Frontend vitest suite | `npm test --prefix frontend -- --run` | **145 passed** (30 test files) | ✓ PASS |
| Hub-specific backend tests (57 Wave-1 gate) | `pytest test_hubs.py test_rate_table.py test_lookup_rate.py test_seed_database.py` | **57 passed** | ✓ PASS |
| Agent integration tests | `pytest test_pricing_agent.py test_planner.py test_api_chat.py test_calculate_route.py test_route_agent.py` | **93 passed** | ✓ PASS |
| D-09 silent-default narration bullet (unit path) | `pytest test_pricing_agent.py -k default_to_hq` | **1 passed** | ✓ PASS |
| Follow-up hub inheritance (HUB-08) | `pytest test_planner.py -k followup_inherits` | **2 passed** | ✓ PASS |
| Route cache key includes hub_id (HUB-08) | `pytest test_calculate_route.py -k cache_key` | **1 passed** | ✓ PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| HUB-01 | Plan 01 | 10 hubs (1 HQ + 9 branches) seeded from `data/raw/hubs.json`, zone distribution 5/3/2 | ✓ SATISFIED | `hubs.json` has 10 entries; `data/express.db` hubs table has 10 rows; zone distribution matches |
| HUB-02 | Plan 03 (final mark Plan 04) | HubPicker dropdown in UI; default = HQ Lat Krabang; sessionStorage persistence | ✓ SATISFIED | `HubPicker.tsx` substantive; `DEFAULT_HUB_ID='hq-lat-krabang'`; sessionStorage effects in `ChatApp.tsx`; 6 vitest tests pass |
| HUB-03 | Plan 02 (final mark Plan 04) | Prose origin override for single turn | ✓ SATISFIED | Planner D-08 token-detection + null-only merge; `test_planner_extracts_origin_hub_id_from_prose` passes |
| HUB-04 | Plan 02 | Silent default to HQ when no hub specified; Pricing Agent narrates assumption | ✓ SATISFIED | `origin_hub_was_unspecified` flag captured before default applied; `_build_bullets` prepends "Origin unspecified — defaulted to HQ Lat Krabang."; unit test passes |
| HUB-05 | Plan 01 | `lookup_rate` extended to 4-arg signature; `origin_hub_id -> origin_zone` resolution | ✓ SATISFIED | `lookup_rate(shipping_type, origin_zone, dest_zone, weight_kg)` live; `origin_zone_for` called in `pricing_agent_node` line 514 |
| HUB-06 | Plan 01 | Rate table extended to 135-row origin×destination matrix | ✓ SATISFIED | `data/express.db` rate_table has 135 rows; generator uses `ORIGIN_DEST_MULTIPLIER` 3×3 matrix |
| HUB-07 | Plan 02 | Planner extracts `origin_hub_id` from user message with allowlist validation; falls back on invalid | ✓ SATISFIED | `planner.py` lines 383-395 (allowlist validation); lines 451-484 (follow-up token detection); 7 hub planner tests pass |
| HUB-08 | Plan 02 | Route TTL cache key includes `origin_hub_id`; follow-up turn inherits `origin_hub_id` | ✓ SATISFIED | `calculate_route` cache key is `(origin_hub_id, destination)` tuple; `_route_matches` compares hub ids; `merged_origin_hub_id = parsed.origin_hub_id or state.get("origin_hub_id")` in planner |

**All 8 HUB requirements: SATISFIED**

Note: HUB-04, HUB-07, and HUB-08 do not appear in any plan's `requirements-completed` frontmatter (Plans 02 and 03 used `PHASE-999.9-D-XX` design-decision markers instead of REQ-IDs; Plan 04 only listed HUB-02 and HUB-03 as final marks). However, codebase inspection confirms all three are fully implemented and tested. This is a SUMMARY-frontmatter labeling gap only — not a functional gap.

---

### Plan 04 Inline Calibration Verification

The live human-verify in Plan 04 Task 3 surfaced 5 calibration gaps fixed inline. Each is verified present in the codebase:

| Commit | Gap | Fix Verification | Status |
|--------|-----|-----------------|--------|
| `64991cc` | Planner emitted `missing_fields=["origin"]` when `origin_hub_id` was set but no prose `origin` present | `planner.py` line 517: `if not merged_origin and not merged_origin_hub_id:` — hub_id satisfies the origin requirement | ✓ PRESENT |
| `b8dccf3` | Pricing narration not hub-aware; bullets only in trace panel not in chat answer | `_build_bullets` takes `origin_hub_id` and emits hub label + origin_zone in bullet 1 (lines 218-223); `hub_label_for` imported at line 207 | ✓ PRESENT |
| `8a6ddbb` | Static `BASELINE_DIESEL_PRICE = 29.94` caused cap saturation at current 2026 prices | `config.py` uses `_compute_rolling_baseline(90)` reading EPPO CSV; current value 36.4 THB/L; env override preserved | ✓ PRESENT |
| `3414325` | Google Maps returns `Lopburi` (no space) and `Phra Nakhon Si Ayutthaya` not in zone index | `zone_definitions.json` central-3 includes both "Lop Buri" and "Lopburi"; central-2 includes both "Ayutthaya" and "Phra Nakhon Si Ayutthaya" | ✓ PRESENT |
| `a95babf` | Demo chips still showed generic "Bangkok" prompts — no hub-specific behavior visible | `frontend/lib/constants.ts` has 6 hub-aware `EXAMPLE_PROMPTS` covering Bang Na, Ayutthaya, Samut Prakan, Lop Buri | ✓ PRESENT |
| `51bf35d` | Screenshots stale (v1.0 UI, no HubPicker) | `docs/screenshots/` has 5 PNG files updated 2026-05-10 (2.3–2.7 MB each — not empty) | ✓ PRESENT |

All 5 calibration fixes (+ screenshot refresh) verified present in codebase.

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| Plans 02 + 03 `requirements-completed` | Use `PHASE-999.9-D-XX` design-decision markers instead of `HUB-XX` REQ-IDs (Plans 02 and 03 list no HUB-04/07/08) | Info | Tech debt only; all 8 HUB requirements are satisfied in the codebase. Not a functional gap. Future plans should use REQ-IDs in `requirements-completed` frontmatter for consistency. |
| `frontend/components/ChatApp.tsx` | The word "placeholder" appears 6 times — all in comments describing the HITL pause UX ("placeholder pending-assistant slot") | Info | Not a stub pattern; all references describe intentional UX behavior during the approval gate flow. No content returns `null` or empty JSX. |

No blocker or warning anti-patterns found.

---

### Human Verification Required

The following items from ROADMAP.md Success Criterion 7 are inherently manual:

1. **Cold-start default + dropdown rendering**
   **Test:** Open `http://localhost:3000` in a fresh browser tab with sessionStorage cleared. Observe HubPicker above the chat input, value = "Express HQ — Lat Krabang Industrial Estate, Bangkok (central-1)".
   **Expected:** Dropdown visible, correct default, glass-morphism styling matches ChatInput.
   **Why human:** Visual styling (focus ring, glass-surface, accent-button hierarchy), native `<select>` chevron rendering on macOS/Chrome cannot be verified programmatically.

2. **Dropdown override → backend reads correct origin_zone**
   **Test:** Pick "Express Branch — Phra Nakhon Si Ayutthaya (central-2)" from dropdown. Send "Calculate surcharge for 5kg bounce to Nonthaburi". Check trace panel.
   **Expected:** `origin_hub_id="branch-ayutthaya"` in route_agent trace; surcharge reflects 1.25× multiplier (central-2 → central-1) vs HQ baseline.
   **Why human:** Real Google Maps geocoding of hub addresses + real Gemini extraction; mocked tests verify the contract but not the live data path.

3. **Prose override beats dropdown — per turn only**
   **Test:** With dropdown on Ayutthaya, send "Ship 5kg bounce from Bang Na to Nonthaburi". Check trace + dropdown state.
   **Expected:** Planner trace shows `origin_hub_id="branch-bang-na"` (prose extracted); dropdown remains on Ayutthaya (not mutated).
   **Why human:** Real LLM extraction with live SYSTEM_PROMPT containing 10-hub shortlist; only live Gemini can prove prose extraction end-to-end.

4. **Default-to-HQ narration bullet NOT firing at API level**
   **Test:** POST `/api/chat` with no `origin_hub_id` field. Observe assistant response + trace panel.
   **Expected:** No "Origin unspecified" bullet visible to user; API boundary default is transparent.
   **Why human:** Requires live stack to verify the Pitfall 1 suppression is end-to-end (unit test covers the unit path only).

5. **sessionStorage persistence across reload + cross-tab isolation**
   **Test:** Pick a non-HQ hub, reload the tab; observe picker restores. Open a new tab; observe it starts at HQ default (sessionStorage is per-tab).
   **Why human:** Browser sessionStorage per-tab scope cannot be exercised in jsdom.

Status: **Human-verify checkpoint was executed live on 2026-05-10 by the user (Plan 04 Task 3). User replied `approved` after 5 inline calibration fixes. This constitutes the human verification for all 5 flows. No re-verification currently required.**

---

### Gaps Summary

No functional gaps found. The phase goal is fully achieved:

- All 10 hubs seeded and queryable from both backend (hubs.py) and frontend (static-import)
- 135-row rate matrix with symmetric ORIGIN_DEST_MULTIPLIER; cross-zone rates are correctly higher
- HubPicker dropdown rendered adjacent to chat input; sessionStorage persistence working
- Prose override wired through planner token-detection; allowlist validation in place
- Silent-default narration bullet implemented in `_build_bullets`; fires only on direct unit path (API boundary always seeds HQ)
- Route cache key includes `origin_hub_id`; follow-up inheritance via null-only merge
- All 8 HUB requirements satisfied in codebase; all 358 backend + 145 frontend tests pass
- 5 Plan 04 calibration fixes (planner origin satisfier, hub-aware narration, rolling baseline, geocode aliases, hub-aware demo prompts) all present

The only non-functional gap is the SUMMARY-frontmatter labeling inconsistency (Plans 02 and 03 used `PHASE-999.9-D-XX` markers instead of `HUB-XX` REQ-IDs). This is tech debt in planning artifacts, not a codebase defect.

---

_Verified: 2026-05-12T13:00:00Z_
_Verifier: Claude (gsd-verifier) — retroactive per v1.1 milestone audit_
