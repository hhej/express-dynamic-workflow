# Phase 999.11 — Hypothesis (c) cold-start: evidence

**Generated:** 2026-05-11
**Tested:** D-06 four-step disambiguation protocol (5 fresh-uvicorn runs per step × 4 steps = 20 runs total)

## Verdict

Hypothesis (c) cold-start: RULED OUT

## Evidence

| Step | Flags | Runs PASS_UNDER_30S | Median first_answer_ms | Logs |
|------|-------|---------------------|------------------------|------|
| 1 | (none) | 0/5 | n/a (no `answer` ever) — `done` at ~10,142 ms | repro/logs/step1/summary.jsonl |
| 2 | --warmup-first | 0/5 | n/a (no `answer` ever) — `done` at ~9,719 ms | repro/logs/step2/summary.jsonl |
| 3 | --skip-coldstart-refresh | 0/5 | n/a (no `answer` ever) — `done` at ~10,509 ms | repro/logs/step3/summary.jsonl |
| 4 | --warmup-first --skip-coldstart-refresh | 0/5 | n/a (no `answer` ever) — `done` at ~10,742 ms | repro/logs/step4/summary.jsonl |

**Per-step event-type sequence (identical across all 20 runs):**
`["meta", "trace", "trace", "trace", "error", "done"]`

**Per-step trace-agent sequence (identical across all 20 runs):**
`planner -> fuel_agent -> planner -> [ValueError raised in route_agent_node]`

**Key timing observations:**
- All 20 runs terminate cleanly at ~10 seconds — well under the 30s D-09 budget for "no answer."
- Warmup (step 2 & step 4) consistently completes in ~2 seconds (proves the warmup mechanism works).
- The original "60s hang with body=0 bytes" symptom from the backlog entry is NOT what reproduces in this environment. The current symptom is a fast deterministic ValueError, NOT a latency-driven hang.
- Median `done_ms` is ~10s across all steps — there is no latency penalty from cold-start that any (c) mitigation can address. Cold start adds ~1s readiness time (separately tracked in `readiness_s`), excluded from request timing per D-09.

## Stderr highlights

**Step 1 (baseline) — repro/logs/step1/run-001-uvicorn.stderr (lines 5-10, representative; identical across all 5 runs):**
```
fuel: level 1 attempt 1 failed: scrape selectors: capture live HTML first
fuel: level 1 attempt 2 failed: scrape selectors: capture live HTML first
fuel: level 1 attempt 3 failed: scrape selectors: capture live HTML first
Chat stream failed
ValueError: route_agent_node requires 'destination' in state; got destination=None
During task with name 'route_agent' and id '...'
```

**Step 2 (--warmup-first) — repro/logs/step2/run-001-uvicorn.stderr:**
Identical pattern. The warmup POST runs cleanly first (~2s, sub-30s); the real legit-baseline POST that follows produces the SAME `route_agent_node requires 'destination'` ValueError. Pool-warming neutralized cold-start handshake costs, but did NOT change the failure mode.

**Step 3 (--skip-coldstart-refresh) — repro/logs/step3/run-001-uvicorn.stderr:**
Identical pattern. The `EXPRESS_SKIP_COLDSTART_REFRESH=1` env flag was set in the subprocess env (verified by `"skip_coldstart_refresh": true` in summary.jsonl). Removing concurrent EPPO refresh contention did NOT change the failure mode.

**Step 4 (combined --warmup-first --skip-coldstart-refresh) — repro/logs/step4/run-001-uvicorn.stderr:**
Identical pattern. With BOTH (c) mitigations applied, the failure is BIT-IDENTICAL to step 1. Conclusive proof that (c) is not the proximate cause.

**Trace pattern at the time of failure (representative run from step 1 run-001, see repro/logs/step1/run-001-sse.log):**
- step 1 = planner, `next_step="fetch_fuel"`, `destination=null`, `missing_fields=[shipping_type, weight_kg, destination]`
- step 2 = fuel_agent, completes successfully (`status="ok"`, `price=39.95`, eppo_cached_csv)
- step 3 = planner (re-entered!), `next_step="fetch_route"`, `destination=null` STILL, `missing_fields=[shipping_type, weight_kg, destination]` STILL
- ValueError raised inside `route_agent_node` at `backend/agent/nodes/route_agent.py:120` because `destination is None`.

## Conclusion

**Hypothesis (c) cold-start is RULED OUT** with the strongest possible evidence: step 4 (combined `--warmup-first --skip-coldstart-refresh`) reproduces the exact same failure 5/5 times with identical event-type and trace-agent sequences. Pool warming + skipping the EPPO refresh task changed timing slightly but did not change the failure mode. The bug is not latency-driven and not contention-driven; it is a deterministic planner-routing error.

**The smoking gun for hypothesis (b) — planner re-loop:** the SSE trace sequence `planner -> fuel_agent -> planner` (step 3 = planner, exactly as the backlog entry called out) matches CONTEXT.md's described pattern. Worse, step 3's planner output asserts `next_step="fetch_route"` even though `destination` is null and `missing_fields` still contains `destination`. The planner is making a routing decision that contradicts the state it just read. `route_agent_node` then raises `ValueError` per D-10 (selective ValueError catch on missing origin/destination, Plan 05-09).

**Note on the original "60s hang" symptom:** the bug we are reproducing in this environment is NOT a 60s hang — it is a fast ~10s deterministic ValueError. This may mean: (1) the live SSE hang was a downstream consequence the user observed (e.g., the stream framing/closing behaviour around the error event); (2) the original 60s hang was an EARLIER variant of this bug that has since been masked by the ValueError; or (3) the symptom shifted between the backlog entry write-up (260509-utd live probe) and now. Either way, the reproduction failure 5/5 across all 4 steps demonstrates a CRITICAL bug exists on the legit-baseline path — Plan 03 (hypothesis (b)) should now drill into the planner re-loop.

## Hand-off to Plan 03 (hypothesis (b))

Hypothesis (c) is RULED OUT. Step 4 (`--warmup-first --skip-coldstart-refresh`) still produced 5/5 hangs (see step4/summary.jsonl), proving the cause persists even with cold-start mitigations applied.

Proceeding to Plan 999.11-03 (hypothesis (b): planner re-loop / `tool_call_count` reducer interaction with parallel fan-out).

**Strong hint for Plan 03:** the planner output at step 3 routes to `fetch_route` despite `destination=None`. This is the cache-aware override or fan-out promotion logic mis-firing on a destinationless baseline question. The 260509-utd guard_input layer + the 999.9 origin_hub_id default + the Plan 05-03/05-04 cache override cascade all interact at planner entry. Trace evidence pinpoints the planner_node code path, not the reducer-aggregation invariant.

Per D-04 discretion, the warmup task is NOT being added defensively — only the confirmed-root-cause fix gets committed. CONTEXT.md Claude's Discretion: "Whether to add the warmup query as a defensive lifespan task even if (c) is ruled out — only if cheap and contained." Skipping for cleanliness — Plan 03 will own the real root-cause fix.
