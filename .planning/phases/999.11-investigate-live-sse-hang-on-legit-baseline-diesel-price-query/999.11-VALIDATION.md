---
phase: 11
slug: live-sse-hang-root-cause-fix
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-11
---

# Phase 11 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x |
| **Config file** | `backend/pytest.ini` (or `pyproject.toml`) |
| **Quick run command** | `cd backend && pytest -x --ff -q` |
| **Full suite command** | `cd backend && pytest -q` |
| **Estimated runtime** | ~45 seconds (baseline 295/295) |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && pytest -x --ff -q` (fail-fast, last-failed-first)
- **After every plan wave:** Run `cd backend && pytest -q` (full suite must remain ≥295/295 green plus new regression)
- **Before `/gsd:verify-work`:** Full suite must be green AND the live-bar harness (5 fresh-uvicorn runs) must each produce `answer` within 30s (D-09)
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| _Populated by gsd-planner_ | _01/02/03_ | _0/1/2_ | FIX-02 | _unit/integration/smoke_ | _populated_ | _populated_ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

> Planner MUST emit one row per task, mapping each to one of the three regression-test shapes from RESEARCH §"Validation Architecture":
> - (a) SSE termination → integration test in `backend/tests/test_api_chat.py` using `app_with_mocks`
> - (b) reducer / re-loop → unit test in `backend/tests/test_planner.py` (direct node invocation)
> - (c) cold-start → smoke test in `backend/tests/test_coldstart_fuel_refresh.py` using `app_with_stub_refresh`

---

## Wave 0 Requirements

- [ ] Repro harness scaffold at `.planning/phases/999.11/repro/` — Python httpx script, fresh-uvicorn orchestration, dual-capture (SSE stream + uvicorn stderr) with wall-clock + elapsed-ms timestamps per D-03/D-04
- [ ] No new pytest framework install — existing `conftest.py` fixtures (`app_with_mocks`, `app_with_stub_refresh`, `in_memory_checkpointer`) cover all three regression-test shapes per RESEARCH

*Existing test infrastructure covers all phase requirements once the root cause is confirmed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live verification bar (D-09): 5 fresh-uvicorn runs of `"What's the current diesel price in Bangkok?"` each emit `answer` SSE event within 30s | FIX-02 | Requires live Gemini + Google Maps + EPPO scrape; CI-incompatible per D-10 "no live network" | 1) Restart uvicorn (`pkill -f uvicorn; uvicorn backend.api.main:app --port 8000`). 2) Run `python .planning/phases/999.11/repro/probe.py`. 3) Confirm `answer` event arrives ≤30s. 4) Repeat 4 more times with fresh uvicorn between each. Record elapsed-ms per run in SUMMARY.md. |
| Confirmed root cause documented in SUMMARY.md with evidence (no diagnosis-by-mitigation, D-02) | FIX-02 | Human judgment on evidence quality | Reviewer reads SUMMARY.md and confirms the cited evidence (uvicorn stderr lines, SSE event log, unit test output) uniquely identifies one of (c)/(b)/(a) and rules out the others per D-05 order. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (repro harness scaffold)
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] Backend suite remains ≥295/295 green + new regression test
- [ ] Live bar (D-09) confirmed via manual verification table above
- [ ] `nyquist_compliant: true` set in frontmatter after planner populates per-task map

**Approval:** pending
