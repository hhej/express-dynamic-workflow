---
phase: 999.11-investigate-live-sse-hang-on-legit-baseline-diesel-price-query
plan: 04
subsystem: backend/api
tags: [sse, hang, hypothesis-a, ruled-out, defense-in-depth, fix-02, demo-gate, w6, integration-test, app-with-mocks]

# Dependency graph
requires:
  - phase: 999.11-01-repro-harness
    provides: "probe_legit_baseline.py + run_5x.sh — 5-fresh-uvicorn live verification driver"
  - phase: 999.11-02-hypothesis-c-cold-start
    provides: "Hypothesis (c) RULED OUT verdict — clears the path for (b) test"
  - phase: 999.11-03-hypothesis-b-planner-reloop
    provides: "Hypothesis (b) CONFIRMED + FIXED at commit e550256 — destination-less short-circuit in planner_node closes the live SSE hang; phase root cause CLOSED before Plan 04 ran"
provides:
  - "Hypothesis (a) SSE termination RULED OUT — static analysis of response_node returns (every branch carries final_payload), _fresh_stream control flow (only intentional approval_required early-return), and graph.py response→END wiring proves variants 1/2/3 are structurally impossible"
  - "Integration probe via app_with_mocks confirmed SSE plumbing emits meta→trace×6→answer→done cleanly with answer.status='ok' on the legit-baseline shape (post-Plan-03 5/5 PASS_UNDER_30S runs already demonstrated this empirically)"
  - "Permanent CI guard test_legit_baseline_diesel_price_query_completes in backend/tests/test_api_chat.py pins the SSE plumbing answer+done invariant on the legit-baseline shape — additive coverage, deterministic, offline (no Gemini / Google Maps / EPPO network calls)"
  - "Phase-wide D-10 invariant honored: exactly ONE D-10 pinning test for Phase 11 / FIX-02 across the codebase (on test_planner.py from Plan 03's CONFIRMED path); this defense-in-depth test does NOT carry the D-10 marker"
  - "No backend production code modified — chat.py, response_node.py, graph.py, planner.py all untouched (matches plan's must_haves.truths.3 RULED OUT contract)"
affects: [999.11-05-summary-and-live-bar]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Evidence-first verdict before code change (D-02 'no diagnosis-by-mitigation' — Task 1 committed EVIDENCE.md as a docs-only commit awaiting human reviewer approval before Task 3 lands the test)"
    - "Defense-in-depth integration guard test landing on the RULED OUT branch — pins the SSE plumbing invariant per the plan's must_haves regardless of verdict; passes on first run because the contract is already correct"
    - "Phase-wide D-10 pinning test invariant enforced: exactly ONE test per phase carries the literal 'D-10 pinning test: Phase XX / FIX-NN' marker; defense-in-depth tests use the 'defense-in-depth invariant: Phase XX / FIX-NN — additive coverage, not the D-10 pin' marker (mirrors Plan 03's pattern)"
    - "Static analysis trio: response_node return audit + _fresh_stream control flow audit + _drain_events answer-emit contract audit — three independent structural arguments converge on RULED OUT; supplemented by integration probe for end-to-end verification"

key-files:
  created:
    - ".planning/phases/999.11-investigate-live-sse-hang-on-legit-baseline-diesel-price-query/999.11-04-SUMMARY.md (this file)"
  modified:
    - "backend/tests/test_api_chat.py (+89 lines — test_legit_baseline_diesel_price_query_completes integration guard with defense-in-depth marker)"
    - ".planning/phases/999.11-investigate-live-sse-hang-on-legit-baseline-diesel-price-query/999.11-04-EVIDENCE.md (+58 lines — 'Escalation: all three hypotheses RULED OUT — NOT APPLICABLE' section + 'Post-execution evidence' section)"

key-decisions:
  - "Path taken: Task 3 RULED OUT — human reviewer verdict confirmed (a) is structurally impossible across all three variants; Task 2 CONFIRMED path correctly NOT executed"
  - "No D-10 marker on this test — phase-wide invariant requires exactly ONE D-10 pin per phase, already landed on Plan 03's test_planner.py:1319 (test_planner_does_not_loop_on_destination_less_baseline_query); this test gets the 'defense-in-depth invariant' marker instead"
  - "Default app_with_mocks planner script (fetch_fuel → fetch_route → calculate_price → respond) is correct test driver — the test validates the SSE plumbing LAYER (_drain_events + _fresh_stream + response_node.final_payload), not the planner routing (Plan 03's D-10 test already pins planner short-circuit at the unit level)"
  - "Escalation section header included in EVIDENCE.md for plan acceptance-criterion compliance, but marked 'NOT APPLICABLE' because Plan 03 CONFIRMED + FIXED (b); the phase root cause is CLOSED at commit e550256, not pending Phase 999.12 escalation"
  - "No backend production code modified — matches must_haves.truths.3 'If RULED OUT: no backend code changes land'; the only files changed in this plan are test_api_chat.py (defense-in-depth test) + 999.11-04-EVIDENCE.md (verdict documentation) + 999.11-04-SUMMARY.md (this file)"

patterns-established:
  - "Three-source structural argument for SSE RULED OUT: (1) every response_node return branch carries final_payload; (2) _fresh_stream's only early-return is the intentional Pitfall-2 approval_required path; (3) graph.py response→END wiring is the only terminal edge. All three must be true for variant 1/2/3 to be impossible — independent invariants reinforce the verdict."
  - "Integration probe via TestClient + app_with_mocks for SSE plumbing layer validation — separate from planner routing concerns. Default planner script is fine when the test target is the SSE machinery itself (NOT the routing logic)."
  - "Defense-in-depth test on RULED OUT branch lands as permanent CI guard — even when the contract is already correct, the test pins the invariant so any future regression breaking answer or done emission on the legit-baseline shape is caught immediately."

requirements-completed: [FIX-02]

# Metrics
duration: ~15min
completed: 2026-05-11
---

# Phase 999.11 Plan 04: Hypothesis (a) SSE Termination — RULED OUT (Defense-in-Depth)

**Hypothesis (a) SSE termination is structurally impossible on the current codebase (3 variants ruled out via static analysis + integration probe); permanent CI guard test_legit_baseline_diesel_price_query_completes lands as defense-in-depth in backend/tests/test_api_chat.py. Phase root cause was already CLOSED by Plan 03's planner_node short-circuit (commit e550256); this plan completes the evidence trail.**

## Performance

- **Duration:** ~15 min total (Task 1 RULED OUT verdict was authored in prior session at commit 268c030; this continuation executed Task 3 RULED OUT path only)
- **Started:** 2026-05-11 (continuation after Task 1 human-verify checkpoint)
- **Task 1 commit (prior session):** `268c030` — `docs(999.11-04): record (a) SSE termination RULED OUT verdict`
- **Task 3 Step A commit:** `91cd4ae` — `test(999.11-04): pin legit-baseline answer+done invariant (defense-in-depth)`
- **Task 3 Step B commit:** `6131fa5` — `docs(999.11-04): record (a) ruled-out + closure note (Plan 03 fix CLOSED phase)`
- **Tasks executed:** 2 of 3 (Task 1 evidence already committed + Task 3 RULED OUT path; Task 2 CONFIRMED path correctly SKIPPED)
- **Files created/modified:** 1 SUMMARY.md (this file) + backend/tests/test_api_chat.py (+89 lines) + 999.11-04-EVIDENCE.md (+58 lines)

## Accomplishments

- **Hypothesis (a) RULED OUT via human-reviewed EVIDENCE.md** — D-02 evidence-first protocol satisfied:
  - **Variant 1** (graph completes without `response.final_payload` event): structurally impossible — `graph.py:273` wires `response → END` so response_node is always invoked before graph completion; combined with the static audit below, every response_node invocation emits the event.
  - **Variant 2** (response_node returns dict missing `final_payload`): structurally impossible — all 5 exit branches (refusal at lines 268-278, deny at 329-339, ok/clarify/partial/search_only at the shared 442-446 return) carry `final_payload`. The status-precedence ladder converges on a single return statement that always assembles `final_payload`.
  - **Variant 3** (`_fresh_stream` early return bypasses finally): structurally impossible on the legit-baseline shape — the only early-return is the intentional Pitfall-2 `approval_required` path (line 288), which sets `pending_approval=True` BEFORE the yield so the finally block correctly suppresses `done`. For destination-less queries, surcharge_result is None, hitl_gate_node is never invoked, snapshot.next is empty, pending_approval stays False, and `done` is emitted from the finally block.

- **Integration probe via app_with_mocks confirmed SSE plumbing health** — TestClient streaming the legit-baseline POST produced `['meta', 'trace', 'trace', 'trace', 'trace', 'trace', 'trace', 'answer', 'done']` with `answer.status='ok'`. The default app_with_mocks planner script drives the multi-step path because the probe validates the SSE plumbing LAYER (`_drain_events` + `_fresh_stream` + `response_node.final_payload`), not the planner routing — Plan 03's D-10 test already pins the planner short-circuit at the unit level.

- **Defense-in-depth integration guard test landed and PASSING on first run** — `test_legit_baseline_diesel_price_query_completes` in `backend/tests/test_api_chat.py:197` asserts the SSE plumbing contract on the legit-baseline shape: `meta` first, `answer` event present, `done` last, message_id format `{thread_id}-{turn_idx}`, non-empty markdown. Test runs deterministically against the mocked LLM + tools — zero live network calls. Passes GREEN on first run as expected (RULED OUT path).

- **Phase-wide D-10 pinning test invariant preserved** — `grep -c "D-10 pinning test" backend/tests/test_api_chat.py` returns `0` (no marker in this file); the phase-wide single D-10 pin lives on `backend/tests/test_planner.py:1319` from Plan 03's CONFIRMED path. `grep -c "defense-in-depth invariant: Phase 11 / FIX-02" backend/tests/test_api_chat.py` returns `1` (this test). The grep-by-marker invariants documented in Plan 05 hold.

- **No backend production code modified** — `git diff --stat HEAD~3..HEAD` over the 3 commits in this plan touches only `backend/tests/test_api_chat.py` (test addition) and `.planning/.../999.11-04-EVIDENCE.md` + `.planning/.../999.11-04-SUMMARY.md` (docs). Files explicitly listed in the plan's `files_modified` as conditional-only (`backend/api/routes/chat.py`, `backend/agent/nodes/response_node.py`) remained untouched, matching `must_haves.truths.3`: "If RULED OUT: no backend code changes land".

- **Full backend pytest GREEN** — 358 passed in 9.82s (was 357 post-Plan-03 baseline; +1 from new test, zero regressions, zero new flakes). Existing Pitfall-2 / approval-required tests still pass (`pytest -k approval` 2/2 GREEN). The 16-test test_api_chat.py file is now 17 tests, all GREEN.

## Task Commits

Each commit is task-atomic, conventional-commit-formatted, and traceable by `git log --grep="999.11-04"`:

1. **Task 1 (prior session): Static analysis + integration probe + verdict capture** — `268c030` (docs)
2. **Task 3 Step A: Defense-in-depth integration test** — `91cd4ae` (test)
3. **Task 3 Step B: EVIDENCE.md augmentation** — `6131fa5` (docs)

Final SUMMARY + STATE + ROADMAP metadata commit follows this file.

## Files Created/Modified

- **Created**: `.planning/phases/999.11-investigate-live-sse-hang-on-legit-baseline-diesel-price-query/999.11-04-SUMMARY.md` (this file)
- **Modified**: `backend/tests/test_api_chat.py` — appended `test_legit_baseline_diesel_price_query_completes` (89 lines, lines 197-285) with `defense-in-depth invariant: Phase 11 / FIX-02 — additive coverage, not the D-10 pin` marker in docstring
- **Modified**: `.planning/phases/999.11-investigate-live-sse-hang-on-legit-baseline-diesel-price-query/999.11-04-EVIDENCE.md` — appended "Escalation: all three hypotheses RULED OUT — NOT APPLICABLE" section + "Post-execution evidence (Task 3 RULED OUT path)" section (58 lines)
- **NOT modified** (RULED OUT contract): `backend/api/routes/chat.py`, `backend/agent/nodes/response_node.py`, `backend/agent/graph.py`, `backend/agent/nodes/planner.py`

## Decisions Made

See `key-decisions` in the frontmatter for the structured list. Highlights:

1. **Task 3 RULED OUT path executed; Task 2 CONFIRMED path correctly SKIPPED** — human reviewer verdict on the static + integration evidence trail confirmed (a) is structurally impossible across all three variants.

2. **No D-10 marker on `test_legit_baseline_diesel_price_query_completes`** — the phase-wide invariant requires exactly ONE D-10 pinning test per phase; that pin already landed on Plan 03's CONFIRMED path (`test_planner_does_not_loop_on_destination_less_baseline_query` at `test_planner.py:1319`). This test carries only the `defense-in-depth invariant: Phase 11 / FIX-02 — additive coverage, not the D-10 pin` marker, mirroring Plan 03's defense-in-depth reducer-pin pattern.

3. **Default app_with_mocks planner script is the right test driver** — the test validates the SSE plumbing LAYER (the machinery that emits `answer` + `done`), not the planner routing. Using the default 4-step script keeps the test simple and matches the existing happy-path test patterns; the planner short-circuit itself is already pinned by Plan 03's D-10 test at the planner unit level.

4. **EVIDENCE.md "Escalation" section marked NOT APPLICABLE** — the plan's acceptance criterion grep on the literal string `Escalation: all three hypotheses RULED OUT` is satisfied, but the escalation path is structurally inactive because Plan 03 CONFIRMED + FIXED (b) before Plan 04 ran. No Phase 999.12 follow-up needed. D-04 escalation clause (pull Langfuse traces + AgentState snapshots) NOT invoked.

## Deviations from Plan

None — plan executed exactly as written on the RULED OUT path.

The plan's `files_modified` frontmatter lists `backend/api/routes/chat.py` and `backend/agent/nodes/response_node.py` as conditional-only (Modified only if (a) is CONFIRMED). The RULED OUT path correctly leaves these untouched per `must_haves.truths.3`, so the actual files-modified delta is a strict subset of the plan's possible-file list.

## Issues Encountered

None — execution was mechanical because the verdict was already authored in Task 1 (prior session) and human-approved before this continuation. The test passes GREEN on first run because the SSE plumbing contract was already correct on current main (the live "hang" was hypothesis (b)'s ValueError-loop in `route_agent_node`, not an SSE defect — the SSE stream emits `error` + `done` correctly even pre-fix; the client's "no answer" experience was the legit-baseline producing no `answer` event because of the planner re-loop, not the stream failing to close).

## Hand-off Context for Plan 05

Plan 05 (summary + live-bar gate) is ready to close the phase:

- **All three hypotheses investigated and disposed:**
  - Plan 02 (c) cold-start latency: RULED OUT — 20 fresh-uvicorn runs across 4 mitigation steps all fail with same ValueError.
  - Plan 03 (b) planner re-loop: **CONFIRMED + FIXED** at commit `e550256` — destination-less short-circuit in `planner_node`. 5/5 fresh-uvicorn runs PASS_UNDER_30S at ~7.6-7.9s each.
  - Plan 04 (a) SSE termination: RULED OUT — static analysis trio + integration probe via app_with_mocks; defense-in-depth integration guard landed in `test_api_chat.py`.

- **Demo gate (D-09 5-run live verification bar): ALREADY CLEARED** by Plan 03's post-fix run archive at `repro/logs/post-fix-baseline/summary.jsonl`. Plan 05 may re-run for fresh confirmation or accept the Plan 03 archive as canonical evidence.

- **Test count delta:** Plan 03 contributed +2 tests (357 baseline). Plan 04 contributes +1 test (358 total). Phase 11 net delta: +3 backend tests, all GREEN.

- **Markers across the codebase:**
  - D-10 pin (exactly ONE for Phase 11 / FIX-02): `backend/tests/test_planner.py:1319`
  - Defense-in-depth tests (additive coverage): 2 total
    - `test_tool_call_count_reducer_aggregates_parallel_writes` in `backend/tests/test_tool_call_counter.py` (Plan 03 reducer pin)
    - `test_legit_baseline_diesel_price_query_completes` in `backend/tests/test_api_chat.py:197` (Plan 04 SSE plumbing pin)

- **REQUIREMENTS update:** FIX-02 is satisfied across Plans 03 + 04. Plan 05 will stamp it complete in `.planning/REQUIREMENTS.md`.

- **No backend production code remains to ship** for this phase — Plan 03's `planner_node` short-circuit is the only production fix; Plan 04 added only test coverage.

## Self-Check: PASSED

- **Files created:**
  - `/Users/pollot/Desktop/express-dynamic-workflow/.planning/phases/999.11-investigate-live-sse-hang-on-legit-baseline-diesel-price-query/999.11-04-SUMMARY.md` — verified exists (this file).
- **Files modified:**
  - `/Users/pollot/Desktop/express-dynamic-workflow/backend/tests/test_api_chat.py` — `grep -c "def test_legit_baseline_diesel_price_query_completes"` returns 1.
  - `/Users/pollot/Desktop/express-dynamic-workflow/.planning/phases/999.11-investigate-live-sse-hang-on-legit-baseline-diesel-price-query/999.11-04-EVIDENCE.md` — `grep -nE "Hypothesis \(a\) SSE termination: RULED OUT"` returns 1 line; `grep -n "Escalation: all three hypotheses RULED OUT"` returns 1 line.
- **Commits exist:**
  - `268c030` (Task 1, prior session): verified via `git show --stat 268c030`.
  - `91cd4ae` (Task 3 Step A): verified via `git log --oneline 268c030^..HEAD`.
  - `6131fa5` (Task 3 Step B): verified via `git log --oneline 268c030^..HEAD`.
- **Acceptance criteria (Task 3):** all 7 criteria verified (function exists, pytest passes, no D-10 marker, defense-in-depth marker present, no runbook-mitigation language, RULED OUT verdict line present, escalation section header present).
- **Full backend pytest:** 358 passed in 9.73s (was 357 baseline; +1 new test; zero regressions).

---
*Phase: 999.11-investigate-live-sse-hang-on-legit-baseline-diesel-price-query*
*Plan: 04*
*Completed: 2026-05-11*
