# Phase 999.11: Investigate live SSE hang on legit baseline diesel-price query - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-10
**Phase:** 999.11-investigate-live-sse-hang-on-legit-baseline-diesel-price-query
**Areas discussed:** Phase scope, Repro protocol, Investigation order, Verification standard

---

## Phase scope

| Option | Description | Selected |
|--------|-------------|----------|
| Investigate + fix | Reproduce, identify root cause, ship fix, regression test. Matches prior 999.x debug pattern (999.5/999.6/999.7). | ✓ |
| Investigate-only | Phase produces a findings doc with confirmed root cause + recommended fix path. Fix lands in a follow-up phase. | |
| Time-boxed: investigate first, decide | Time-box ~1–2 hours of investigation. If root cause is local + fix is contained, ship in this phase; else split. | |

**User's choice:** Investigate + fix (recommended)
**Notes:** Demo is W6; pure-investigation phase risks needing a follow-up under time pressure.

| Option | Description | Selected |
|--------|-------------|----------|
| Fix root cause | Address the underlying issue. Stronger demo story; works in any environment. | ✓ |
| Demo-runbook mitigation only | Document a warmup procedure and accept the cold-start hang as known behavior. | |
| Both — fix what's cheap, document the rest | Pragmatic given W6 demo. | |

**User's choice:** Fix root cause (recommended)
**Notes:** Locks D-02 in CONTEXT.md.

---

## Repro protocol

| Option | Description | Selected |
|--------|-------------|----------|
| Fresh-uvicorn + httpx script | Kill+restart uvicorn, run a Python httpx script with 180s timeout that drains the full SSE stream and prints every event with timestamps. Capture uvicorn stderr. Mirrors the original failing repro. | ✓ |
| Pytest integration test | Spin up app via TestClient, fire the legit baseline, assert the answer event arrives. Deterministic but does NOT reproduce real cold-start network latency. | |
| Both — script for diagnosis, pytest for regression | Higher effort but stronger evidence. | |

**User's choice:** Fresh-uvicorn + httpx script (recommended)
**Notes:** Deterministic regression test will still be added separately (D-10) once root cause is known.

| Option | Description | Selected |
|--------|-------------|----------|
| Full SSE event stream with wall-clock timestamps | Dump every meta/trace/answer/error/done event with elapsed-ms since request start. | ✓ |
| uvicorn stderr | Capture full uvicorn log including any exceptions raised in `_drain_events` / `response_node` / lifespan tasks. | ✓ |
| Langfuse trace inspection | Pull the trace for the failing run from Langfuse Cloud. | |
| AgentState snapshot via /api/conversations after the hang | Reveals whether the graph completed or paused mid-flight. | |

**User's choice:** SSE stream + uvicorn stderr (multi-select)
**Notes:** Langfuse and AgentState pulled on-demand only if the stderr+stream pair is insufficient.

---

## Investigation order

| Option | Description | Selected |
|--------|-------------|----------|
| Cold-start latency (c) | Cheapest to rule out: warm uvicorn first, then re-run. Fix path becomes lifespan warmup. | ✓ |
| Planner re-loop / tool_call_count reducer (b) | Trace shows planner→fuel_agent→planner — step 3 is the smoking gun. Riskier first step. | |
| SSE termination bug (a) | Static analysis of `_drain_events` + `_fresh_stream`. Hardest to falsify empirically. | |

**User's choice:** Cold-start latency (c) (recommended)
**Notes:** Cheapest to rule out empirically; gives clean evidence trail before tackling reasoning-heavy hypotheses.

| Option | Description | Selected |
|--------|-------------|----------|
| Then (b) re-loop, then (a) SSE | (b) has stronger evidence (planner-as-step-3 + new reducer). (a) last because it requires inspecting an event-stream tail. | ✓ |
| Then (a) SSE, then (b) re-loop | Static analysis fastest if you spot the bug. (b) reserved for last. | |
| Investigate (a) and (b) in parallel | Faster if both wrong but risks mixing evidence. | |

**User's choice:** (b) then (a) (recommended)
**Notes:** Keeps evidence trail clean per hypothesis.

---

## Verification standard

| Option | Description | Selected |
|--------|-------------|----------|
| 5 fresh-uvicorn runs of legit baseline, all answer<30s | Strong: ~95% demo confidence under cold-start variability. | ✓ |
| 3 fresh-uvicorn runs, all answer<60s | Looser: faster to verify but doesn't push the cold-start envelope. | |
| 1 fresh-uvicorn run + 5 warm follow-ups, all answer<10s | Demo-realistic but doesn't stress cold start. | |

**User's choice:** 5 fresh-uvicorn runs @ <30s (recommended)
**Notes:** Locks D-09 in CONTEXT.md.

| Option | Description | Selected |
|--------|-------------|----------|
| Pytest covering the root-cause fix | Deterministic, CI-friendly. Test shape depends on which root cause is confirmed. | ✓ |
| Demo runbook entry | Defense-in-depth even if fix is solid. | |
| Langfuse smoke screenshot | Lightweight evidence in docs/screenshots/. | |

**User's choice:** Pytest only (multi-select; demo runbook + Langfuse screenshot deselected)
**Notes:** D-02 already made root-cause fix the standard, so a runbook mitigation is redundant. Langfuse screenshot deferred.

---

## Claude's Discretion

- Exact Python httpx script structure, logging format, and 5-run orchestration.
- Granular structure of the regression pytest (file location, fixtures, async vs sync) once root cause is known.
- How to surface evidence in the eventual SUMMARY.md.
- Whether to add a defensive lifespan warmup query even if (c) is ruled out.

## Deferred Ideas

- `/api/health` endpoint with a real warmup probe.
- Switching SSE framing to `sse-starlette` once FastAPI is upgraded past 0.128.8.
- Per-request soft timeout that emits `error` + `done` if any node exceeds N seconds.
- Surfacing `tool_call_count` in the SSE trace `tool_output` for dashboard plotting.
- Generalizing the repro script into a `scripts/probe_live.py` live-stack smoke.
