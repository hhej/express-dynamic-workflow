---
phase: 999.10-guard-input-bypass-paths-return-inconsistent-refusal-copy
plan: 01
subsystem: guardrails

tags: [langgraph, guard-input, typing, literal, refusal-contract, planner, type-system]

# Dependency graph
requires:
  - phase: quick-260509-utd
    provides: "Two-layer guardrails: guard_input + guard_output nodes; GuardCategory Literal (5 members); locked REFUSAL_COPY; guard_decision shape on AgentState"
provides:
  - "GuardCategory Literal extended additively from 5 to 7 members"
  - "Type-system gate for two new planner-side refusal categories: planner_off_topic, planner_parse_failed"
  - "Doc comment on the type alias documenting which node emits which subset (guard_input_node vs planner_node)"
  - "Zero logic change in guard_input.py — _classify, _DOMAIN_ALLOW_PATTERNS, _llm_fallback, guard_input_node byte-identical except for the Literal expansion + comment"
affects: [999.10-02-planner-refusal-paths, 999.10-03-adversarial-pack-regression, planner_node, response_node]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additive Literal extension: existing members first, new members appended at end (stable order for readers)"
    - "Doc-comment on type alias documents which subsystem emits which subset of Literal values (the type itself stays a single source of truth)"
    - "D-09 contract: guard_decision.category is the differentiator across refusal sources; guard_decision.layer stays 'input' (D-10) so response_node's existing 'status = refused if layer == input' predicate flows through"

key-files:
  created: []
  modified:
    - "backend/agent/nodes/guard_input.py — line 47 single-line Literal replaced with 13-line annotated multi-line Literal (7 members, doc comment)"

key-decisions:
  - "Append new members at the end (not alphabetical) so readers see the original five classifications first — preserves quick-260509-utd's vocabulary as the conceptual anchor for the guard_input node, with the planner-side additions as a clearly-marked extension."
  - "Doc comment on the type alias names which node emits which subset (guard_input_node sets the first 5; planner_node will set the last 2 in Plan 02). Documentation lives next to the contract so future readers cannot miss the split."
  - "No changes to _classify(), _llm_fallback(), or guard_input_node() — the planner-side categories are NOT emitted by this module. D-09 explicitly locates the new emission inside planner_node (Plan 02)."
  - "No edits to _DOMAIN_ALLOW_PATTERNS (D-03 explicit ban) — kept the 'weather in Bangkok' admit-by-allow-list behavior; the planner becomes the second-line defense in Plan 02."
  - "No edits to backend/agent/state.py — guard_decision is Optional[dict] (D-09/D-10), so adding new category strings requires no schema migration."

patterns-established:
  - "Wave 1 type-system gate before Wave 2 emission edits: extending the Literal first means Plan 02's planner edits land against an already-widened type alias, eliminating a class of type-check errors at the wave boundary."
  - "Comment-above-Literal documentation pattern for split-ownership type aliases — when one Literal is shared across multiple emitters, list each emitter's subset in a comment immediately above the alias."

requirements-completed: [GUARD-07]

# Metrics
duration: ~2min
completed: 2026-05-11
---

# Phase 999.10 Plan 01: Extend GuardCategory Literal Summary

**Additive Literal extension in `backend/agent/nodes/guard_input.py` — `GuardCategory` widened from 5 members to 7 (adding `planner_off_topic` and `planner_parse_failed`) with a documentation comment naming which node emits which subset. Zero logic changes; opens the type-system gate for Wave 2 (Plan 02) to set these new categories from `planner_node` without tripping any static-type check.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-11T08:50:37Z
- **Completed:** 2026-05-11T08:52:06Z
- **Tasks:** 1 / 1
- **Files modified:** 1

## Accomplishments

- `GuardCategory` Literal now has exactly 7 members in the documented order: `allow`, `injection`, `off_topic`, `cost_bombing`, `unclear`, `planner_off_topic`, `planner_parse_failed`.
- Doc comment immediately above the alias makes the split-ownership contract explicit: `guard_input_node` emits the first 5, `planner_node` (Plan 02) will emit the last 2; in all cases `guard_decision.layer` stays `'input'` (D-10) and `category` is the differentiator (D-09).
- `_classify`, `_llm_fallback`, `_DOMAIN_ALLOW_PATTERNS`, `guard_input_node`, and `_route_from_guard_input` byte-identical otherwise — pure type-system + comment change.
- Full backend pytest suite green: 345/345 passed in 9.81s (no regression; no test asserted the closed set so the Literal extension is invisible to runtime behavior).

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend GuardCategory Literal additively** — `a58105b` (feat)

**Plan metadata:** _to be appended after SUMMARY.md + STATE.md + ROADMAP.md edits are committed._

## Files Created/Modified

- `backend/agent/nodes/guard_input.py` — Replaced the single-line `GuardCategory` Literal at line 47 with a 13-line annotated multi-line Literal (7 members, 6-line doc comment immediately above). Net diff: +15 / -1.

## Decisions Made

- **Append new members at the end (not alphabetical).** Stable order; readers see the original quick-260509-utd vocabulary first, then the Phase 999.10 additions as a clearly-marked extension. Mirrors how the planner SYSTEM_PROMPT (Phase 5 Plan 03-02) lists user_intent values in stable order rather than alphabetical.
- **Doc comment on the type alias (not a docstring at module top).** Decision lives next to the contract; future code search by name (`GuardCategory`) lands on the documentation immediately. Module docstring already describes the guard_input node's behaviour and does not need expansion for a type-only widening.
- **Touch only the type alias + comment.** No changes to `_classify`, `_llm_fallback`, `_DOMAIN_ALLOW_PATTERNS`, or `guard_input_node` — the planner-side categories are NOT emitted by this module. Smallest possible Wave 1 change.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plan 02 (Wave 2) is now type-safe to set `state.guard_decision.category = 'planner_off_topic'` or `'planner_parse_failed'` from `planner_node` without tripping mypy / Pylance.
- Plan 03 (Wave 3) adversarial-pack regression can assert on these strings without redefining the Literal.
- `_DOMAIN_ALLOW_PATTERNS` deliberately unchanged (D-03 ban honoured) — the "weather in Bangkok" admit-by-allow-list behavior remains; Plan 02's planner edits become the second-line defense.
- `backend/agent/state.py` unchanged — `guard_decision` is `Optional[dict]`, so the new category strings flow through without schema migration.

## Self-Check

### Files claimed in this SUMMARY

- `backend/agent/nodes/guard_input.py` — modified ✓ (FOUND)
- `.planning/phases/999.10-guard-input-bypass-paths-return-inconsistent-refusal-copy/999.10-01-SUMMARY.md` — created by this write ✓

### Commit claimed in this SUMMARY

- `a58105b` (Task 1: extend GuardCategory Literal) — FOUND via `git log --oneline | grep a58105b`

### Verification commands run

- `typing.get_args(GuardCategory)` returned exactly the 7-tuple `('allow', 'injection', 'off_topic', 'cost_bombing', 'unclear', 'planner_off_topic', 'planner_parse_failed')` — PASS
- `grep -n "_DOMAIN_ALLOW_PATTERNS" backend/agent/nodes/guard_input.py` returned 2 hits (definition + use in `_classify`) — unchanged from pre-plan baseline — PASS
- `.venv/bin/python -m pytest backend/tests/` reported `345 passed in 9.81s` — no regression — PASS

## Self-Check: PASSED

---
*Phase: 999.10-guard-input-bypass-paths-return-inconsistent-refusal-copy*
*Completed: 2026-05-11*
