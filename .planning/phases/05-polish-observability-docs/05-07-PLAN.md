---
phase: 05-polish-observability-docs
plan: 07
type: execute
wave: 6
depends_on:
  - 05-02
  - 05-03
  - 05-04
  - 05-05
  - 05-06
files_modified:
  - README.md
  - docs/architecture.md
  - docs/data-sources.md
  - docs/screenshots/.gitkeep
autonomous: false
requirements:
  - DOC-01
  - DOC-02
  - DOC-04
must_haves:
  truths:
    - "README.md exists at repo root and contains every section DOC-01 enumerates: project overview, team, problem statement, agent design (with Mermaid diagram), data sources, setup instructions, AI tools used, limitations, license"
    - "docs/architecture.md exists with hybrid Mermaid + ASCII layout reflecting Phase 5 topology — parallel branches, HITL gate, search agent, Langfuse callback boundary"
    - "docs/data-sources.md exists with EPPO + simulated rate-table assumptions + Google Maps + Tavily sections"
    - "All user-facing copy uses 'Bangkok Metro' phrasing — never 'Central Region'"
    - "5 screenshots embedded in README cover: chat with breakdown, trace mid-stream (parallel), dashboard, ApprovalCard, Langfuse trace view"
    - "demo.mp4 (or .gif) lands at docs/demo.mp4 showing fresh-thread surcharge query end-to-end including parallel timestamps + HITL approval"
    - "v1.0 annotated tag is created on the develop→main merge commit with submission deliverables checklist in the message"
  artifacts:
    - path: "README.md"
      provides: "DOC-01 — submission-ready repo readme"
      contains: "## Setup Instructions"
    - path: "docs/architecture.md"
      provides: "DOC-02 — Mermaid + ASCII architecture reference"
      contains: "mermaid"
    - path: "docs/data-sources.md"
      provides: "DOC-04 — data provenance"
      contains: "EPPO"
    - path: "docs/demo.mp4"
      provides: "D-20 demo recording artifact"
  key_links:
    - from: "README.md"
      to: "docs/architecture.md"
      via: "agent design section link"
      pattern: "docs/architecture.md"
    - from: "README.md"
      to: "docs/data-sources.md"
      via: "data sources section link"
      pattern: "docs/data-sources.md"
    - from: "README.md"
      to: "docs/demo.mp4"
      via: "demo section embed"
      pattern: "docs/demo.mp4"
---

<objective>
Land the submission-ready documentation deliverables for the v1.0 tag — DOC-01 README, DOC-02 architecture.md update with Mermaid + Phase 5 topology, DOC-04 data-sources.md, screenshots, and the demo.mp4 recording. Cap the phase with a developer→main merge and the annotated v1.0 git tag per D-21.

DOC-01 is the highest-stakes deliverable: it's what graders read first, sets the AI/Vibe-Coding 15% rubric narrative, and links every other artifact (architecture, data sources, demo). DOC-02 needs Mermaid blocks (GitHub renders natively) for the new parallel + HITL + search topology and for the Langfuse callback boundary. DOC-04 makes the simulated rate-table assumptions transparent — required for grading credibility on the Data Integration 20% rubric.

The demo recording (D-20) and screenshots are HUMAN-required tasks — Claude can outline the script but cannot run a screen recorder. This plan therefore has TWO checkpoints: one for the demo recording, one for the v1.0 tag push (only the IT Lead pushes per D-21).

Purpose: complete every Phase 5 deliverable and ship v1.0 — the MADT7204 final submission artifact.

Output:
- New `README.md` at repo root, rewritten per DOC-01 verbatim outline.
- Updated `docs/architecture.md` with Mermaid blocks + Phase 5 sections (parallel, HITL, search, Langfuse).
- New `docs/data-sources.md`.
- Screenshots placed in `docs/screenshots/` directory (5 files referenced by README).
- New `docs/demo.mp4` (or .gif if mp4 generation fails — UI-SPEC accepts either).
- `v1.0` annotated git tag on the `main` merge commit with deliverables checklist annotation.
</objective>

<execution_context>
@/Users/pollot/Desktop/express-dynamic-workflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pollot/Desktop/express-dynamic-workflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-polish-observability-docs/05-CONTEXT.md
@.planning/phases/05-polish-observability-docs/05-RESEARCH.md
@.planning/phases/05-polish-observability-docs/05-UI-SPEC.md
@.planning/phases/05-polish-observability-docs/05-01-PLAN.md
@.planning/phases/05-polish-observability-docs/05-02-PLAN.md
@.planning/phases/05-polish-observability-docs/05-03-PLAN.md
@.planning/phases/05-polish-observability-docs/05-04-PLAN.md
@.planning/phases/05-polish-observability-docs/05-05-PLAN.md
@.planning/phases/05-polish-observability-docs/05-06-PLAN.md
docs/architecture.md
README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md per DOC-01 outline + place screenshot stubs</name>
  <files>README.md, docs/screenshots/.gitkeep</files>
  <read_first>
    - README.md (current state — Phase 5 may FULLY REWRITE; if existing content has team or rubric notes, preserve them)
    - .planning/phases/05-polish-observability-docs/05-RESEARCH.md §Documentation Deliverables (full README outline)
    - .planning/phases/05-polish-observability-docs/05-CONTEXT.md (D-17 enumerated sections; Bangkok Metro phrasing)
    - .planning/PROJECT.md (team, rubric weights, MADT7204 framing)
    - CLAUDE.md (Project section — tech stack + constraints + course context)
  </read_first>
  <action>
    Step 1 — Create `docs/screenshots/.gitkeep` (empty file) so git tracks the screenshots directory:
    ```bash
    mkdir -p docs/screenshots
    touch docs/screenshots/.gitkeep
    ```

    Step 2 — Replace `README.md` with the following content. Bangkok Metro phrasing throughout. All sections from D-17 verbatim.

    ```markdown
    # Express Dynamic Surcharge Orchestrator

    > Agentic AI that calculates fuel surcharges for Express logistics in
    > Bangkok Metro by reasoning over live diesel prices, route data, and
    > internal rate tables — visibly and explainably.

    **Course:** MADT7204 — Generative AI for Business
    **Submission:** v1.0 (final)

    ![Chat with surcharge breakdown](docs/screenshots/chat-breakdown.png)

    ## Project Overview

    The Express Dynamic Surcharge Orchestrator is a multi-agent AI product
    that helps Express logistics operators in Thailand's Bangkok Metro
    derive accurate, explainable fuel-driven surcharge recommendations.
    The agent reasons over EPPO diesel B7 prices, Google Maps route data,
    and a simulated Express rate table to compute surcharge percentage and
    final amount per shipment, with a fully visible reasoning trace —
    every LLM call, tool invocation, and routing decision is auditable.

    Three shipping types (Bounce, Retail Standard, Retail Fast), three
    Bangkok Metro zones (`central-1`, `central-2`, `central-3` — internal
    IDs), and a configurable diesel baseline drive a deterministic
    surcharge formula (`calculate_surcharge`). The LLM's role is intent
    classification, narration, and conditional routing — never numerical
    derivation. This split is what makes the system *agentic* (visible
    reasoning) without sacrificing accuracy.

    ## Team

    | Member | Role |
    |--------|------|
    | Panjapol Pollot (IT Lead) | Agent architecture, tool design, integration, technical grading |
    | (other members per project register) | (per project register) |

    ## Problem Statement

    Diesel prices in Thailand fluctuate weekly. Express's surcharge tariffs
    must adjust to keep pace with fuel volatility without overcharging
    customers or eroding margin. Manual recompute by ops teams is slow,
    error-prone, and opaque to customers asking "why this number?".

    The orchestrator solves this by deriving surcharge programmatically
    from live data and presenting the *reasoning* alongside the answer:
    base rate, fuel delta, traffic adjustment, cap/floor, and any
    high-value approval gating — every step explainable.

    ## Agent Design

    The system is a five-node LangGraph multi-agent graph plus optional
    parallel fan-out, an HITL approval gate, and a Tavily-backed Search
    Agent. The Planner classifies intent and routes; specialists fetch
    data and compute; the Response Node renders prose + breakdown.

    ```mermaid
    flowchart TD
        START([START]) --> P[Planner]
        P -->|next_step=fanout_fuel_route<br/>both stale| FAN{Parallel<br/>superstep}
        FAN --> F[Fuel Agent]
        FAN --> R[Route Agent]
        P -->|fetch_fuel<br/>route cached| F
        P -->|fetch_route<br/>fuel cached| R
        P -->|search_context<br/>news intent| S[Search Agent]
        P -->|calculate_price| PR[Pricing Agent]
        P -->|clarify/respond| RESP[Response Node]
        F --> P
        R --> P
        S --> P
        PR --> H{HITL Gate<br/>total > threshold?}
        H -->|low value| RESP
        H -->|high value| INT([interrupt — await user])
        INT -->|Command resume=approve/deny| RESP
        RESP --> END([END])
    ```

    Key design decisions:

    - **Cache-aware planner**: follow-ups reuse fuel/route data without
      re-calling tools (LangGraph SQLite checkpointer). On follow-ups the
      planner short-circuits to `calculate_price` directly.
    - **Parallel fan-out (ORCH-07)**: when both fuel and route are stale
      on a fresh thread, the planner emits a sentinel that the
      conditional edge translates to a list of two destination nodes —
      same-superstep parallel execution. Trace timestamps overlap by
      &lt; 1s, visible evidence of concurrent execution.
    - **HITL approval gate (ORCH-09)**: between Pricing and Response,
      a tiny gate node calls `langgraph.types.interrupt()` when total
      &gt; `HITL_TOTAL_THB_THRESHOLD` (default 500 THB; override via env).
      The chat handler emits a sixth SSE event `approval_required`,
      and the frontend renders Approve/Deny buttons. Resume happens via
      a follow-up `POST /api/chat` with `{thread_id, approve}`.
    - **Search Agent (TOOL-05)**: news/market/trend questions route
      through a Tavily-backed node that populates `state.search_context`
      with a 1–2 sentence summary + ranked sources. The Response Node
      prepends a "Market context: …" line above the prose answer.
      Search NEVER feeds the deterministic surcharge formula.

    Full topology, conditional routing table, and SSE event vocabulary
    are documented in [docs/architecture.md](docs/architecture.md).

    ## Data Sources

    See [docs/data-sources.md](docs/data-sources.md) for full provenance,
    refresh cadence, and assumptions.

    - **EPPO diesel B7 historical prices** (real, public): fetched daily
      via `data/scripts/fetch_fuel_prices.py`; multi-level fallback chain
      (live API → cached CSV → hardcoded baseline).
    - **Simulated Express rate table** (transparent assumptions): zone
      multipliers 1.0/1.25/1.55, 3 ship types × 3 zones × multiple weight
      tiers, base-rate range 50–698 THB. Generated by
      `data/scripts/generate_rate_table.py`.
    - **Google Maps Directions API**: distance, duration, traffic for
      Bangkok Metro provinces; 15-minute TTL cache.
    - **Tavily Search API** (news topic): 30-minute TTL cache; gated by
      news/trend intent — standard surcharge queries do NOT trigger search.

    ## Setup Instructions

    Requirements:
    - Python 3.11+
    - Node.js 18+
    - API keys: Google AI Studio (Gemini), Google Maps, Tavily, Langfuse
      Cloud (free tier)

    Install + run:

    ```bash
    # 1. Python venv + backend deps
    python3.11 -m venv .venv
    source .venv/bin/activate
    pip install -r requirements.txt

    # 2. Environment variables
    cp .env.example .env
    # Edit .env, fill in API keys (placeholders documented per key)

    # 3. Seed the rate-table SQLite DB
    python data/scripts/seed_database.py

    # 4. (Optional) Refresh fuel price history
    python data/scripts/fetch_fuel_prices.py

    # 5. Run backend (uvicorn @ port 8000)
    uvicorn backend.api.main:app --port 8000 --reload

    # 6. In another shell — frontend
    cd frontend
    npm install
    npm run dev   # http://localhost:3000

    # 7. Run tests
    pytest backend/tests/                # backend
    cd frontend && npm test              # frontend unit
    cd frontend && npm run test:e2e      # frontend e2e (Playwright)
    ```

    ### Demo prompts

    Try these in the chat to exercise the full agent topology:

    1. `Calculate surcharge for 15kg bounce shipment from Bangkok to Nonthaburi`
       — fresh-thread query, exercises **parallel fan-out** (Fuel + Route in
       same superstep).
    2. `What about Retail Fast?` (after #1) — exercises **cache-aware
       skip** (no Fuel/Route re-fetch).
    3. `What's driving diesel prices this week?` — exercises **Search
       Agent** + Tavily (Market context: line above the prose answer).
    4. `Calculate surcharge for 200kg retail_fast from Bangkok to a
       central-3 destination` — exercises **HITL approval gate** (total
       &gt; 500 THB threshold; ApprovalCard renders Approve/Deny).

    ## AI Tools Used

    Per the AI/Vibe-Coding 15% rubric:

    - **Claude Code (Anthropic)** — primary development environment.
      Used for architecture design, multi-file refactors, test scaffolding,
      and documentation generation. The `/gsd:` workflow commands
      (`research-phase`, `plan-phase`, `execute-phase`) drove the entire
      Phase 1–5 cycle and are versioned alongside this repo at
      `.claude/get-shit-done/`.
    - **Claude Agent SDK** — informs the in-product agent architecture
      (multi-agent + tool routing patterns).
    - **Cursor** — light inline edits during code review.
    - **GitHub Copilot** — typing acceleration for repetitive Pydantic
      models and test fixtures.

    Each tool's contribution is auditable via the commit log
    (descriptive messages per the Git Practice 20% rubric).

    ## Observability

    Every LLM call, tool invocation, and routing decision is captured by
    Langfuse via a single CallbackHandler attached at the chat handler
    boundary. Each chat turn maps to one Langfuse trace named
    `chat_turn_{thread_id}_{turn_idx}` (deterministic seed) so user
    feedback (thumbs up/down) attaches to the same trace.

    A formula-accuracy auto-eval runs after every Pricing Agent invocation:
    re-runs the deterministic Phase 1 pure function with the same inputs
    and posts a `formula_accuracy` Score (1.0 match / 0.0 divergence).
    Visible in the Langfuse dashboard, fire-and-forget so eval failure
    never affects the user response.

    ![Langfuse trace view](docs/screenshots/langfuse-trace.png)

    ## Limitations

    - **Bangkok Metro only** — central-1/2/3 zones cover Bangkok plus
      Nonthaburi, Pathum Thani, Samut Prakan, Nakhon Pathom, Samut Sakhon,
      and Ayutthaya. Multi-region expansion is V2-02 (deferred).
    - **Gemini Flash 15 RPM** — sufficient for demo; not production
      throughput. Free tier limit.
    - **Tavily 1000 searches/month** — free tier; demo footprint ~10/run.
    - **Simulated rate table** — real Express tariffs are confidential.
      Assumptions are transparent in
      [docs/data-sources.md](docs/data-sources.md).
    - **Single demo user** — no auth/OAuth (out of scope; not
      relevant to agent architecture grading).
    - **Local reproducibility only** — no production deployment infra
      (Docker/K8s out of scope per CLAUDE.md constraints).

    ## License

    MADT7204 course project. See [LICENSE](LICENSE) if present.

    ## Demo

    A 1–2 minute end-to-end recording is available at
    [docs/demo.mp4](docs/demo.mp4). It shows a fresh-thread surcharge
    query end-to-end including the parallel trace timestamps and a HITL
    approval flow.

    Static screenshots:

    | View | File |
    |------|------|
    | Chat with surcharge breakdown for Bangkok Metro shipment | [docs/screenshots/chat-breakdown.png](docs/screenshots/chat-breakdown.png) |
    | Reasoning trace mid-stream — fuel and route agents in parallel | [docs/screenshots/trace-parallel.png](docs/screenshots/trace-parallel.png) |
    | Dashboard — diesel price (THB/L) and recent surcharges | [docs/screenshots/dashboard.png](docs/screenshots/dashboard.png) |
    | Approval required — high-value shipment paused for review | [docs/screenshots/hitl-approval.png](docs/screenshots/hitl-approval.png) |
    | Langfuse trace view — every LLM and tool call captured | [docs/screenshots/langfuse-trace.png](docs/screenshots/langfuse-trace.png) |

    ## Repository Layout

    ```
    backend/         # FastAPI + LangGraph orchestrator
      agent/         # graph.py, nodes/, tools/, prompts/, observability.py
      api/           # main.py, routes/ (chat, conversations, fuel_prices, feedback), sse.py
      tests/         # pytest suite — 280+ tests across all phases
    frontend/        # Next.js 15 + React 19 + Tailwind UI
      app/           # Next.js app directory
      components/    # chat/, trace/, dashboard/, sidebar/, shared/
      hooks/         # useChatStream, useConversations, useFuelPrices
      lib/           # api.ts, sse.ts, formatters.ts, constants.ts
      types/         # api.types.ts, agent.types.ts (snake_case mirrored)
    data/            # CSVs, SQLite DBs, scripts/
    docs/            # architecture.md, data-sources.md, demo.mp4, screenshots/
    .planning/       # GSD phase plans, summaries, retrospectives (audit trail)
    ```

    ---

    *Built for MADT7204 by Panjapol Pollot (IT Lead) + team.*
    ```

    Replace the file content as a whole — the existing README likely is a sparse stub.

    Step 3 — Smoke check that GitHub-flavored Markdown renders the Mermaid block (cannot fully verify without push, but at least confirm no broken syntax):
    ```bash
    grep -c '```mermaid' README.md   # should be 1
    grep -c '```' README.md           # should be even (open + close)
    ```
  </action>
  <verify>
    <automated>test -f README.md && grep -q "## Project Overview" README.md && grep -q "## Setup Instructions" README.md && grep -q "## AI Tools Used" README.md && grep -q "## Limitations" README.md && grep -q "Bangkok Metro" README.md && grep -q '```mermaid' README.md && test -f docs/screenshots/.gitkeep && ! grep -q "Central Region" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at repo root
    - `grep -q "## Project Overview" README.md` returns 0
    - `grep -q "## Team" README.md` returns 0
    - `grep -q "## Problem Statement" README.md` returns 0
    - `grep -q "## Agent Design" README.md` returns 0
    - `grep -q "## Data Sources" README.md` returns 0
    - `grep -q "## Setup Instructions" README.md` returns 0
    - `grep -q "## AI Tools Used" README.md` returns 0
    - `grep -q "## Limitations" README.md` returns 0
    - `grep -q "## License" README.md` returns 0
    - `grep -q '```mermaid' README.md` returns 0 (Mermaid block present)
    - `grep -c "Bangkok Metro" README.md` returns ≥ 3
    - `grep -c "Central Region" README.md` returns 0 (backlog 999.2 invariant)
    - `grep -q "docs/architecture.md" README.md` returns 0 (link)
    - `grep -q "docs/data-sources.md" README.md` returns 0 (link)
    - `grep -q "docs/demo.mp4" README.md` returns 0 (link)
    - `test -f docs/screenshots/.gitkeep` exits 0
  </acceptance_criteria>
  <done>README.md rewritten per DOC-01; screenshots directory created; Bangkok Metro phrasing throughout; Mermaid block validates.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/architecture.md with Mermaid + Phase 5 sections</name>
  <files>docs/architecture.md</files>
  <read_first>
    - docs/architecture.md (current ASCII content — Phase 5 PRESERVES ASCII as fallback, ADDS Mermaid + new sections)
    - .planning/phases/05-polish-observability-docs/05-RESEARCH.md §Documentation Deliverables (architecture.md update plan + Mermaid blocks)
    - .planning/phases/05-polish-observability-docs/05-CONTEXT.md (D-18 diagram requirements; Phase 5 topology changes)
    - backend/agent/graph.py (current topology — sanity check Mermaid matches reality)
    - backend/api/sse.py (six EventTypes — Phase 5 adds approval_required)
    - backend/agent/state.py (AgentState fields — Phase 5 adds approval_decision, search_context)
  </read_first>
  <action>
    Step 1 — Read the current `docs/architecture.md`. Identify these sections for update:
    - System Overview
    - Agent Graph Flow (currently ASCII — REPLACE with Mermaid + keep ASCII fallback)
    - Conditional Routing table
    - AgentState schema
    - API Endpoints
    - SSE Event Types
    - Memory Management
    - Error Handling

    Step 2 — Make the following targeted edits. Bangkok Metro phrasing throughout:

    **a) Agent Graph Flow** — replace existing ASCII diagram (or add above it) with the Mermaid flowchart from RESEARCH §Common Operation 4. Keep the existing ASCII as a `<details>` block for terminal-readable fallback:

    ```markdown
    ## Agent Graph Flow

    ```mermaid
    flowchart TD
        START([START]) --> P[Planner Node]
        P -->|next_step=fanout_fuel_route<br/>both stale| FAN{Parallel<br/>superstep}
        FAN --> F[Fuel Agent]
        FAN --> R[Route Agent]
        P -->|fetch_fuel<br/>route cached| F
        P -->|fetch_route<br/>fuel cached| R
        P -->|search_context<br/>news intent| S[Search Agent]
        P -->|calculate_price| PR[Pricing Agent]
        P -->|clarify/respond| RESP[Response Node]
        F --> P
        R --> P
        S --> P
        PR --> H{HITL Gate<br/>total > threshold?}
        H -->|low value| RESP
        H -->|high value| INT([interrupt — await user])
        INT -->|Command resume=approve/deny| RESP
        RESP --> END([END])
    ```

    <details>
    <summary>ASCII diagram (terminal-readable fallback)</summary>

    ```
    [existing ASCII art kept verbatim]
    ```
    </details>
    ```

    **b) Conditional Routing table** — add row for `search_context → search_agent` (was → response stub) and `fanout_fuel_route → [fuel_agent, route_agent]`:

    | next_step | Routes to | Notes |
    |-----------|-----------|-------|
    | fetch_fuel | fuel_agent | Sequential when route is fresh |
    | fetch_route | route_agent | Sequential when fuel is fresh |
    | fanout_fuel_route | [fuel_agent, route_agent] | **Phase 5 NEW** — same-superstep parallel when both stale (D-01) |
    | calculate_price | pricing_agent | After fuel + route + inputs ready |
    | search_context | search_agent | **Phase 5 NEW** — news/market/trend intent (D-09); was response stub |
    | clarify | response | Render clarify-status answer |
    | respond | response | Final hop; status=ok if surcharge ready |

    **c) AgentState schema** — append two field rows:

    | Field | Type | Reducer | Set by | Notes |
    |-------|------|---------|--------|-------|
    | (existing fields preserved) | ... | ... | ... | ... |
    | approval_decision | Optional[Literal["approve", "deny"]] | last-write-wins | hitl_gate_node | **Phase 5 D-07** — HITL gate decision; read by response_node |
    | search_context | Optional[dict] | last-write-wins | search_agent_node | **Phase 5 D-11** — Tavily summary + sources; read by response_node for "Market context:" prefix |

    **d) API Endpoints** — add `POST /api/feedback` row + amend `POST /api/chat` row to include the new `approve` body field:

    | Method | Path | Body | Response | Notes |
    |--------|------|------|----------|-------|
    | POST | /api/chat | {message, thread_id?, approve?} | SSE stream | **Phase 5 NEW** — `approve` body field for HITL resume (D-06); when present, calls Command(resume=approve) |
    | POST | /api/feedback | {thread_id, message_id, score, reason?} | {status, delivered, trace_id?} | **Phase 5 NEW** — DOC-01 / OBS-02 — forwards thumbs vote to Langfuse Score |

    **e) SSE Event Types** — expand from 5 to 6:

    | Event | Payload | Phase |
    |-------|---------|-------|
    | meta | {thread_id} | Phase 3 |
    | trace | TraceEntry (12 fields) | Phase 3 |
    | answer | FinalPayload (markdown, status, surcharge_result, capped, search_context?) | Phase 3 + 5 (search_context optional) |
    | error | {message, retryable} | Phase 3 |
    | done | {} | Phase 3 |
    | **approval_required** | {thread_id, surcharge_result, threshold} | **Phase 5 NEW** (D-06) — emitted before interrupt; FE renders Approve/Deny |

    Per RESEARCH Pitfall 2: `approval_required` is NEVER followed by `done` in the same stream. Resume sends a fresh stream with its own meta + trace + answer + done envelope.

    **f) Memory Management** — note that AsyncSqliteSaver is REQUIRED for HITL resume (was: nice-to-have for follow-ups):

    > Memory persistence is provided by `AsyncSqliteSaver` (langgraph-checkpoint-sqlite 2.0.11) at `data/checkpoints.db`. **Required for HITL resume** (Phase 5) — the `interrupt()` primitive snapshots state to the checkpointer; resume calls reconstruct the in-flight execution state across HTTP requests. Also enables conversation memory: follow-up turns reuse cached fuel/route data per Phase 3 D-12.

    **g) Error Handling** — add three Phase 5 paths:

    - **Tavily failure (Phase 5 D-12)**: `search_fuel_news` raises RuntimeError → `search_agent_node` emits a `warn`-status trace entry; `state.search_context` stays None; planner continues; surcharge response is unaffected.
    - **HITL deny path (Phase 5 D-07)**: `approval_decision="deny"` → `response_node` renders status="partial" with decline prose, no breakdown table; `surcharge_result` retained in state for Langfuse audit but NOT shown in the answer.
    - **Langfuse keys missing (Phase 5 D-13)**: `get_callback_handler()` returns None → chat handler attaches no callbacks; `post_formula_accuracy_score()` no-ops; `POST /api/feedback` returns 200 with `delivered=false`. The agent runs identically without Langfuse — local reproducibility preserved per CLAUDE.md.

    **h) NEW section: Observability Architecture** — add after Memory Management:

    ```markdown
    ## Observability Architecture

    ```mermaid
    flowchart LR
        FE[Frontend] -->|POST /api/chat| CH[Chat Handler]
        CH -->|graph.astream_events with config={callbacks: [CallbackHandler]}| G[LangGraph Graph]
        G --> Planner
        G --> FuelAgent
        G --> RouteAgent
        G --> SearchAgent
        G --> PricingAgent
        G --> ResponseNode
        Planner & FuelAgent & RouteAgent & SearchAgent & PricingAgent & ResponseNode -.LLM + tool calls.-> CB[Langfuse CallbackHandler]
        CB -->|trace_id = seed_trace_id thread_id, turn_idx| LF[Langfuse Cloud]
        PricingAgent -.fire-and-forget.-> AE[post_formula_accuracy_score]
        AE -->|create_score formula_accuracy 1.0/0.0| LF
        FE -->|POST /api/feedback| FB[Feedback Handler]
        FB -->|seed_trace_id same trace as chat turn| LF
        FB -->|create_score user_feedback 1/-1| LF
    ```

    Key invariants:
    - `seed_trace_id(thread_id, turn_idx)` is the SINGLE source of truth for
      trace correlation. Renaming in either chat handler or feedback handler
      breaks the wire silently.
    - Auto-eval is fire-and-forget — never blocks the user response.
    - All Langfuse calls are no-op when `LANGFUSE_*` env vars are missing.
    ```

    **i) NEW section: Parallel Execution** — add after Observability:

    ```markdown
    ## Parallel Execution (ORCH-07)

    The conditional edge from the Planner returns a **list** of node names
    (`["fuel_agent", "route_agent"]`) when both fuel and route are stale
    on the current turn. LangGraph's runtime schedules every node in the
    returned list within the same Pregel superstep — concurrent execution
    of disjoint state writes (fuel_agent writes `fuel_data`; route_agent
    writes `route_data`).

    Trigger condition (D-01):
    1. Planner LLM emits `next_step="fetch_fuel"` or `"fetch_route"`
    2. AND `state.fuel_data` is stale (no `fetched_at` or older than
       `FUEL_DATA_TTL_SECONDS=3600`)
    3. AND `state.route_data` does not match merged origin/destination
    4. AND all extraction inputs (shipping_type, weight_kg, origin,
       destination) are present

    Reducer safety (D-02):
    - `reasoning_trace` and `errors` use `operator.add` reducer —
      concurrent appends merge cleanly (Phase 2 Pitfall 1).
    - `fuel_data` and `route_data` are scalar dict keys with
      last-write-wins; safe because the two branches write disjoint
      keys.

    Demo evidence: trace timestamps for `fuel_agent` and `route_agent`
    on the parallel turn differ by &lt; 1 second (typical: 50–200 ms,
    bounded by network latency to Google Maps + EPPO). Visible in the
    chat trace panel.
    ```

    Step 3 — Run a sanity grep:
    ```bash
    grep -c '```mermaid' docs/architecture.md   # >= 2 (graph topology + observability)
    grep -c "approval_required" docs/architecture.md
    grep -c "search_agent" docs/architecture.md
    grep -c "fanout_fuel_route" docs/architecture.md
    grep -q "Central Region" docs/architecture.md   # MUST return non-zero (i.e., NOT found)
    ```
  </action>
  <verify>
    <automated>test -f docs/architecture.md && grep -q "approval_required" docs/architecture.md && grep -q "search_agent" docs/architecture.md && grep -q "fanout_fuel_route" docs/architecture.md && grep -q "hitl_gate" docs/architecture.md && grep -c '```mermaid' docs/architecture.md | grep -E "^[2-9]" && grep -q "Bangkok Metro" docs/architecture.md && ! grep -q "Central Region" docs/architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "approval_required" docs/architecture.md` returns 0
    - `grep -q "search_agent" docs/architecture.md` returns 0
    - `grep -q "fanout_fuel_route" docs/architecture.md` returns 0
    - `grep -q "hitl_gate" docs/architecture.md` returns 0
    - `grep -q "approval_decision" docs/architecture.md` returns 0 (AgentState schema updated)
    - `grep -q "search_context" docs/architecture.md` returns 0 (AgentState schema updated)
    - `grep -q "POST /api/feedback" docs/architecture.md` returns 0 (API endpoints updated)
    - `grep -q "Observability Architecture" docs/architecture.md` returns 0 (new section)
    - `grep -q "Parallel Execution" docs/architecture.md` returns 0 (new section)
    - `grep -c '```mermaid' docs/architecture.md` returns ≥ 2
    - `grep -c "Bangkok Metro" docs/architecture.md` ≥ 1
    - `grep -c "Central Region" docs/architecture.md` returns 0
  </acceptance_criteria>
  <done>architecture.md updated with Mermaid + Phase 5 topology + observability + parallel + HITL + search sections; Bangkok Metro phrasing.</done>
</task>

<task type="auto">
  <name>Task 3: Create docs/data-sources.md (DOC-04)</name>
  <files>docs/data-sources.md</files>
  <read_first>
    - data/scripts/generate_rate_table.py (rate-table assumptions verbatim)
    - data/scripts/fetch_fuel_prices.py (EPPO fetch mechanics)
    - backend/agent/tools/calculate_route.py (Google Maps usage + cache TTL)
    - backend/agent/tools/search_fuel_news.py (Tavily query + cache TTL — from Plan 05-04)
    - .planning/phases/05-polish-observability-docs/05-RESEARCH.md §Documentation Deliverables (data-sources.md outline)
    - .planning/phases/05-polish-observability-docs/05-CONTEXT.md (D-19 sections)
  </read_first>
  <action>
    Create `docs/data-sources.md`:

    ```markdown
    # Data Sources

    Provenance, refresh cadence, and assumptions for every external data
    source the Express Dynamic Surcharge Orchestrator consumes.

    ## EPPO Diesel B7 Historical Prices

    - **Source:** Thailand Department of Energy Business — Energy Policy
      and Planning Office (EPPO).
      URL: https://www.eppo.go.th/index.php/en/petroleum-statistics/petroleum-data
    - **Refresh cadence:** daily (manual or cron-driven) via
      `data/scripts/fetch_fuel_prices.py`.
    - **Storage:** `data/raw/eppo_diesel_prices.csv`.
    - **Schema:** `date, diesel_b7_price, source` (date YYYY-MM-DD,
      price in THB/L, source tag).
    - **Fallback chain (Phase 2 TOOL-01):** live API → cached CSV →
      hardcoded baseline (29.94 THB/L from `BASELINE_DIESEL_PRICE` env).
      Each step is logged in the agent's reasoning_trace as the `source`
      field on the FuelData payload.
    - **Baseline:** 29.94 THB/L (configurable via `BASELINE_DIESEL_PRICE`
      env). Treated as the surcharge zero-point — when current price
      equals baseline, surcharge_pct = 0.

    ## Simulated Express Rate Table

    - **Generator:** `data/scripts/generate_rate_table.py`.
    - **Schema:** `shipping_type, zone, weight_min_kg, weight_max_kg,
      base_rate_thb`.
    - **Assumptions (verbatim):**
      - Zone multipliers: `central-1 = 1.0`, `central-2 = 1.25`,
        `central-3 = 1.55`. Approximate the cost stack-up from Bangkok
        outward — central-1 is intra-Bangkok (no zone toll), central-2
        adjacent provinces, central-3 the outer Bangkok Metro ring.
      - Shipping multipliers (applied to the surcharge percentage,
        NOT base rate): `bounce = 1.0` (most fuel-sensitive),
        `retail_standard = 0.5`, `retail_fast = 0.8`.
      - Base rate range produced: 50 – 698 THB.
      - Total rows: 45 (3 ship types × 3 zones × 5 weight tiers).
    - **HITL threshold (Phase 5 D-04):** `HITL_TOTAL_THB_THRESHOLD = 500`
      (default). Calibrated empirically against the 45-row distribution
      to gate ~9% of representative demo queries (RESEARCH §HITL
      Threshold Calibration). Override via env to demo more/fewer
      gate triggers.
    - **Why simulated:** real Express tariff sheets are confidential.
      The simulation captures the structural assumptions (zone +
      shipping-type multipliers + weight tiers) sufficient for an
      end-to-end agent demo with transparent, reproducible numbers.

    ## Google Maps Directions API

    - **Endpoint:** `directions/json` via the official `googlemaps` Python
      SDK.
    - **Request shape:** `directions(origin, destination, mode='driving',
      departure_time='now')` — the `departure_time='now'` opt-in returns
      `duration_in_traffic` alongside `duration`, used to compute traffic
      severity (1–5).
    - **Cache:** 15-minute TTL via `backend/agent/tools/_cache.py::TTLCache`,
      keyed on `(origin_normalised, destination_normalised)`.
    - **Bangkok Metro provinces covered:** Bangkok, Nonthaburi, Pathum Thani,
      Samut Prakan, Nakhon Pathom, Samut Sakhon, Ayutthaya.
    - **Province → zone mapping (verbatim):**
      - `central-1`: Bangkok
      - `central-2`: Nonthaburi, Pathum Thani, Samut Prakan, Nakhon Pathom,
        Samut Sakhon, Ayutthaya
      - `central-3`: Reserved for outer Bangkok Metro ring (e.g., outer
        Ayutthaya districts, far Pathum Thani) — currently sparse.
    - **Free-tier quota:** $200/month Google Maps credit (sufficient for
      demo + dev).

    ## Tavily News Search

    - **Endpoint:** `client.search(query, topic='news',
      max_results=5, include_answer='basic', search_depth='basic')` via
      the official `tavily-python` SDK.
    - **Cache:** 30-minute TTL via the same `TTLCache` class. Cache key
      is the normalised query string (`lowercase + strip + collapse
      whitespace`) to mitigate cache-key drift on semantically identical
      queries (RESEARCH §Pitfall 3).
    - **Trigger semantics (D-09):** Planner emits
      `next_step="search_context"` ONLY when its user_intent classifier
      identifies a news/market/trend question (e.g. "why is fuel up",
      "diesel news this week"). Standard `surcharge_query` and
      `followup_query` paths NEVER trigger search by default — quota
      conservation.
    - **Free-tier quota:** 1000 searches/month. Demo footprint estimate
      ~10 searches/run.
    - **Failure handling (D-12):** any Tavily error (network, quota,
      auth) raises `RuntimeError` from `search_fuel_news` → caught by
      `search_agent_node` → emits a `warn`-status trace entry,
      `state.search_context` stays None, planner continues. Search
      failure NEVER blocks the surcharge response.

    ## SQLite Databases

    - **`data/express.db`** — rate_table + zones (seeded by
      `data/scripts/seed_database.py`). Static reference data; not
      modified by the running agent.
    - **`data/checkpoints.db`** — LangGraph `AsyncSqliteSaver`
      conversation memory. One thread per `thread_id`. Stores the full
      state snapshot per superstep — required for HITL resume across
      HTTP requests (Phase 5).

    ## Langfuse Cloud (Observability)

    - **Host:** https://cloud.langfuse.com (free tier).
    - **Captured events:** every Gemini structured-output call,
      every `@tool` invocation, every node entry/exit. Single
      `langfuse.langchain.CallbackHandler` attached at the chat handler
      boundary captures the full graph automatically.
    - **Trace naming:** `chat_turn_{thread_id}_{turn_idx}` —
      deterministic seed via `langfuse.create_trace_id(seed=...)` so
      `POST /api/feedback` resolves the same trace ID without a name
      lookup.
    - **Scores:**
      - `formula_accuracy` (1.0 / 0.0) — fire-and-forget auto-eval
        after pricing_agent.
      - `user_feedback` (+1 / -1) — thumbs vote forwarded by
        `POST /api/feedback`.
    - **Graceful no-op:** when any of `LANGFUSE_PUBLIC_KEY`,
      `LANGFUSE_SECRET_KEY`, `LANGFUSE_HOST` is missing, the callback
      handler is None and all Score helpers return without calling the
      API. Agent runs identically — local reproducibility preserved per
      CLAUDE.md.

    ## Internal Constants (config.py)

    | Constant | Default | Meaning |
    |----------|---------|---------|
    | BASELINE_DIESEL_PRICE | 29.94 THB/L | Surcharge zero-point |
    | SURCHARGE_CAP | 0.15 | Maximum surcharge percentage (15%) |
    | SURCHARGE_FLOOR | -0.05 | Minimum surcharge percentage (-5%) |
    | FUEL_DATA_TTL_SECONDS | 3600 | Phase 3 D-12 cache freshness window |
    | ROUTE_CACHE_TTL_SECONDS | 900 | Google Maps cache TTL |
    | SEARCH_CACHE_TTL_SECONDS | 1800 | Tavily cache TTL (Phase 5) |
    | HITL_TOTAL_THB_THRESHOLD | 500.0 | HITL gate trigger (Phase 5) |
    | PLANNER_MAX_ITERATIONS | 6 | Planner loop budget per turn |
    | TRAFFIC_RATIO_BUCKETS | 1.1,1.3,1.5,1.8 | Severity thresholds (Phase 2) |
    ```

    Sanity grep:
    ```bash
    grep -q "Bangkok Metro" docs/data-sources.md
    grep -q "EPPO" docs/data-sources.md
    grep -q "Tavily" docs/data-sources.md
    grep -q "Langfuse" docs/data-sources.md
    ! grep -q "Central Region" docs/data-sources.md
    ```
  </action>
  <verify>
    <automated>test -f docs/data-sources.md && grep -q "EPPO" docs/data-sources.md && grep -q "Tavily" docs/data-sources.md && grep -q "Langfuse" docs/data-sources.md && grep -q "Bangkok Metro" docs/data-sources.md && ! grep -q "Central Region" docs/data-sources.md && grep -q "HITL_TOTAL_THB_THRESHOLD" docs/data-sources.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/data-sources.md` exits 0
    - `grep -q "## EPPO Diesel B7" docs/data-sources.md` returns 0
    - `grep -q "## Simulated Express Rate Table" docs/data-sources.md` returns 0
    - `grep -q "## Google Maps Directions API" docs/data-sources.md` returns 0
    - `grep -q "## Tavily News Search" docs/data-sources.md` returns 0
    - `grep -q "## SQLite Databases" docs/data-sources.md` returns 0
    - `grep -q "## Langfuse Cloud" docs/data-sources.md` returns 0
    - `grep -q "Bangkok Metro" docs/data-sources.md` returns 0
    - `grep -c "Central Region" docs/data-sources.md` returns 0
    - `grep -q "HITL_TOTAL_THB_THRESHOLD" docs/data-sources.md` returns 0
  </acceptance_criteria>
  <done>data-sources.md created with all DOC-04 sections; Bangkok Metro phrasing; calibration assumptions transparent.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: HUMAN — record demo.mp4 + capture 5 screenshots</name>
  <files>docs/demo.mp4, docs/screenshots/chat-breakdown.png, docs/screenshots/trace-parallel.png, docs/screenshots/dashboard.png, docs/screenshots/hitl-approval.png, docs/screenshots/langfuse-trace.png</files>
  <action>This is a HUMAN-only checkpoint. Claude cannot run a screen recorder or interact with a live browser session. The full step-by-step procedure is documented under &lt;how-to-verify&gt; below. The user must produce 6 artifacts (demo recording + 5 screenshots) using a screen recording tool of their choice (QuickTime, Loom, OBS Studio, ScreenFlow), commit them to docs/, and signal completion.</action>
  <what-built>
    The Phase 5 system is fully implemented across plans 01–06: parallel fan-out, HITL approval gate, Tavily search agent, Langfuse observability, feedback wire, and ApprovalCard / SearchContextLine UI. The README references a demo recording at `docs/demo.mp4` and 5 screenshots at `docs/screenshots/{chat-breakdown,trace-parallel,dashboard,hitl-approval,langfuse-trace}.png`.

    These artifacts cannot be produced by Claude — they require running the live UI with real API keys (Gemini, Google Maps, Tavily, Langfuse) and a screen recorder.
  </what-built>
  <how-to-verify>
    Recording script (~90 seconds):

    1. Start backend: `uvicorn backend.api.main:app --port 8000`
    2. Start frontend: `cd frontend && npm run dev`
    3. Open http://localhost:3000 with Langfuse Cloud dashboard in another tab.
    4. **Recording starts.** Submit prompt #1: `Calculate surcharge for 15kg bounce shipment from Bangkok to Nonthaburi`
       - Capture **screenshot 1** (chat-breakdown.png) when the answer renders with breakdown table.
       - Capture **screenshot 2** (trace-parallel.png) by expanding `fuel_agent` and `route_agent` trace entries to show overlapping timestamps.
    5. Submit prompt #4: `Calculate surcharge for 200kg retail_fast from Bangkok to a central-3 destination` (or any query that crosses the 500 THB threshold).
       - Capture **screenshot 4** (hitl-approval.png) when ApprovalCard renders with Approve/Deny buttons.
       - Click Approve — observe final answer renders.
    6. Click the Dashboard tab — capture **screenshot 3** (dashboard.png) showing the fuel price chart + surcharge history chart.
    7. Click thumbs-up on the surcharge answer.
    8. Open the Langfuse Cloud dashboard — locate the `chat_turn_*` trace from step 5. Capture **screenshot 5** (langfuse-trace.png) showing the trace timeline, the `formula_accuracy` Score, and the `user_feedback` Score.
    9. **Stop recording.** Save as `docs/demo.mp4` (1080p+; .gif or .mp4 acceptable per UI-SPEC).
    10. Save the 5 screenshots into `docs/screenshots/` with the EXACT filenames listed above.

    Tooling notes:
    - macOS: QuickTime Player → File → New Screen Recording (CMD+R for area). Export as .mp4.
    - Cross-platform: Loom, OBS Studio, ScreenFlow — any tool that exports .mp4.
    - Bangkok Metro phrasing on every visible UI surface verified — no "Central Region" leakage.

    Once artifacts are placed, also confirm:
    ```bash
    test -f docs/demo.mp4 || test -f docs/demo.gif
    ls docs/screenshots/{chat-breakdown,trace-parallel,dashboard,hitl-approval,langfuse-trace}.png
    ```

    Each command must succeed.
  </how-to-verify>
  <verify>The user runs the post-recording commands listed in &lt;how-to-verify&gt; above; both must exit 0 (`test -f docs/demo.mp4 || test -f docs/demo.gif` AND `ls docs/screenshots/{chat-breakdown,trace-parallel,dashboard,hitl-approval,langfuse-trace}.png`).</verify>
  <done>docs/demo.mp4 (or .gif) exists and 5 screenshots with the exact required filenames are committed to docs/screenshots/.</done>
  <resume-signal>Type "approved" once all 6 artifacts (demo.mp4 + 5 screenshots) are committed under docs/. Or describe any gap (e.g., "Langfuse rate-limited, will record screenshot later") and we'll proceed without it.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: HUMAN — merge develop → main + tag v1.0 (D-21)</name>
  <files>(no files edited; only git ref changes — main branch advance + v1.0 tag)</files>
  <action>This is a HUMAN-only checkpoint. Per D-21 only the IT Lead pushes the v1.0 tag. The full git command sequence is documented under &lt;how-to-verify&gt; below. The user must run the pre-merge checklist (test suites green, artifacts present), execute the merge + annotated tag commands, push main + v1.0, and signal completion.</action>
  <what-built>
    All Phase 5 deliverables (code, tests, docs, demo, screenshots) are committed on the `develop` branch. The submission deliverable is the `v1.0` annotated git tag on the `main` merge commit (per D-21). Only the IT Lead pushes the tag.
  </what-built>
  <how-to-verify>
    Pre-merge checklist (run from repo root):
    ```bash
    # 1. All Phase 5 plans show as committed on develop.
    git log --oneline | head -30

    # 2. Full test suite green.
    .venv/bin/pytest backend/tests/ -x -q
    cd frontend && npm test
    cd frontend && npm run test:e2e   # if Playwright e2e specs exist for Phase 5

    # 3. Required artifacts exist.
    ls README.md docs/architecture.md docs/data-sources.md docs/demo.mp4 || ls docs/demo.gif
    ls docs/screenshots/*.png | wc -l   # >= 5

    # 4. No Central Region leakage in user-facing docs.
    ! grep -r "Central Region" README.md docs/architecture.md docs/data-sources.md
    ```

    Merge + tag:
    ```bash
    git checkout main
    git pull origin main
    git merge --no-ff develop -m "Merge develop into main for v1.0 submission"

    # Annotated tag with deliverables checklist (D-21).
    git tag -a v1.0 -m "v1.0 — MADT7204 final submission

    Includes:
    - README per DOC-01 (project overview, team, problem, agent design,
      data sources, setup, AI tools, limitations, license).
    - docs/architecture.md (Mermaid + ASCII; parallel + HITL + search +
      Langfuse).
    - docs/data-sources.md (EPPO + simulated rate-table assumptions +
      Google Maps + Tavily + Langfuse + internal constants).
    - docs/demo.mp4 (1–2 min end-to-end including parallel timestamps +
      HITL approval).
    - 5 screenshots in docs/screenshots/.

    Backend tests: <count> passing.
    Frontend tests: <unit>+<e2e> passing.

    Phase 5 implements: ORCH-07 (parallel), ORCH-09 (HITL), TOOL-05 (Tavily),
    API-05 (feedback), OBS-01/02/03 (Langfuse traces + scores), DOC-01/02/04."

    git push origin main
    git push origin v1.0
    ```

    Replace `<count>` and `<unit>+<e2e>` with the actual numbers from the
    test runs above.

    Final verification:
    ```bash
    git tag -l v1.0
    git show v1.0 --stat | head -50    # confirms tag annotation + commit content
    ```

    Submit the repo URL (https://github.com/<org>/<repo>) per the course
    submission portal.
  </how-to-verify>
  <verify>The user runs `git tag -l v1.0` from the repo root (after pushing) and the output is exactly `v1.0`. `git show v1.0 --stat | head -1` shows an annotated-tag header (i.e., `tag v1.0` line, NOT `commit ...`).</verify>
  <done>main branch advanced to merge commit; annotated v1.0 tag created with submission deliverables checklist; both pushed to origin; repo URL submitted.</done>
  <resume-signal>Type "tagged" once v1.0 is on main and pushed. Phase 5 is then complete.</resume-signal>
</task>

</tasks>

<verification>
**Plan 05-07 acceptance:**
1. `test -f README.md && test -f docs/architecture.md && test -f docs/data-sources.md` exits 0
2. `grep -c "Central Region" README.md docs/architecture.md docs/data-sources.md` returns 0 (Bangkok Metro invariant)
3. `grep -c '```mermaid' README.md docs/architecture.md` returns ≥ 3 (1 in README + ≥ 2 in architecture.md)
4. `ls docs/screenshots/*.png | wc -l` ≥ 5 (after Task 4 checkpoint)
5. `test -f docs/demo.mp4 || test -f docs/demo.gif` exits 0 (after Task 4)
6. `git tag -l v1.0` returns `v1.0` (after Task 5)

**Doc smoke (manual on GitHub web UI):**
- Visit the develop branch on GitHub. README renders with the Mermaid graph; no broken-block parsing. All section headings present.
- Visit `docs/architecture.md` on GitHub. Both Mermaid blocks render natively.
- Visit `docs/data-sources.md`. All sections present.
- All linked screenshots resolve (no 404s).
</verification>

<success_criteria>
1. README.md rewritten per DOC-01 with all 9 enumerated sections.
2. docs/architecture.md updated with Mermaid blocks + Phase 5 topology + Observability + Parallel Execution sections.
3. docs/data-sources.md created with EPPO + simulated rate table + Google Maps + Tavily + Langfuse + internal constants.
4. 5 screenshots in `docs/screenshots/` with the exact filenames the README references.
5. `docs/demo.mp4` (or .gif fallback) at the location the README references.
6. Bangkok Metro phrasing throughout — no "Central Region" leakage in any user-facing doc.
7. v1.0 annotated tag on the main merge commit with deliverables checklist annotation; pushed to remote.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-observability-docs/05-07-SUMMARY.md` covering:
- README sections delivered
- architecture.md sections updated + new (Observability, Parallel Execution)
- data-sources.md sections delivered
- Screenshots + demo recording (filenames + sizes if known)
- v1.0 tag annotation message
- Final test counts (backend + frontend unit + e2e)
- Phase 5 retrospective bullet points (what worked, what surprised)
- Commit SHA of the main merge commit + the v1.0 tag SHA
</output>
