---
phase: quick-260425-vyj-fix-planner-bugs-999-1-state-merge-on-fo
verified: 2026-04-25T17:00:00Z
status: passed
score: 6/6 must-haves verified
---

# Quick Task 260425-vyj: Fix Planner Bugs 999.1 + 999.3 Verification Report

**Task Goal:** Fix planner bugs 999.1 (planner doesn't merge prior conversation state into extracted fields on follow-up turns) and 999.3 (planner serialized next_step in trace is stale across loop iterations). All existing 103 tests must still pass; 2+ regression tests required (one for each bug); only `backend/agent/nodes/planner.py`, `backend/agent/prompts/planner.py`, `backend/tests/test_planner.py`, `backend/tests/test_graph.py`, and `.planning/ROADMAP.md` may be touched.

**Verified:** 2026-04-25
**Status:** passed
**Re-verification:** No (initial verification)

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                                          | Status     | Evidence                                                                                                                                                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | On a follow-up turn that mentions only one of the four extraction fields (e.g. switches shipping_type), the planner does NOT emit next_step=clarify when prior state already has the unmentioned fields.                       | VERIFIED   | `planner.py:213-218` adds the clarify->fetch_fuel promotion: `if next_step == "clarify" and not missing: next_step = "fetch_fuel"`. Unit test `test_followup_merges_prior_state_promotes_clarify_to_fetch` (test_planner.py:183-218) exercises exactly this scenario and asserts `result["next_step"] == "fetch_fuel"`. |
| 2   | After the D-12 cache-aware override mutates the local next_step, the trace entry's tool_output.next_step matches the actual routed step (not the pre-override LLM emission).                                                   | VERIFIED   | `planner.py:257-266` constructs trace `tool_output` as an explicit dict using the post-override `next_step` local. Test `test_trace_tool_output_reflects_post_override_next_step` (test_planner.py:273-313) asserts `trace_output["next_step"] == "fetch_route"` while `parsed.next_step` was `"fetch_fuel"`.            |
| 3   | After the merge, the trace entry's tool_output.shipping_type / weight_kg / origin / destination / missing_fields reflect the merged values returned to the graph (not the raw LLM emission).                                   | VERIFIED   | `planner.py:257-266` includes `merged_shipping`, `merged_weight`, `merged_origin`, `merged_destination`, `missing` (post-override) in the trace dict. Test `test_trace_tool_output_reflects_merged_inherited_fields` (test_planner.py:316-349) asserts trace shows merged `weight_kg=15.0` while LLM emitted `null`.    |
| 4   | All 103 existing backend tests still pass after the fix.                                                                                                                                                                       | VERIFIED   | `python -m pytest backend/tests/ --tb=no` -> `108 passed, 4 warnings in 3.72s`. 103 baseline + 4 new planner unit tests + 1 new E2E test = 108 (matches plan).                                                                                                                                                          |
| 5   | At least one new pytest test fails on the unfixed planner.py and passes after the fix for bug 999.1 (state merge on follow-up).                                                                                                | VERIFIED   | Two tests: `test_followup_merges_prior_state_promotes_clarify_to_fetch` (unit) and `test_followup_param_switch_routes_through_pricing` (E2E). Both directly exercise the clarify->fetch promotion / D-12 cascade — without the fix the LLM's `next_step=clarify` would propagate and the assertions would fail.        |
| 6   | At least one new pytest test fails on the unfixed planner.py and passes after the fix for bug 999.3 (trace narration matches routed step).                                                                                     | VERIFIED   | `test_trace_tool_output_reflects_post_override_next_step` asserts `trace_output["next_step"] == "fetch_route"` after a D-12 override forced fetch_fuel -> fetch_route. The pre-fix `parsed.model_dump()` would have emitted `"fetch_fuel"`, failing the assertion. Plus `test_trace_tool_output_reflects_merged_inherited_fields`. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact                                                          | Expected                                                                                                              | Status     | Details                                                                                                                                                                                                                                                                       |
| ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `backend/agent/nodes/planner.py`                                  | Post-merge recompute of missing_fields + next_step; trace tool_output reflects post-override merged values            | VERIFIED   | Lines 192-218: `merged_shipping/weight/origin/destination` locals + `missing` recompute + clarify->fetch_fuel promotion. Lines 220-236: D-12 override uses `merged_origin`/`merged_destination`. Lines 257-266: explicit-dict trace `tool_output`. No `parsed.model_dump()` remaining. |
| `backend/tests/test_planner.py`                                   | Unit regression tests for 999.1 (state merge) and 999.3 (trace next_step matches override)                            | VERIFIED   | Test count: 9 (5 existing + 4 new). New tests: `test_followup_merges_prior_state_promotes_clarify_to_fetch`, `test_followup_with_full_cache_routes_calculate_price`, `test_trace_tool_output_reflects_post_override_next_step`, `test_trace_tool_output_reflects_merged_inherited_fields`. All pass. |
| `backend/tests/test_graph.py`                                     | E2E regression test for 999.1 covering parameter-switch follow-up on the same thread_id                               | VERIFIED   | New test `test_followup_param_switch_routes_through_pricing` (lines 492-647) uses `in_memory_checkpointer`, two `graph.ainvoke` calls with thread_id `t-vyj-followup`, asserts surcharge_result not None + shipping_type=='bounce' + cache hits + lookup re-fired + status=='ok'. |
| `.planning/ROADMAP.md`                                            | Backlog entries for 999.1 and 999.3 marked Resolved 2026-04-25                                                        | VERIFIED   | `grep -E "^### 999\.(1|2|3):" .planning/ROADMAP.md \| wc -l` -> 3. Both 999.1 (lines 141-151) and 999.3 (lines 153-163) entries cite the slug `260425-vyj-fix-planner-bugs-999-1-state-merge-on-fo` with `Status: Resolved 2026-04-25`. 999.2 entry unchanged.                       |

### Key Link Verification

| From                                                       | To                                                       | Via                                                                                | Status | Details                                                                                                                                                                                                                            |
| ---------------------------------------------------------- | -------------------------------------------------------- | ---------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| planner.py merge block (~lines 192-218)                    | next_step / missing_fields recompute                     | merged_* locals computed BEFORE trace dict and BEFORE the return                   | WIRED  | Confirmed: merged_* lines 192-199, missing line 203-211, promotion line 217-218, all BEFORE the D-12 override (220-236) and the return dict (238-275).                                                                              |
| planner.py trace entry tool_output                         | post-override next_step + merged extraction fields       | explicit dict construction (not parsed.model_dump())                               | WIRED  | Confirmed: lines 257-266 are an explicit dict literal containing `next_step` (post-override local), `shipping_type=merged_shipping`, etc. Grep for `parsed\.model_dump` in planner.py -> 0 matches.                                  |
| test_graph.py follow-up test                               | in_memory_checkpointer fixture from conftest.py          | graph.ainvoke with same thread_id across two turns                                 | WIRED  | `test_followup_param_switch_routes_through_pricing` accepts `in_memory_checkpointer` fixture (line 494), uses `cfg = {"configurable": {"thread_id": "t-vyj-followup"}}` (line 612), invokes `graph.ainvoke` twice with same `cfg` (lines 619, 635). |

### Behavioral Spot-Checks

| Behavior                                                                                                                              | Command                                                                                                                                                                            | Result                                                          | Status |
| ------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ------ |
| Full backend test suite passes (108 tests, 0 failures)                                                                                | `python -m pytest backend/tests/ --tb=no`                                                                                                                                          | `108 passed, 4 warnings in 3.72s`                               | PASS   |
| New 999.1/999.3 unit tests pass (4 new tests in test_planner.py)                                                                       | `python -m pytest backend/tests/test_planner.py -k "follow or trace_tool"`                                                                                                         | `4 passed, 5 deselected`                                        | PASS   |
| New 999.1 E2E test passes (1 new test in test_graph.py)                                                                                | `python -m pytest backend/tests/test_graph.py -k "follow"`                                                                                                                         | `3 passed, 5 deselected` (existing 2 + new 1)                   | PASS   |
| `parsed.model_dump()` no longer present in planner.py (999.3 narration fix)                                                            | grep `parsed\.model_dump` in planner.py                                                                                                                                            | `No matches found`                                              | PASS   |
| ROADMAP.md has 3 backlog subsections (999.1, 999.2, 999.3)                                                                            | `grep -E "^### 999\.(1|2|3):" .planning/ROADMAP.md \| wc -l`                                                                                                                       | `3`                                                             | PASS   |
| Only allowed files were touched by the three task commits                                                                             | `git diff b3ca85c^..dcf9984 --stat`                                                                                                                                                | 4 files: planner.py, test_planner.py, test_graph.py, ROADMAP.md | PASS   |

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                              | Status     | Evidence                                                                                                                                                                |
| ----------- | ----------- | -------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BUG-999.1   | 260425-vyj-PLAN | Planner state merge on follow-up turns; parameter-switch follow-up must route through pricing not clarify  | SATISFIED  | Fix in planner.py:192-218 (merge + recompute + promotion). Unit test `test_followup_merges_prior_state_promotes_clarify_to_fetch` + `test_followup_with_full_cache_routes_calculate_price` + E2E test `test_followup_param_switch_routes_through_pricing` all pass. |
| BUG-999.3   | 260425-vyj-PLAN | Trace tool_output narration mismatch; tool_output.next_step must match actually-routed step               | SATISFIED  | Fix in planner.py:257-266 (explicit dict with post-override values). Unit tests `test_trace_tool_output_reflects_post_override_next_step` + `test_trace_tool_output_reflects_merged_inherited_fields` pass.                                                          |

### Anti-Patterns Found

| File                                | Line  | Pattern                                                                                                | Severity | Impact                                                                                                                                                                                                                                                                                                                                                            |
| ----------------------------------- | ----- | ------------------------------------------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| backend/tests/test_graph.py         | 517   | `monkeypatch.setattr(planner_mod, "PLANNER_MAX_ITERATIONS", 100)`                                       | Info     | Monkey-patch is scoped to a single test (`test_followup_param_switch_routes_through_pricing`) via the pytest `monkeypatch` fixture which auto-reverts at test end. Documented in test docstring (lines 511-516) explaining D-04 interaction with two-turn-same-thread design. Not a regression of D-04 — the existing `test_loop_budget_exhaustion_forces_respond` still uses the default value and passes. |
| backend/tests/test_graph.py         | 269-337, 411-482 | Pre-existing tests `test_followup_only_runs_pricing` and `test_followup_reuses_cached_fuel` "pass vacuously" because turn 2's planner short-circuits via D-04 before assertions fire | Info     | **Out of scope for this quick task.** Flagged by executor in SUMMARY for future quality work. The tests still pass; their guard value is weak under the current D-04 + reasoning_trace accumulation interaction. Future quality candidate: either (a) bump `PLANNER_MAX_ITERATIONS` in those tests too, or (b) add a test-only mechanism for clearing reasoning_trace between turns. |

No blocker or warning anti-patterns. The two info-level items above are documented and explicitly out of scope per the task brief.

### Human Verification Required

None. All assertions are programmatically verifiable, and the live-smoke-testing reproducer ("Calculate surcharge for 50kg retail_standard from Bangkok to Pathum Thani" -> "What if I switched it to a Bounce shipment instead?") is now exercised end-to-end by `test_followup_param_switch_routes_through_pricing`. Optional human re-test on a live LLM is encouraged but not required for verification — the unit + E2E coverage proves the merge/promotion/trace logic deterministically.

### Sanity-Check: Unchanged Surfaces

| Surface                                                                  | Expected                          | Verified                                                                                                                                                                                                                |
| ------------------------------------------------------------------------ | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `backend/agent/prompts/planner.py` SYSTEM_PROMPT                         | Unchanged (per Option-b decision) | `git diff acbc3b5..HEAD -- backend/agent/prompts/planner.py` returns empty diff. Allowed file but explicitly not modified per design.                                                                                    |
| `PlannerOutput` Pydantic schema (Literal vocabulary, field types)        | Unchanged                         | Schema definition (planner.py:42-67) is identical to the pre-fix version per `git diff` review. No changes to user_intent, shipping_type, weight_kg, origin, destination, missing_fields, next_step, clarification_reason. |
| D-04 loop budget guard (`_loop_budget_exhausted`)                        | Unchanged                         | Function definition (planner.py:95-104) and call site (line 143) preserved verbatim.                                                                                                                                      |
| D-02 one-retry parse + clarify fallback                                  | Unchanged                         | Try/except loop (planner.py:160-183) and `planner_parse_failed` clarification reason (line 182) preserved.                                                                                                                |
| D-24 error sink (`if state.get("errors"): return {"next_step": "respond"}`) | Unchanged                         | Branch (planner.py:139-140) preserved verbatim BEFORE the D-04 guard.                                                                                                                                                     |
| D-12 cache-aware override semantics                                      | Updated to use merged_*           | Override block (planner.py:223-236) now references `merged_origin`/`merged_destination` and `merged_shipping`/`merged_weight` (instead of `parsed.origin`/`parsed.destination`/etc). This is an INTENTIONAL upgrade documented in the plan — without it, parameter-switch follow-ups would not hit the route cache. Verified via `test_followup_with_full_cache_routes_calculate_price`. |

### Gaps Summary

No gaps. The fix is complete, deterministic, and well-tested:

- **999.1 (HIGH IMPACT):** Post-LLM merge + recompute + clarify->fetch_fuel promotion correctly route parameter-switch follow-ups through pricing (proven by unit + E2E tests).
- **999.3 (LOW IMPACT, narration only):** Trace `tool_output` is now an explicit dict reflecting post-override values, eliminating the stale narration bug (proven by two unit tests).
- **Constraints honored:** All 4 task-commit changes are in the 5 allowed files (planner.py, test_planner.py, test_graph.py, ROADMAP.md). `backend/agent/prompts/planner.py` was allowed but intentionally not touched per Option-b design. 108 tests pass (103 baseline + 5 new). PlannerOutput schema, D-02/D-04/D-24 guards, and SYSTEM_PROMPT are all unchanged.
- **Single deviation (executor's Rule 1 fix):** `monkeypatch.setattr(planner_mod, "PLANNER_MAX_ITERATIONS", 100)` is scoped to one E2E test only — it bypasses the orthogonal D-04 short-circuit caused by reasoning_trace accumulation across turns, NOT a regression of D-04 behavior. Documented in test docstring and in SUMMARY's Deviations section.
- **Note for future work:** Two pre-existing tests (`test_followup_only_runs_pricing`, `test_followup_reuses_cached_fuel`) "pass vacuously" because turn-2 planner short-circuits via D-04 before assertions fire. Out of scope for this task; flagged in SUMMARY for future quality work.

---

_Verified: 2026-04-25_
_Verifier: Claude (gsd-verifier)_
