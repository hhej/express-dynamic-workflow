---
phase: quick-260503-rs8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - backend/api/routes/chat.py
  - backend/tests/test_observability_wiring.py
autonomous: true
requirements:
  - OBS-FIX-LANGCHAIN-PIN
  - OBS-FIX-TRACE-NAME

must_haves:
  truths:
    - "Installing from requirements.txt produces a working `from langfuse.langchain import CallbackHandler` import path (the missing top-level `langchain` package is pinned)."
    - "Every Langfuse trace emitted by /api/chat carries the trace_name `express-surcharge-agent`, so the Langfuse dashboard can filter all agent traces under one stable name."
    - "Existing _make_config metadata keys (langfuse_session_id, langfuse_user_id, langfuse_tags, langfuse_trace_id) are unchanged — only one new key is added."
    - "All 186 existing backend tests still pass; the metadata-shape assertion in test_observability_wiring.py now also covers langfuse_trace_name."
  artifacts:
    - path: "requirements.txt"
      provides: "langchain==0.3.28 pin grouped with the other lang* dependencies"
      contains: "langchain==0.3.28"
    - path: "backend/api/routes/chat.py"
      provides: "_make_config returning metadata with `langfuse_trace_name: 'express-surcharge-agent'`"
      contains: "langfuse_trace_name"
    - path: "backend/tests/test_observability_wiring.py"
      provides: "Extended assertion in test_chat_attaches_callback_when_enabled (or test_chat_skips_callback_when_disabled — whichever already inspects metadata shape) covering the new key"
      contains: "langfuse_trace_name"
  key_links:
    - from: "requirements.txt"
      to: "backend/agent/observability.py:50 (`from langfuse.langchain import CallbackHandler`)"
      via: "pip-installed top-level `langchain` package required by langfuse.langchain"
      pattern: "langchain==0\\.3\\.28"
    - from: "backend/api/routes/chat.py:_make_config"
      to: "Langfuse Cloud trace metadata (consumed via the CallbackHandler)"
      via: "metadata.langfuse_trace_name"
      pattern: "\"langfuse_trace_name\":\\s*\"express-surcharge-agent\""
---

<objective>
Land three surgical fixes that make Langfuse tracing actually fire in the live system AND give every trace a stable, filterable name.

Purpose: Root cause is the `from langfuse.langchain import CallbackHandler` line in `observability.py:50` — it requires the top-level `langchain` package, but `requirements.txt` only pins `langchain-core==0.3.84`. Because `get_callback_handler()` silently returns None on the import failure (graceful no-op pattern, by design), the 186-test suite passed while OBS-01/02/03 were effectively dead in production. Live install of `langchain==0.3.28` confirmed compatibility and 25 traces successfully landed in Langfuse Cloud. While we are touching `_make_config`, also add `langfuse_trace_name: "express-surcharge-agent"` (CONSTANT, not derived from message/intent) so the dashboard has one stable name to filter by.

Output:
- `requirements.txt` gains one pinned line: `langchain==0.3.28`.
- `backend/api/routes/chat.py:_make_config` returns one extra metadata key: `langfuse_trace_name: "express-surcharge-agent"`; docstring Returns section gains one matching bullet.
- `backend/tests/test_observability_wiring.py` extends the existing `_make_config` metadata-shape assertion with one new line.
- `pytest backend/tests/` shows 186/186 (or 187/187 if a brand-new test was preferred) passing.
</objective>

<execution_context>
@/Users/pollot/Desktop/express-dynamic-workflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pollot/Desktop/express-dynamic-workflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pollot/Desktop/express-dynamic-workflow/.planning/STATE.md
@/Users/pollot/Desktop/express-dynamic-workflow/CLAUDE.md
@/Users/pollot/Desktop/express-dynamic-workflow/requirements.txt
@/Users/pollot/Desktop/express-dynamic-workflow/backend/api/routes/chat.py
@/Users/pollot/Desktop/express-dynamic-workflow/backend/agent/observability.py
@/Users/pollot/Desktop/express-dynamic-workflow/backend/tests/test_observability_wiring.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. -->
<!-- Use these directly — no further exploration needed. -->

From backend/agent/observability.py (UNCHANGED by this plan — referenced for context only):
```python
def get_callback_handler(trace_id: Optional[str] = None):
    """Returns a Langfuse CallbackHandler, or None when keys missing."""
    if not _enabled():
        return None
    try:
        from langfuse.langchain import CallbackHandler  # <-- THIS IS THE LINE
        # that silently returned None until langchain was installed.
        ...
```

From backend/api/routes/chat.py:_make_config (BEFORE this plan — current shape):
```python
def _make_config(thread_id: str, turn_idx: int) -> dict:
    trace_id = seed_trace_id(thread_id, turn_idx)
    handler = get_callback_handler(trace_id=trace_id)
    return {
        "configurable": {"thread_id": thread_id},
        "callbacks": [handler] if handler else [],
        "metadata": {
            "langfuse_session_id": thread_id,
            "langfuse_user_id": "demo",
            "langfuse_tags": ["express-surcharge", f"turn-{turn_idx}"],
            "langfuse_trace_id": trace_id,
        },
    }
```

After this plan, the metadata dict gains exactly ONE new key (constant value):
```python
"langfuse_trace_name": "express-surcharge-agent",
```

From backend/tests/test_observability_wiring.py — the existing `_make_config` metadata-shape assertions live in:
- test_chat_attaches_callback_when_enabled (lines 19-37) — asserts session_id, trace_id, user_id, tags
- test_chat_skips_callback_when_disabled (lines 40-53) — asserts trace_id, tags
EXTEND the FIRST test (test_chat_attaches_callback_when_enabled) with one new assertion:
```python
assert cfg["metadata"]["langfuse_trace_name"] == "express-surcharge-agent"
```
The skips-when-disabled test does NOT need the assertion — _make_config builds the same metadata regardless of whether keys are set, but extending only the enabled-path test is sufficient to lock the contract and matches the user's "single test extension preferred" guidance.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin langchain dep, add langfuse_trace_name, extend test</name>
  <files>requirements.txt, backend/api/routes/chat.py, backend/tests/test_observability_wiring.py</files>
  <action>
Three atomic edits — single commit. All three are co-required: the dep pin makes the CallbackHandler import succeed at runtime, the trace_name gives the now-firing traces a stable filter name in the Langfuse dashboard, and the test assertion locks the metadata contract.

(1) **requirements.txt** — Append `langchain==0.3.28` directly under the existing `langchain-core==0.3.84` line so the lang* pins stay grouped (they already cluster `langgraph`, `langchain-core`, `langchain-google-genai` in the "Phase 2 additions" block; the new line should sit between `langchain-core==0.3.84` and `langchain-google-genai==2.1.12`). Reason for pinning 0.3.28 specifically: that exact version was verified compatible with the existing `langchain-core==0.3.84` and `langfuse==4.5.1` pins via a live install where 25 traces successfully reached Langfuse Cloud. DO NOT bump to a different version. DO NOT touch any other line.

(2) **backend/api/routes/chat.py:_make_config** — Inside the metadata dict (currently has 4 keys: langfuse_session_id, langfuse_user_id, langfuse_tags, langfuse_trace_id), add one new key: `"langfuse_trace_name": "express-surcharge-agent"`. The value is a STRING CONSTANT — not f-string-interpolated, not derived from thread_id/turn_idx/message/intent. User decision: one stable name to filter by in the dashboard. Place it adjacent to the existing keys; suggested ordering is right after `langfuse_session_id` so session/name/user/tags/id reads naturally, but any position inside the dict is acceptable. Then update the docstring's Returns section: add one bullet under the existing five, mirroring their format, e.g.:
```
- ``metadata.langfuse_trace_name``: ``"express-surcharge-agent"`` —
  constant agent name so all traces filter under one Langfuse name.
```
DO NOT touch session_id/user_id/tags/trace_id keys, the seed_trace_id call, the get_callback_handler call, the configurable.thread_id key, or the callbacks list. DO NOT touch observability.py — the import-time failure is fixed by the requirements.txt pin alone.

(3) **backend/tests/test_observability_wiring.py** — In `test_chat_attaches_callback_when_enabled` (around line 19), after the existing 6 metadata assertions (lines 33-37: session_id, trace_id, user_id, tags), add one new line:
```python
assert cfg["metadata"]["langfuse_trace_name"] == "express-surcharge-agent"
```
DO NOT add a new standalone test. DO NOT extend test_chat_skips_callback_when_disabled — the enabled-path test is sufficient since _make_config builds metadata identically regardless of key presence (callbacks list is the only thing that branches). DO NOT introduce any Langfuse SDK calls in the test — the existing pattern of monkeypatching get_callback_handler + seed_trace_id is correct and must stay.

Final-task informational note (not part of execution): a running uvicorn process holds the old `_make_config` in memory and must be restarted before the new `langfuse_trace_name` key takes effect on live requests. This is a deploy-side action, not a plan task.
  </action>
  <verify>
    <automated>cd /Users/pollot/Desktop/express-dynamic-workflow && .venv/bin/pytest backend/tests/ -q</automated>
  </verify>
  <done>
- `grep -n "^langchain==0.3.28$" requirements.txt` returns exactly one match, positioned between the `langchain-core` and `langchain-google-genai` lines.
- `grep -n "langfuse_trace_name" backend/api/routes/chat.py` returns at least 2 matches: the dict entry inside `_make_config` AND the docstring bullet under Returns.
- `grep -n 'express-surcharge-agent' backend/api/routes/chat.py` returns the dict-value match (string constant).
- `grep -n "langfuse_trace_name" backend/tests/test_observability_wiring.py` returns exactly one match, located inside `test_chat_attaches_callback_when_enabled`.
- `pytest backend/tests/ -q` reports 186 passed (or 187 if the executor preferred a fresh test — single-extension is the documented preference, so 186 is expected).
- `_make_config("t-1", 0)["metadata"]["langfuse_trace_name"] == "express-surcharge-agent"` (covered by the extended test).
- No other files modified beyond the three listed in `<files>`.
- observability.py byte-identical before/after (the dep pin alone fixes the import; no source change needed).
  </done>
</task>

</tasks>

<verification>
Phase-level checks (run after Task 1 completes):

1. **Test suite still green**:
   ```bash
   cd /Users/pollot/Desktop/express-dynamic-workflow && .venv/bin/pytest backend/tests/ -q
   ```
   Expected: `186 passed` (no skips, no failures, no new tests added beyond the in-place assertion extension).

2. **Dep pin grouped correctly**:
   ```bash
   grep -n "^langchain" requirements.txt
   ```
   Expected output (in this order):
   ```
   langchain-core==0.3.84
   langchain==0.3.28
   langchain-google-genai==2.1.12
   ```

3. **Metadata key wired through**:
   ```bash
   grep -nC 1 "langfuse_trace_name" backend/api/routes/chat.py
   ```
   Expected: shows the constant `"express-surcharge-agent"` value in the metadata dict AND a matching docstring bullet.

4. **Test contract locked**:
   ```bash
   grep -n "langfuse_trace_name" backend/tests/test_observability_wiring.py
   ```
   Expected: exactly one match, inside `test_chat_attaches_callback_when_enabled`.

5. **observability.py untouched** (regression guard):
   ```bash
   git diff --stat backend/agent/observability.py
   ```
   Expected: no changes (empty diff).

6. **Informational deploy reminder** (NOT executed by Claude — note for human/operator): any running uvicorn process must be restarted for the new `langfuse_trace_name` metadata key to take effect on live `/api/chat` requests. Pytest exercises a fresh import each run so the test suite covers the new key without a server restart.
</verification>

<success_criteria>
- [ ] `langchain==0.3.28` pinned in requirements.txt, grouped with the other lang* lines.
- [ ] `_make_config` metadata dict in chat.py contains `langfuse_trace_name: "express-surcharge-agent"` (constant string).
- [ ] Docstring Returns section in `_make_config` documents the new metadata key.
- [ ] `test_chat_attaches_callback_when_enabled` includes an assertion on `langfuse_trace_name`.
- [ ] `pytest backend/tests/` passes 186/186 (no test count change unless executor opted to add a new test, in which case 187/187).
- [ ] No changes to observability.py, models.py, sse.py, or any other file outside the three listed.
- [ ] Existing metadata keys (langfuse_session_id, langfuse_user_id, langfuse_tags, langfuse_trace_id) preserved verbatim.
- [ ] No per-question dynamic trace name introduced (constant only — out-of-scope guard honored).
- [ ] No new dependencies beyond the langchain==0.3.28 pin.
</success_criteria>

<output>
After completion, create `.planning/quick/260503-rs8-pin-langchain-dep-set-langfuse-trace-nam/260503-rs8-SUMMARY.md` summarizing:
- The three edits landed (requirements.txt, chat.py, test file)
- Exact line counts changed (~1 + ~2 + ~1)
- Test result (186/186 or 187/187)
- A reminder line: "uvicorn must be restarted for the new langfuse_trace_name metadata to take effect on live requests"
- Root-cause one-liner: "Langfuse traces were silently no-op'd because `from langfuse.langchain import CallbackHandler` requires top-level `langchain`, which wasn't pinned. Now pinned at 0.3.28; live install verified 25 traces reaching Langfuse Cloud."
</output>
