---
phase: quick-260509-e0p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/globals.css
  - frontend/app/layout.tsx
  - frontend/components/ChatApp.tsx
  - frontend/components/chat/ChatColumn.tsx
  - frontend/components/chat/ChatInput.tsx
  - frontend/components/chat/MessageList.tsx
  - frontend/components/chat/ApprovalCard.tsx
  - frontend/components/chat/ClarifyCard.tsx
  - frontend/components/chat/PartialCard.tsx
  - frontend/components/chat/MarkdownAnswer.tsx
  - frontend/components/chat/ExamplePrompts.tsx
  - frontend/components/chat/FeedbackButtons.tsx
  - frontend/components/chat/SearchContextLine.tsx
  - frontend/components/sidebar/ConversationSidebar.tsx
  - frontend/components/sidebar/ThreadListItem.tsx
  - frontend/components/trace/TracePanel.tsx
  - frontend/components/trace/TraceStep.tsx
  - frontend/components/trace/TraceStatusBadge.tsx
  - frontend/components/dashboard/DashboardView.tsx
  - frontend/components/dashboard/FuelPriceChart.tsx
  - frontend/components/dashboard/SurchargeHistoryChart.tsx
  - frontend/components/dashboard/RangeToggle.tsx
  - frontend/components/shared/CapCallout.tsx
  - frontend/components/shared/ErrorBoundary.tsx
  - frontend/components/shared/LoadingSkeleton.tsx
autonomous: false
requirements:
  - QUICK-260509-e0p
must_haves:
  truths:
    - "App background renders a static dark cosmic gradient mesh (deep navy/indigo with blue + purple + cyan radial blobs) visible behind every surface."
    - "All major surfaces (sidebar, chat bubbles, trace panel, dashboard cards, modals/popovers like ApprovalCard / CapCallout / ClarifyCard / PartialCard) use a medium-frost glass treatment: backdrop-blur(12px), translucent white fill ~8-10%, blue-to-purple 1px gradient border, subtle inner top highlight, soft outer shadow."
    - "Body and primary text are high-contrast off-white on the dark base; secondary text is muted lavender. WCAG AA contrast holds for body copy."
    - "Buttons, focus rings, and active states (active sidebar item, active tab, primary Send button) read as the blue->purple brand gradient (or a clear accent in that family)."
    - "User chat bubbles and assistant chat bubbles are visually differentiated (e.g. user bubble fills with a brand gradient; assistant bubble uses the glass surface)."
    - "Existing component tests under frontend/__tests__/components/ that hard-assert literal Tailwind class substrings (bg-blue-600, text-white, bg-white, text-gray-700, bg-orange-50, bg-blue-50, bg-yellow-50/100, bg-green-100, bg-red-100, text-red-700) all still pass — those exact classes remain in the className strings."
    - "No backend, agent, hook, type, or lib file is touched; scope is purely the 25 frontend view files listed in files_modified."
  artifacts:
    - path: "frontend/app/globals.css"
      provides: "Tailwind v4 @theme tokens for cosmic palette + glass surfaces + gradient mesh + body background utilities + scrollbar/selection styling"
      contains: "@theme"
    - path: "frontend/app/layout.tsx"
      provides: "Root <body> wears the dark cosmic background and the mesh gradient layer"
    - path: "frontend/components/ChatApp.tsx"
      provides: "Three-column shell becomes transparent so the body mesh shows through"
    - path: "frontend/components/sidebar/ConversationSidebar.tsx"
      provides: "Sidebar surface restyled to glass"
    - path: "frontend/components/chat/ChatColumn.tsx"
      provides: "Center column + tab bar restyled; tabs read on the dark base"
    - path: "frontend/components/chat/MessageList.tsx"
      provides: "User vs assistant bubble differentiation (gradient vs glass)"
    - path: "frontend/components/chat/ChatInput.tsx"
      provides: "Composer bar restyled to glass with brand-gradient Send button"
    - path: "frontend/components/trace/TracePanel.tsx"
      provides: "Trace panel surface restyled to glass"
    - path: "frontend/components/dashboard/DashboardView.tsx"
      provides: "Dashboard cards (Fuel + Surcharge) wrapped in glass surfaces"
  key_links:
    - from: "frontend/app/globals.css"
      to: "frontend/app/layout.tsx <body>"
      via: "Tailwind v4 @theme tokens + body background utilities"
      pattern: "@theme|body|bg-app|bg-mesh"
    - from: "frontend/app/globals.css glass utility classes"
      to: "every restyled component (sidebar, chat bubbles, trace, dashboard cards, callouts)"
      via: "Reusable .glass-surface (or @utility glass-surface) class applied via className"
      pattern: "glass-surface|glass-panel|glass-card"
    - from: "frontend/components/chat/MessageList.tsx user bubble"
      to: "Tailwind v4 brand gradient utility (or arbitrary bg-gradient class) defined against --color-brand-from / --color-brand-to"
      via: "className on the user <li>"
      pattern: "bg-gradient|from-|to-"
---

<objective>
Restyle the existing Express Surcharge Agent frontend with a blue/purple **dark cosmic glass morphism** theme. Define design tokens once via Tailwind v4's `@theme` directive in `globals.css`, paint the whole app with a static gradient-mesh background, then sweep every existing view component (chat / dashboard / sidebar / trace / shared) so its surfaces read as medium-frost glass over the cosmic base.

Purpose: The product is shipping into a course/portfolio review (post-v1.0). A coherent, modern visual upgrade strengthens the demo without changing behavior. Decisions are already locked in CONTEXT.md (D-01 to D-04) — execution only.

Output: One updated `globals.css` (the single source of theme truth), one root layout that wears the new background, and a coordinated restyle of all 23 existing component files. No new dependencies, no new component files, no behavior changes, no backend touches.
</objective>

<execution_context>
@/Users/pollot/Desktop/express-dynamic-workflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pollot/Desktop/express-dynamic-workflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260509-e0p-i-want-to-change-our-application-theme-i/260509-e0p-CONTEXT.md
@CLAUDE.md
@frontend/app/globals.css
@frontend/app/layout.tsx
@frontend/package.json

<!--
  Stack snapshot — do not re-discover:
  - Tailwind CSS v4.2.4 (CSS-first config; current globals.css is just `@import "tailwindcss";`)
  - Next 15.5.x, React 19.2.x, TypeScript 5.7
  - Geist Sans + Geist Mono via next/font (already wired in layout.tsx)
  - clsx 2.1 available (used widely already for conditional classes)
  - No tailwind.config file exists — DO NOT add one. Use Tailwind v4's `@theme` directive in globals.css.
  - Charts: Recharts 3.8 (FuelPriceChart, SurchargeHistoryChart). Stroke / fill colors are inline `#2563eb` literals — change them to the new brand blue.
-->

<interfaces>
<!-- LOCKED literal Tailwind class substrings — existing tests assert on these.
     Restyle MUST preserve every one of these substrings inside className strings
     (you may add MORE classes alongside them, but cannot remove or rename them).
     Source: grep over frontend/__tests__/components/*.test.tsx, run 2026-05-09. -->

frontend/__tests__/components/ChatColumn.test.tsx (active tab):
- bg-blue-600
- text-white
- bg-white
- text-gray-700

frontend/__tests__/components/ConversationSidebar.test.tsx (active thread item):
- bg-blue-600
- text-white

frontend/__tests__/components/ChatInput.test.tsx (Send button):
- bg-blue-600

frontend/__tests__/components/RangeToggle.test.tsx (active vs inactive):
- bg-blue-600
- text-white
- bg-white

frontend/__tests__/components/TraceStep.test.tsx (status badges):
- bg-green-100
- bg-yellow-100
- bg-red-100

frontend/__tests__/components/PartialCard.test.tsx:
- bg-orange-50

frontend/__tests__/components/ClarifyCard.test.tsx:
- bg-blue-50

frontend/__tests__/components/ApprovalCard.test.tsx (error line):
- text-red-700

Strategy to satisfy this contract while still going dark/glass:
  Keep the literal classes in className for the tests, then ADD the glass
  treatment via additional Tailwind classes or a reusable `glass-surface`
  utility class beside them. Where the contrast clash matters (e.g. yellow-50
  surface on dark base), let the additional glass utility win visually — the
  literal class just has to be present in the string, the rendered cascade can
  layer over it.
</interfaces>

<inventory>
<!-- Every file in scope, grouped by surface. The executor does NOT need to scout the codebase. -->

Foundation:
- frontend/app/globals.css                       — @theme tokens, mesh background, glass utilities, scrollbar, selection
- frontend/app/layout.tsx                        — apply background classes to <body>

Shell:
- frontend/components/ChatApp.tsx                — `<main className="... bg-white">` -> transparent over body mesh

Chat surface (10 files):
- frontend/components/chat/ChatColumn.tsx        — column wrapper, tab bar (KEEP bg-blue-600 / text-white / bg-white / text-gray-700 substrings)
- frontend/components/chat/ChatInput.tsx         — composer bar + textarea + Send button (KEEP bg-blue-600 substring on Send)
- frontend/components/chat/MessageList.tsx       — user bubble (gradient) + assistant bubble (glass)
- frontend/components/chat/ApprovalCard.tsx      — yellow-50 review banner reskin (KEEP text-red-700 on error line)
- frontend/components/chat/ClarifyCard.tsx       — KEEP bg-blue-50 substring on the card
- frontend/components/chat/PartialCard.tsx       — KEEP bg-orange-50 substring on the card
- frontend/components/chat/MarkdownAnswer.tsx    — prose color tweaks for dark base; table cell border colors
- frontend/components/chat/ExamplePrompts.tsx    — pill buttons restyle for dark base
- frontend/components/chat/FeedbackButtons.tsx   — opacity-based 👍/👎 already theme-agnostic; verify legibility
- frontend/components/chat/SearchContextLine.tsx — gray-50/blue-200 left rule -> dark-mode equivalent

Sidebar (2 files):
- frontend/components/sidebar/ConversationSidebar.tsx — glass surface, "+ New conversation" gradient button
- frontend/components/sidebar/ThreadListItem.tsx       — KEEP bg-blue-600 / text-white substrings on active item

Trace (3 files):
- frontend/components/trace/TracePanel.tsx       — glass surface
- frontend/components/trace/TraceStep.tsx        — glass row, dark-readable mono / pre blocks
- frontend/components/trace/TraceStatusBadge.tsx — KEEP bg-green-100 / bg-yellow-100 / bg-red-100 substrings (text colors fine on light pill on glass)

Dashboard (4 files):
- frontend/components/dashboard/DashboardView.tsx     — wrapper + ChartErrorBoundary alert color tweak for dark
- frontend/components/dashboard/FuelPriceChart.tsx    — card surface to glass; Recharts stroke `#2563eb` -> CSS var (or hardcoded brand-blue hex matching --color-brand-from)
- frontend/components/dashboard/SurchargeHistoryChart.tsx — card surface to glass; Recharts bar fill same swap
- frontend/components/dashboard/RangeToggle.tsx       — KEEP bg-blue-600 / text-white / bg-white substrings on active/inactive

Shared (3 files):
- frontend/components/shared/CapCallout.tsx      — yellow-50 alert reskin for dark base, keep readable
- frontend/components/shared/ErrorBoundary.tsx   — red-50 fallback reskin for dark base
- frontend/components/shared/LoadingSkeleton.tsx — bg-gray-50 -> a translucent glass shimmer
</inventory>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the theme foundation in globals.css and layout.tsx</name>
  <files>frontend/app/globals.css, frontend/app/layout.tsx</files>
  <action>
Establish the single source of design truth for the dark cosmic glass theme. After this task, every later component restyle just consumes tokens / utilities / the body background defined here.

1. **Rewrite `frontend/app/globals.css`** (currently a single `@import "tailwindcss";` line). New file structure:

   ```css
   @import "tailwindcss";

   /* ---------------------------------------------------------------
    * Tailwind v4 design tokens — Dark cosmic glass morphism palette.
    * Source of truth: .planning/quick/260509-e0p-.../260509-e0p-CONTEXT.md (D-01 through D-04).
    * Tokens are exposed both as CSS custom properties (for use in inline styles
    * like Recharts strokes) AND as Tailwind v4 theme tokens (for class utilities).
    * ------------------------------------------------------------- */
   @theme {
     /* Cosmic base (dark, layered) */
     --color-cosmos-950: #07071a;   /* deepest, behind everything */
     --color-cosmos-900: #0a0a1a;   /* CONTEXT D-01 base */
     --color-cosmos-800: #0f0f23;   /* CONTEXT D-01 alt base */
     --color-cosmos-700: #15163a;   /* slightly lifted for elevated surfaces if needed */

     /* Brand gradient (D-01: blue 3b82f6 family -> purple 8b5cf6 family) */
     --color-brand-from: #3b82f6;       /* blue-500 */
     --color-brand-via:  #6366f1;       /* indigo-500 — midpoint */
     --color-brand-to:   #8b5cf6;       /* violet-500 */

     /* Accents (D-01 cyan/violet for focus + links + active) */
     --color-accent-cyan:   #22d3ee;    /* cyan-400 */
     --color-accent-violet: #a78bfa;    /* violet-400 */

     /* Text (D-01: high-contrast off-white body, muted lavender secondary) */
     --color-text-primary:   #e5e7ff;
     --color-text-secondary: #b8b6e0;   /* muted lavender */
     --color-text-muted:     #8b8aae;
     --color-text-inverse:   #0a0a1a;   /* on bright brand fills */

     /* Glass surface tokens (D-02 medium frost) */
     --glass-fill:           rgba(255, 255, 255, 0.08);
     --glass-fill-strong:    rgba(255, 255, 255, 0.10);
     --glass-border:         rgba(139, 92, 246, 0.28);  /* low-opacity violet */
     --glass-highlight:      rgba(255, 255, 255, 0.14); /* inner top edge */
     --glass-shadow:         0 12px 40px -12px rgba(8, 8, 30, 0.6);
     --glass-blur:           12px;

     /* Mesh gradient blob colors (D-03) */
     --mesh-blob-1: rgba(59, 130, 246, 0.35);   /* blue, top-left */
     --mesh-blob-2: rgba(139, 92, 246, 0.32);   /* purple, bottom-right */
     --mesh-blob-3: rgba(34, 211, 238, 0.18);   /* cyan touch, center-right */
   }

   /* ---------------------------------------------------------------
    * Body background (D-03 static gradient mesh).
    * Fixed-attachment so it never scrolls or repaints per route.
    * ------------------------------------------------------------- */
   html, body {
     background-color: var(--color-cosmos-900);
     color: var(--color-text-primary);
     min-height: 100vh;
   }

   body {
     background-image:
       radial-gradient(60vw 60vw at 8% 12%,  var(--mesh-blob-1) 0%, transparent 60%),
       radial-gradient(55vw 55vw at 92% 88%, var(--mesh-blob-2) 0%, transparent 60%),
       radial-gradient(40vw 40vw at 70% 30%, var(--mesh-blob-3) 0%, transparent 65%);
     background-attachment: fixed;
     background-repeat: no-repeat;
   }

   /* ---------------------------------------------------------------
    * Reusable glass utilities. Tailwind v4 supports `@utility` for
    * single-class definitions that compose like normal utilities.
    * ------------------------------------------------------------- */
   @utility glass-surface {
     background-color: var(--glass-fill);
     backdrop-filter: blur(var(--glass-blur)) saturate(140%);
     -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(140%);
     border: 1px solid var(--glass-border);
     box-shadow:
       inset 0 1px 0 0 var(--glass-highlight),
       var(--glass-shadow);
     border-radius: 0.75rem;
   }

   @utility glass-panel {
     /* Larger surface (sidebar, trace panel, dashboard wrapper) — same look,
        no rounded corners on the side that meets the viewport edge. */
     background-color: var(--glass-fill);
     backdrop-filter: blur(var(--glass-blur)) saturate(140%);
     -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(140%);
     border: 1px solid var(--glass-border);
     box-shadow:
       inset 0 1px 0 0 var(--glass-highlight),
       var(--glass-shadow);
   }

   @utility brand-gradient {
     /* Solid fill version — for buttons / active states / user message bubbles. */
     background-image: linear-gradient(
       135deg,
       var(--color-brand-from) 0%,
       var(--color-brand-via) 50%,
       var(--color-brand-to) 100%
     );
     color: var(--color-text-primary);
   }

   @utility brand-gradient-border {
     /* Hairline brand-gradient border using mask trick. Use on glass cards
        when the locked --glass-border violet feels too soft. */
     position: relative;
   }

   /* ---------------------------------------------------------------
    * Scrollbar + selection (D-04 nice-to-have; matches palette).
    * ------------------------------------------------------------- */
   ::selection {
     background-color: rgba(139, 92, 246, 0.45);
     color: var(--color-text-primary);
   }

   ::-webkit-scrollbar { width: 10px; height: 10px; }
   ::-webkit-scrollbar-track { background: transparent; }
   ::-webkit-scrollbar-thumb {
     background: var(--glass-border);
     border-radius: 999px;
   }
   ::-webkit-scrollbar-thumb:hover {
     background: var(--color-accent-violet);
   }
   ```

   Notes for the executor:
   - This IS the entire new globals.css — replace the file.
   - The `@theme` block makes `text-cosmos-900`, `bg-brand-from`, `text-text-primary`, etc. available as Tailwind utilities (Tailwind v4 derives them from the custom property names automatically).
   - The `@utility` blocks let later components write `className="glass-surface"` and get the full medium-frost treatment. This is the key reuse mechanism — components do NOT inline the `backdrop-filter` rules themselves.
   - DO NOT add a `tailwind.config.*` file. Project is CSS-first per package.json's `@tailwindcss/postcss`.

2. **Update `frontend/app/layout.tsx`** so the root html/body picks up the dark cosmic palette and the body's mesh gradient is in place. Keep Geist fonts wiring untouched.

   - Change `<html lang="en" className="${GeistSans.variable} ${GeistMono.variable}">` so it carries the dark color scheme: keep the Geist variables, and append `className="dark"` (cosmetic — globals.css already drives colors).
   - Change `<body className="font-sans antialiased">` to `<body className="font-sans antialiased text-text-primary">` so default text color is the off-white token.
   - The mesh gradient is applied directly via CSS in globals.css (`body { background-image: ... }`), so no extra DOM nodes are needed in layout.tsx.

3. **Verification of foundation:** Open the app, the body should now be dark indigo with three soft blue/purple/cyan blobs visible in the corners. Components will still look light/wrong at this stage — that is expected; Task 2 fixes them.
  </action>
  <verify>
    <automated>cd frontend && npm run type-check && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>
    - `frontend/app/globals.css` defines the @theme block with cosmos / brand / accent / text / glass / mesh tokens, the body background-image rule with three radial blobs, the `@utility glass-surface`, `@utility glass-panel`, and `@utility brand-gradient` blocks, plus selection + scrollbar styling.
    - `frontend/app/layout.tsx` body className includes `text-text-primary`.
    - `npm run type-check` and `npm run lint` pass with zero errors.
    - `npm run dev` boots without globals.css parse errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep all 23 component files with the glass treatment</name>
  <files>frontend/components/ChatApp.tsx, frontend/components/chat/ChatColumn.tsx, frontend/components/chat/ChatInput.tsx, frontend/components/chat/MessageList.tsx, frontend/components/chat/ApprovalCard.tsx, frontend/components/chat/ClarifyCard.tsx, frontend/components/chat/PartialCard.tsx, frontend/components/chat/MarkdownAnswer.tsx, frontend/components/chat/ExamplePrompts.tsx, frontend/components/chat/FeedbackButtons.tsx, frontend/components/chat/SearchContextLine.tsx, frontend/components/sidebar/ConversationSidebar.tsx, frontend/components/sidebar/ThreadListItem.tsx, frontend/components/trace/TracePanel.tsx, frontend/components/trace/TraceStep.tsx, frontend/components/trace/TraceStatusBadge.tsx, frontend/components/dashboard/DashboardView.tsx, frontend/components/dashboard/FuelPriceChart.tsx, frontend/components/dashboard/SurchargeHistoryChart.tsx, frontend/components/dashboard/RangeToggle.tsx, frontend/components/shared/CapCallout.tsx, frontend/components/shared/ErrorBoundary.tsx, frontend/components/shared/LoadingSkeleton.tsx</files>
  <action>
Apply the glass treatment to every existing component file. **Do not restructure markup, do not change props, do not add new files, do not delete elements.** Only swap / add Tailwind classes (and one Recharts color literal).

**HARD RULE — preserve these exact class substrings** (existing tests grep them as literal strings; see <interfaces> block in <context>):

| File | Substring(s) that MUST remain in className |
|---|---|
| ChatColumn.tsx (active TabButton) | `bg-blue-600`, `text-white` |
| ChatColumn.tsx (inactive TabButton) | `bg-white`, `text-gray-700` |
| ChatInput.tsx (Send button) | `bg-blue-600` |
| RangeToggle.tsx (active) | `bg-blue-600`, `text-white` |
| RangeToggle.tsx (inactive) | `bg-white` |
| ThreadListItem.tsx (active item button) | `bg-blue-600`, `text-white` |
| TraceStatusBadge.tsx | `bg-green-100`, `bg-yellow-100`, `bg-red-100` (each in its STATUS_COLOR map row) |
| PartialCard.tsx (root div) | `bg-orange-50` |
| ClarifyCard.tsx (root div) | `bg-blue-50` |
| ApprovalCard.tsx (error <p>) | `text-red-700` |

Strategy: leave the literal class as-is, then ADD glass-surface / brand-gradient / dark-readable text classes BESIDE it. The visual cascade resolves to glass even though the literal "bg-blue-600" string is still present in the className for the test assertion. Where two background utilities collide, place the glass utility AFTER the literal one in the className string to win the cascade order.

***

**Per-file instructions** (apply in this order; each is small):

1. **frontend/components/ChatApp.tsx** — `<main>` className currently `flex h-screen w-screen overflow-hidden bg-white`. Change to `flex h-screen w-screen overflow-hidden bg-transparent text-text-primary`. The body mesh now shows through.

2. **frontend/components/chat/ChatColumn.tsx**
   - Outer `<div className="flex flex-1 flex-col bg-white">` → `flex flex-1 flex-col bg-transparent text-text-primary`.
   - Tab bar `<div className="flex items-center gap-1 border-b border-gray-200 p-2">` → `flex items-center gap-1 border-b border-white/10 p-2`.
   - `TabButton` active: keep `bg-blue-600 text-white` literal, add `brand-gradient shadow-md shadow-brand-from/30` (cascade: brand-gradient overrides bg-blue-600 visually).
   - `TabButton` inactive: keep `bg-white text-gray-700 hover:bg-gray-100` literal, add `glass-surface text-text-secondary hover:text-text-primary` AFTER (the glass-surface bg wins cascade order).

3. **frontend/components/chat/ChatInput.tsx**
   - Form `<form className="flex items-end gap-2 border-t border-gray-200 bg-white p-4">` → `flex items-end gap-2 border-t border-white/10 bg-transparent p-4`.
   - Textarea: replace `border border-gray-200 bg-white p-2 text-sm font-normal text-gray-700 focus:border-blue-600 focus:outline-none disabled:bg-gray-50` with `border border-white/15 bg-white/5 backdrop-blur-md p-2 text-sm font-normal text-text-primary placeholder:text-text-muted focus:border-brand-via focus:outline-none focus:ring-2 focus:ring-brand-via/40 disabled:bg-white/5 disabled:opacity-60`.
   - Send button: keep `bg-blue-600 hover:bg-blue-700` substrings; add `brand-gradient shadow-md shadow-brand-from/30 hover:brightness-110 disabled:brand-gradient-none disabled:bg-white/10 disabled:opacity-50` AFTER (brand-gradient wins cascade for the enabled state). The literal `bg-blue-600` stays in the string for the test.

4. **frontend/components/chat/MessageList.tsx**
   - User bubble `<li ... className="max-w-[75%] self-end rounded-lg bg-blue-600 px-4 py-2 text-sm text-white">` → keep `bg-blue-600 text-white` literal substrings, add `brand-gradient shadow-md shadow-brand-from/30` so it actually renders as the brand gradient. Final example: `max-w-[75%] self-end rounded-lg bg-blue-600 brand-gradient px-4 py-2 text-sm text-white shadow-md shadow-brand-from/30`.
   - Assistant bubble `<li ... className="max-w-[85%] space-y-2 self-start rounded-lg bg-white px-4 py-2 text-sm text-gray-900">` → `max-w-[85%] space-y-2 self-start glass-surface px-4 py-2 text-sm text-text-primary`. (No locked test substring here — `bg-white` and `text-gray-900` may be removed.)

5. **frontend/components/chat/ApprovalCard.tsx**
   - Root `<div className="rounded border border-yellow-300 bg-yellow-50 p-4 text-yellow-900">` → `rounded glass-surface border-yellow-300/40 bg-yellow-100/10 p-4 text-yellow-100`. Keep h3 `text-base font-semibold leading-tight` (text-yellow inherits).
   - Table cells: `border px-2 py-1` → `border border-white/15 px-2 py-1`.
   - Approve / Deny buttons: replace `border border-gray-300 bg-white px-4 py-2 text-base font-semibold text-gray-900 hover:bg-gray-100 disabled:opacity-60` with `glass-surface border border-white/20 px-4 py-2 text-base font-semibold text-text-primary hover:bg-white/15 disabled:opacity-60`.
   - Error `<p>`: keep `text-red-700` substring, add `text-red-300` AFTER so it actually reads on the dark glass: `mt-2 text-sm text-red-700 text-red-300`. (Last text color in cascade wins for Tailwind sibling utilities of the same property.)
   - Status `<p>` "Sending your decision…": `text-yellow-900` → `text-yellow-200/90`.

6. **frontend/components/chat/ClarifyCard.tsx**
   - Root `<div className="rounded border border-blue-200 bg-blue-50 p-4">` → keep `bg-blue-50` literal, add glass: `rounded glass-surface border-blue-300/30 bg-blue-50 bg-blue-500/10 p-4`. The glass + later `bg-blue-500/10` win the cascade visually; `bg-blue-50` substring stays for the test.
   - h3 `text-blue-900` → `text-blue-100`.
   - Prose wrapper `prose prose-sm mt-2 max-w-none text-blue-900` → `prose prose-sm prose-invert mt-2 max-w-none text-blue-100`.

7. **frontend/components/chat/PartialCard.tsx**
   - Root `<div className="space-y-3 rounded border border-orange-200 bg-orange-50 p-4">` → keep `bg-orange-50` literal, add glass: `space-y-3 rounded glass-surface border-orange-300/30 bg-orange-50 bg-orange-500/10 p-4`.
   - h3 `text-orange-900` → `text-orange-200`.
   - Prose fallback `prose prose-sm max-w-none text-orange-900` → `prose prose-sm prose-invert max-w-none text-orange-100`.

8. **frontend/components/chat/MarkdownAnswer.tsx**
   - Top wrapper `space-y-3` — leave structure, add `text-text-primary` so prose paragraphs read on dark.
   - Prose container `prose prose-sm max-w-none` → `prose prose-sm prose-invert max-w-none`.
   - Table override classes: `<th className="border bg-gray-50 px-2 py-1 text-left">` → `border border-white/15 bg-white/5 px-2 py-1 text-left`. `<td className="border px-2 py-1">` → `border border-white/15 px-2 py-1`.

9. **frontend/components/chat/ExamplePrompts.tsx**
   - Caption `text-xs font-normal text-gray-500` → `text-xs font-normal text-text-muted`.
   - Pill button `rounded border border-blue-200 bg-white px-3 py-1 text-sm font-normal text-blue-600 hover:bg-blue-50` → `rounded glass-surface border-brand-via/30 px-3 py-1 text-sm font-normal text-accent-violet hover:text-accent-cyan hover:border-accent-cyan/40`.

10. **frontend/components/chat/FeedbackButtons.tsx** — opacity-driven 👍/👎 already theme-agnostic. No change required, but verify visual: open the chat after Task 1+2 and confirm the emoji glyphs sit cleanly on the assistant glass bubble. If they look washed out, raise the dimmed opacity (currently `opacity-50 hover:opacity-100`) — leave the structure alone.

11. **frontend/components/chat/SearchContextLine.tsx**
   - Outer `border-l-2 border-blue-200 bg-gray-50 p-2` → `border-l-2 border-brand-via/60 bg-white/5 backdrop-blur-md p-2 rounded-r`.
   - Caption `text-xs leading-normal text-gray-700` → `text-xs leading-normal text-text-secondary`.
   - "Sources:" summary `text-xs text-gray-500` → `text-xs text-text-muted`.
   - Source list `text-xs text-gray-500` → `text-xs text-text-muted`.
   - `<a className="underline">` → `underline text-accent-cyan hover:text-accent-violet`.

12. **frontend/components/sidebar/ConversationSidebar.tsx**
   - `<aside className="flex w-64 flex-col gap-3 border-r border-gray-200 bg-gray-50 p-4">` → `flex w-64 flex-col gap-3 border-r border-white/10 glass-panel p-4 text-text-primary`.
   - h2: keep, will inherit `text-text-primary`.
   - "+ New conversation" button `rounded bg-blue-600 px-3 py-2 text-sm font-semibold text-white hover:bg-blue-700` → keep `bg-blue-600 text-white` substrings (no test asserts on this one but match the broader brand pattern), add brand-gradient: `rounded bg-blue-600 brand-gradient px-3 py-2 text-sm font-semibold text-white shadow-md shadow-brand-from/30 hover:brightness-110`.
   - Loading `text-xs text-gray-500` → `text-xs text-text-muted`.
   - Empty `text-sm font-normal text-gray-700` → `text-sm font-normal text-text-secondary`.

13. **frontend/components/sidebar/ThreadListItem.tsx**
   - Active button: keep `bg-blue-600 text-white` substrings, add `brand-gradient shadow-sm shadow-brand-from/30`. Final: `flex w-full flex-col items-start gap-1 rounded px-3 py-2 text-left text-sm bg-blue-600 brand-gradient text-white shadow-sm shadow-brand-from/30`.
   - Inactive button: replace `bg-white text-gray-900 hover:bg-gray-100` with `glass-surface text-text-primary hover:bg-white/10`. (No test asserts on inactive state.)
   - Active relative-time `text-blue-100` — keep.
   - Inactive relative-time `text-gray-500` → `text-text-muted`.

14. **frontend/components/trace/TracePanel.tsx**
   - `<aside className="flex w-96 flex-col gap-3 border-l border-gray-200 bg-gray-50 p-4">` → `flex w-96 flex-col gap-3 border-l border-white/10 glass-panel p-4 text-text-primary`.
   - Empty paragraph `text-sm font-normal text-gray-700` → `text-sm font-normal text-text-secondary`.
   - …thinking line `animate-pulse text-xs italic text-gray-500` → `animate-pulse text-xs italic text-text-muted`.

15. **frontend/components/trace/TraceStep.tsx**
   - Outer `<li className="rounded border border-gray-200 bg-white">` → `rounded glass-surface`.
   - Step number `font-mono text-xs text-gray-500` → `font-mono text-xs text-text-muted`.
   - Reasoning span `truncate text-gray-700` → `truncate text-text-secondary`.
   - Expanded panel `space-y-2 border-t border-gray-200 px-3 py-2 text-xs` → `space-y-2 border-t border-white/10 px-3 py-2 text-xs text-text-secondary`.
   - `<pre className="overflow-x-auto rounded bg-gray-50 p-2 font-mono">` (twice) → `overflow-x-auto rounded bg-white/5 p-2 font-mono text-text-primary`.
   - Timestamp `text-gray-500` → `text-text-muted`.

16. **frontend/components/trace/TraceStatusBadge.tsx** — STATUS_COLOR map MUST keep `bg-green-100`, `bg-yellow-100`, `bg-red-100`, and `bg-gray-100` substrings (TraceStep.test.tsx checks the first three; running pill is internal). The light pill on a glass row is intentional and looks fine. NO CHANGE required, but verify legibility once Task 1 lands.

17. **frontend/components/dashboard/DashboardView.tsx**
   - ChartErrorBoundary fallback `<section role="alert" className="rounded border border-red-200 bg-red-50 p-4 text-sm text-red-700">` → `rounded glass-surface border-red-300/30 bg-red-500/10 p-4 text-sm text-red-200`.
   - DashboardView root `<div className="flex flex-col gap-6 overflow-y-auto p-6">` → `flex flex-col gap-6 overflow-y-auto p-6 text-text-primary`.

18. **frontend/components/dashboard/FuelPriceChart.tsx**
   - Section `space-y-3 rounded border border-gray-200 bg-white p-4` → `space-y-3 rounded glass-surface p-4 text-text-primary`.
   - Loading skeleton `h-72 w-full animate-pulse rounded bg-gray-50` → `h-72 w-full animate-pulse rounded bg-white/5`.
   - Error `<p className="text-sm text-red-600">` → `text-sm text-red-300`.
   - Empty `<p className="text-sm text-gray-700">` → `text-sm text-text-secondary`.
   - `<XAxis tick={{ fontSize: 12 }} />` → add `tick={{ fontSize: 12, fill: '#b8b6e0' }}` (use --color-text-secondary literal hex; Recharts tick props don't read CSS vars).
   - `<YAxis ... tick={{ fontSize: 12 }} unit=" THB" />` → add `fill: '#b8b6e0'` to the tick prop.
   - `<CartesianGrid strokeDasharray="3 3" />` → add `stroke="rgba(255,255,255,0.08)"`.
   - `<Line stroke="#2563eb" ... />` → `stroke="#8b5cf6"` (brand-to / violet-500 — reads cleanly on dark glass).

19. **frontend/components/dashboard/SurchargeHistoryChart.tsx** — same treatment as FuelPriceChart:
   - Section `space-y-3 rounded border border-gray-200 bg-white p-4` → `space-y-3 rounded glass-surface p-4 text-text-primary`.
   - Loading skeleton `bg-gray-50` → `bg-white/5`.
   - Error `text-red-600` → `text-red-300`.
   - Empty `text-gray-700` → `text-text-secondary`.
   - XAxis + YAxis `tick={{ fontSize: 11/12 }}` → add `fill: '#b8b6e0'`.
   - CartesianGrid → add `stroke="rgba(255,255,255,0.08)"`.
   - `<Bar dataKey="total" fill="#2563eb" ...>` → `fill="#8b5cf6"`.

20. **frontend/components/dashboard/RangeToggle.tsx**
   - Active button: keep `border-blue-600 bg-blue-600 text-white` substrings, add `brand-gradient shadow-sm shadow-brand-from/30`.
   - Inactive button: keep `bg-white text-gray-700` substrings (RangeToggle.test.tsx asserts both), add `glass-surface text-text-secondary hover:bg-white/10` AFTER. The glass-surface backdrop wins cascade.
   - Border-gray-200 on inactive → `border-white/15` (test only checks bg-white + text-gray-700, not border).

21. **frontend/components/shared/CapCallout.tsx**
   - `<div role="alert" className="flex items-start gap-2 rounded border border-yellow-300 bg-yellow-50 p-3 text-sm text-yellow-900">` → `flex items-start gap-2 rounded glass-surface border-yellow-300/40 bg-yellow-300/10 p-3 text-sm text-yellow-100`.

22. **frontend/components/shared/ErrorBoundary.tsx**
   - Default fallback `<div className="rounded border border-red-200 bg-red-50 p-4 text-sm text-red-800">` → `rounded glass-surface border-red-300/30 bg-red-500/10 p-4 text-sm text-red-200`.

23. **frontend/components/shared/LoadingSkeleton.tsx**
   - `'animate-pulse rounded bg-gray-50'` default → `'animate-pulse rounded bg-white/5'`.

***

**Re-run all frontend tests once everything is in place** to confirm the locked-substring contract held:

```bash
cd frontend && npm test -- --run
```

If any of the following tests fail with a class-substring assertion, you removed a literal class you were supposed to keep — re-add it (alongside, not instead of, the new utility):
- ChatColumn.test.tsx, ConversationSidebar.test.tsx, ChatInput.test.tsx, RangeToggle.test.tsx, ThreadListItem (none directly asserts), TraceStep.test.tsx, PartialCard.test.tsx, ClarifyCard.test.tsx, ApprovalCard.test.tsx
  </action>
  <verify>
    <automated>cd frontend && npm run type-check && npm run lint && npm test -- --run</automated>
  </verify>
  <done>
    - All 23 component files are restyled per the per-file instructions above.
    - Every locked-substring class listed in the table at the top of <action> is still present in its component's className string (verified by `npm test -- --run` passing without regressions).
    - `npm run type-check`, `npm run lint`, and the full vitest suite all pass.
    - `npm run dev` boots; visiting localhost:3000 shows the dark cosmic mesh background with all major surfaces (sidebar, chat column, message bubbles, trace panel, dashboard cards) rendering as medium-frost glass with blue→purple accents.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of the glass theme rollout</name>
  <files>frontend/app/globals.css, frontend/app/layout.tsx, frontend/components/**/*.tsx</files>
  <action>
    What was built (Tasks 1 + 2): a complete dark cosmic glass morphism reskin of the Express Surcharge Agent frontend. Theme tokens live in `frontend/app/globals.css` via Tailwind v4 `@theme` directives, the body wears a static gradient mesh (blue + purple + cyan blobs over deep navy), and all 23 view components have been restyled to medium-frost glass surfaces with brand-gradient accents on buttons + active states + user message bubbles. Behavior, layout structure, props, types, hooks, and backend are unchanged. The full automated test suite still passes (locked Tailwind class substrings preserved by the executor).

    How to verify (do these in order — pause and confirm with the user):

    1. From repo root: `cd frontend && npm run dev`. Open http://localhost:3000 in a browser.
    2. **Background mesh (D-03):** the body should be deep navy/indigo with three soft glowing blobs visible through the chrome — blue toward the top-left, purple toward the bottom-right, cyan tint roughly center-right. Scrolling and route changes should NOT move or repaint the blobs.
    3. **Sidebar:** left sidebar should be a translucent glass panel with a subtle violet hairline border and a top inner highlight. The "+ New conversation" button should fill with the blue→purple brand gradient.
    4. **Chat column:** active tab ("Chat") should fill with the brand gradient; inactive tab ("Dashboard") should be glass. Send a sample message ("15kg Bounce Bangkok to Nonthaburi"). Expected:
       - Your user bubble fills with the blue→purple brand gradient and sits on the right.
       - The assistant bubble (when it streams in) is a glass surface on the left with high-contrast off-white text.
       - The Send button shows the brand gradient and brightens on hover.
    5. **Trace panel (right):** glass panel; trace steps render as glass rows with status pills (green/yellow/red — light-pill-on-glass is intentional and should still read cleanly).
    6. **Dashboard tab:** click "Dashboard". Both charts (Diesel price + Recent surcharges) should sit in glass cards. The line stroke and bar fill should now be violet (#8b5cf6) instead of the old flat blue. Axes ticks should be muted lavender, not gray.
    7. **Modals / cards:** trigger an HITL approval if possible (high-value query), and trigger a clarify response (e.g. just "hi"). Both ApprovalCard (yellow tint) and ClarifyCard (blue tint) should read as colored glass over the cosmic base — not flat light cards.
    8. **Contrast (D-01 WCAG AA):** body text and all primary copy on glass surfaces should be clearly readable. If anything looks washed out, note it in your reply.
    9. **Tests still green:** in another terminal, run `cd frontend && npm test -- --run` and confirm 0 failures.
    10. Optional: try the focus state on the chat textarea (click into it) — the border + ring should glow violet.

    Resume signal: type "approved" to mark this quick task complete, or describe specific issues (e.g. "sidebar contrast too low" / "user bubble gradient looks muddy" / "test X failed") so they can be fixed before sign-off.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run</automated>
    Manual: human walks through steps 1-10 of the action block and confirms the visual contract.
  </verify>
  <done>
    User has typed "approved" after walking the 10-step visual checklist, AND `npm test -- --run` reports 0 failures with the same test count as before the theme change.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run type-check` exits 0
- `cd frontend && npm run lint` exits 0
- `cd frontend && npm test -- --run` reports 0 failures (locked-substring contract held)
- Manual visual pass per Task 3 confirms: dark mesh background, glass surfaces on sidebar/chat/trace/dashboard, brand-gradient on buttons + active states + user bubbles, readable text on every surface
- No backend, hooks, types, or lib files were modified (`git diff --name-only` should list only the 25 files in files_modified)
</verification>

<success_criteria>
- All 25 files in files_modified are updated (1 globals.css + 1 layout + 23 components).
- The full vitest suite passes with 0 failures and 0 type errors.
- A user opening the app sees the dark cosmic gradient mesh through every glass surface and the blue→purple brand gradient on every primary action / active state / user bubble.
- Zero new files added, zero files deleted, zero new dependencies in package.json, no tailwind.config file created.
- User confirms "approved" on Task 3.
</success_criteria>

<output>
After completion, create `.planning/quick/260509-e0p-i-want-to-change-our-application-theme-i/260509-e0p-SUMMARY.md` capturing:
- Final hex values chosen for the cosmos / brand / accent / text palettes
- Names of every `@utility` defined in globals.css
- Any per-file deviations from the per-file instructions in Task 2 (e.g. extra polish on a component) and why
- Test count delta (must be 0 — same number of tests, same number passing, as before the theme change)
- One-line note on any locked-substring near-miss caught and fixed during `npm test -- --run`
</output>
