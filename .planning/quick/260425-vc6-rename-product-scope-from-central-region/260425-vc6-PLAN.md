---
phase: 260425-vc6-rename-product-scope-from-central-region
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
  - README.md
  - docs/architecture.md
  - backend/agent/tools/calculate_route.py
  - data/scripts/generate_rate_table.py
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - BACKLOG-999.2
must_haves:
  truths:
    - "All user-facing references to 'Central Region' are renamed to 'Bangkok Metro'"
    - "Internal zone identifiers (central-1, central-2, central-3) and the 'central' fragment are unchanged"
    - "Backend test suite still passes (103 passed)"
    - "Backlog item 999.2 in ROADMAP.md is marked resolved with a pointer to this quick task"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Renamed project description (line 6)"
      contains: "Bangkok Metro"
    - path: ".planning/PROJECT.md"
      provides: "Renamed What This Is, Validated requirement, Out of Scope entry"
      contains: "Bangkok Metro"
    - path: "README.md"
      provides: "Renamed tagline, scope line, EPPO data source row, limitation line"
      contains: "Bangkok Metro"
    - path: "docs/architecture.md"
      provides: "Renamed Overview section"
      contains: "Bangkok Metro"
    - path: "backend/agent/tools/calculate_route.py"
      provides: "Renamed docstrings + ValueError messages"
      contains: "No Bangkok Metro zone"
    - path: "data/scripts/generate_rate_table.py"
      provides: "Renamed module docstring + zone comment"
      contains: "Bangkok Metro"
    - path: ".planning/ROADMAP.md"
      provides: "Backlog section with item 999.2 marked resolved"
      contains: "Resolved 2026-04-25"
  key_links:
    - from: "user-facing docs"
      to: "ValueError messages"
      via: "scope name consistency"
      pattern: "Bangkok Metro"
---

<objective>
Rename the user-facing product scope from "Central Region" to "Bangkok Metro" across documentation, narration docstrings, and runtime error messages — without touching internal zone identifiers (`central-1/2/3`), test fixtures, or historical phase artifacts.

Purpose: Live smoke testing surfaced that the rate table and zone classifier only cover Bangkok metro despite docs saying "Central Region". Aligns marketing language with what the system actually supports. Resolves backlog item 999.2 option (b).
Output: 7 files updated; backend tests still green; ROADMAP backlog item 999.2 marked resolved.
</objective>

<execution_context>
@/Users/pollot/Desktop/express-dynamic-workflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pollot/Desktop/express-dynamic-workflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<scope_rules>
DO RENAME (user-facing language):
- The literal string "Central Region" everywhere it appears as scope/marketing/narration text
- This includes docstrings, comments that describe product scope, and runtime ValueError messages

DO NOT RENAME (load-bearing internal identifiers):
- Zone IDs: `central-1`, `central-2`, `central-3` (rate table keys, lookup_rate logic, fixtures)
- The bare token `central` as a zone-name fragment in code/data/tests
- Database rows/columns named `central-*`
- Test fixture data using `central-*` zone names

DO NOT TOUCH (frozen historical records):
- Anything under `.planning/phases/*/` (PLAN, SUMMARY, RESEARCH, VERIFICATION) — point-in-time records
- `.planning/REQUIREMENTS.md`, `.planning/research/`, `.planning/codebase/` — internal planning artifacts
- ROADMAP.md backlog item 999.2 body content (it IS the source of truth for this rename — only update its status)
</scope_rules>

<inventory>
<!-- Pre-surveyed via Grep "Central Region" across the repo. These are the exact in-scope hits. -->

CLAUDE.md
  Line 6: "Express logistics operations in Thailand's Central Region."

.planning/PROJECT.md
  Line  5: "Express logistics operations in Thailand's Central Region."
  Line 18: "Three Central Region zones: central-1 (Bangkok inner)..."
            (rename "Central Region" → "Bangkok Metro"; KEEP central-1/2/3 untouched)
  Line 42: "Multi-region support beyond Central Region — scope limited..."

README.md
  Line   3: "...Express logistics operations in Thailand's Central Region, based on..."
  Line   6: "**Scope**: Bangkok + Central Region (Nonthaburi, Pathum Thani, ...)"
  Line 124: "Daily diesel retail prices, Central Region" (data source table cell)
  Line 245: "**Central Region only**: Does not cover northern, southern, or northeastern Thailand"
            (also rename the bold label "Central Region only" → "Bangkok Metro only")

docs/architecture.md
  Line 7: "Express logistics operations in Thailand's Central Region."

backend/agent/tools/calculate_route.py
  Line  90: docstring "...doesn't map to a Central Region zone."
  Line 100: ValueError(f"No Central Region zone for {destination!r}")
  Line 115: docstring "ValueError: No route found, or destination outside Central Region."

data/scripts/generate_rate_table.py
  Line  3: module docstring "Base rates modeled on Thai Express domestic pricing for Central Region."
  Line 25: comment "# Zone definitions for Central Region"

.planning/ROADMAP.md
  No existing "Backlog" section — Task 3 will add one with item 999.2 marked resolved.
</inventory>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename "Central Region" to "Bangkok Metro" in user-facing docs</name>
  <files>CLAUDE.md, .planning/PROJECT.md, README.md, docs/architecture.md</files>
  <action>
    Replace the literal string "Central Region" with "Bangkok Metro" at each of these exact locations (per inventory). Use Edit tool with the surrounding line as old_string for unambiguous matches.

    1. CLAUDE.md line 6 — change "in Thailand's Central Region." to "in Thailand's Bangkok Metro."

    2. .planning/PROJECT.md line 5 — same change as CLAUDE.md line 6 (this is the source of CLAUDE.md's project block).
    .planning/PROJECT.md line 18 — change "Three Central Region zones: central-1 (Bangkok inner), central-2 (Bangkok outer), central-3 (extended central)" to "Three Bangkok Metro zones: central-1 (Bangkok inner), central-2 (Bangkok outer), central-3 (extended central)". KEEP central-1/2/3 untouched.
    .planning/PROJECT.md line 42 — change "Multi-region support beyond Central Region — scope limited for course timeline" to "Multi-region support beyond Bangkok Metro — scope limited for course timeline".

    3. README.md line 3 — change "in Thailand's Central Region, based on" to "in Thailand's Bangkok Metro, based on".
    README.md line 6 — change "**Scope**: Bangkok + Central Region (Nonthaburi, Pathum Thani, Samut Prakan, Nakhon Pathom, Samut Sakhon, Ayutthaya)" to "**Scope**: Bangkok Metro (Nonthaburi, Pathum Thani, Samut Prakan, Nakhon Pathom, Samut Sakhon, Ayutthaya)" — drop the redundant "Bangkok +" prefix since "Bangkok Metro" already implies Bangkok.
    README.md line 124 — change "Daily diesel retail prices, Central Region" to "Daily diesel retail prices, Bangkok Metro".
    README.md line 245 — change "- **Central Region only**: Does not cover northern, southern, or northeastern Thailand" to "- **Bangkok Metro only**: Does not cover northern, southern, or northeastern Thailand".

    4. docs/architecture.md line 7 — change "in Thailand's Central Region." to "in Thailand's Bangkok Metro."

    Do NOT touch:
    - Zone identifiers `central-1`, `central-2`, `central-3` (they appear on .planning/PROJECT.md line 18 and elsewhere — leave them as-is)
    - Any file under .planning/phases/, .planning/codebase/, .planning/research/, or .planning/REQUIREMENTS.md
  </action>
  <verify>
    <automated>grep -rn "Central Region" CLAUDE.md .planning/PROJECT.md README.md docs/architecture.md 2>&1 | grep -v "^Binary" || echo "CLEAN"</automated>
  </verify>
  <done>The 4 doc files contain zero "Central Region" matches; "Bangkok Metro" appears in their place; central-1/2/3 zone identifiers are untouched; .planning/phases/ artifacts are not modified.</done>
</task>

<task type="auto">
  <name>Task 2: Rename "Central Region" in backend code (docstrings + ValueError) and verify tests still pass</name>
  <files>backend/agent/tools/calculate_route.py, data/scripts/generate_rate_table.py</files>
  <action>
    Rename the literal string "Central Region" in code-level user-facing strings (docstrings, comments, runtime error messages). Do NOT touch zone identifiers, function logic, or test files.

    1. backend/agent/tools/calculate_route.py:
       - Line ~90 (docstring of `_zone_for_destination`): change "doesn't map to a Central Region zone." to "doesn't map to a Bangkok Metro zone."
       - Line ~100 (ValueError): change `raise ValueError(f"No Central Region zone for {destination!r}")` to `raise ValueError(f"No Bangkok Metro zone for {destination!r}")`
       - Line ~115 (docstring of `calculate_route` Raises section): change "ValueError: No route found, or destination outside Central Region." to "ValueError: No route found, or destination outside Bangkok Metro."

    2. data/scripts/generate_rate_table.py:
       - Line ~3 (module docstring): change "Base rates modeled on Thai Express domestic pricing for Central Region." to "Base rates modeled on Thai Express domestic pricing for Bangkok Metro."
       - Line ~25 (comment): change "# Zone definitions for Central Region" to "# Zone definitions for Bangkok Metro"

    Sanity check before declaring done:
    - No test in backend/tests/ asserts on the literal substring "Central Region" (pre-survey confirmed zero matches), so the ValueError rename is safe — but re-grep to be certain.
    - Do not regenerate the rate table CSV — the comment/docstring rename has zero runtime effect.

    Then run the backend test suite to confirm no regression:
       `python -m pytest backend/tests/ -q`
    Expected: 103 passed (matches STATE.md last known good count).
  </action>
  <verify>
    <automated>grep -rn "Central Region" backend/ data/scripts/ 2>&1 | grep -v "^Binary" || echo "CLEAN-CODE" && python -m pytest backend/tests/ -q</automated>
  </verify>
  <done>Zero "Central Region" matches under backend/ and data/scripts/; `pytest backend/tests/ -q` reports 103 passed / 0 failed; no test files modified; zone identifiers central-1/2/3 unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve backlog item 999.2 in ROADMAP.md and final repo-wide audit</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Update ROADMAP.md to record that backlog item 999.2 is resolved.

    Step 1 — Read current ROADMAP.md and check if a "Backlog" section exists. Per pre-survey, no `Backlog` section exists yet (grep returned no files). Append a new section to ROADMAP.md (after the existing Phase Details and Progress sections, at the end of the file):

    ```markdown
    ## Backlog

    Out-of-band items surfaced during execution (not part of the planned 5-phase milestone).

    ### 999.2: Scope-naming mismatch — "Central Region" vs Bangkok Metro

    **Status**: Resolved 2026-04-25 via quick task `260425-vc6-rename-product-scope-from-central-region` (option b: rename docs to match code).

    **Origin**: Live smoke testing on 2026-04-25 surfaced that the rate table and zone classifier only cover Bangkok metro provinces (Nonthaburi, Pathum Thani, Samut Prakan, Nakhon Pathom, Samut Sakhon, Ayutthaya), despite user-facing docs and runtime error messages calling the scope "Central Region".

    **Options considered**:
    - (a) Expand zones to cover the full Thai Central Region (north to Lop Buri, west to Kanchanaburi, etc.) — deferred as a v2.0 possibility; would require new zone definitions, rate table rows, and Google Maps coverage testing.
    - (b) Rename the product scope language from "Central Region" to "Bangkok Metro" across user-facing docs, narration docstrings, and runtime error messages — chosen, smaller blast radius, no rate-table/zone churn.

    **Decision**: Option (b). Internal zone identifiers `central-1/2/3` were intentionally NOT renamed to avoid churn in rate tables, fixtures, and lookup_rate logic. Option (a) remains open as a future expansion possibility for v2.0.
    ```

    Step 2 — Run a final repo-wide audit grep to confirm no in-scope "Central Region" matches remain. Acceptable remaining matches (out of scope):
    - `.planning/phases/**/*` (frozen historical phase artifacts)
    - `.planning/REQUIREMENTS.md`, `.planning/codebase/`, `.planning/research/` (internal planning records)
    - `.planning/ROADMAP.md` itself (the Backlog section legitimately mentions the old name as part of resolution history)
    - `.planning/quick/260425-vc6-rename-product-scope-from-central-region/*` (this task's own files — directory name + this PLAN inventory)

    The audit command should return zero matches in `backend/`, `docs/`, `README.md`, `CLAUDE.md`, `.planning/PROJECT.md`, and `data/scripts/`.

    Step 3 — Skim the cumulative diff (`git diff --stat` then `git diff`) and confirm:
    - No zone-name churn (no `central-1` → anything)
    - No test files modified
    - No phase summary files modified
    - All 7 expected files appear in the diff: CLAUDE.md, .planning/PROJECT.md, README.md, docs/architecture.md, backend/agent/tools/calculate_route.py, data/scripts/generate_rate_table.py, .planning/ROADMAP.md
  </action>
  <verify>
    <automated>grep -rn "Central Region" backend/ docs/ README.md CLAUDE.md .planning/PROJECT.md data/scripts/ 2>&1 | grep -v "^Binary" || echo "CLEAN-FINAL" && grep -c "Resolved 2026-04-25" .planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP.md contains a Backlog section with item 999.2 marked "Resolved 2026-04-25" and pointing to this quick task; final audit grep returns zero matches in the in-scope file set; git diff shows exactly 7 modified files with no zone-identifier churn.</done>
</task>

</tasks>

<verification>
End-to-end checks (run after all 3 tasks complete):

1. **Zero in-scope matches** — confirms no user-facing "Central Region" string survives:
   ```
   grep -rn "Central Region" backend/ docs/ README.md CLAUDE.md .planning/PROJECT.md data/scripts/
   ```
   Expected: no output (exit code 1 from grep = no matches).

2. **Tests still green** — confirms ValueError rename did not break any assertion:
   ```
   python -m pytest backend/tests/ -q
   ```
   Expected: `103 passed` (STATE.md baseline).

3. **No zone-identifier churn** — confirms `central-1/2/3` survived the rename:
   ```
   git diff -- backend/ data/ | grep -E "central-[123]" | grep "^[-+]"
   ```
   Expected: no output (no add/remove lines mention zone identifiers).

4. **Expected file set** — confirms the rename touched exactly the 7 planned files:
   ```
   git diff --name-only
   ```
   Expected: CLAUDE.md, .planning/PROJECT.md, README.md, docs/architecture.md, backend/agent/tools/calculate_route.py, data/scripts/generate_rate_table.py, .planning/ROADMAP.md (and possibly this PLAN.md if quick mode commits it alongside).

5. **Backlog item 999.2 marked resolved** — confirms ROADMAP records the resolution:
   ```
   grep -A1 "999.2" .planning/ROADMAP.md
   ```
   Expected: header line + "Resolved 2026-04-25 via quick task..." line.
</verification>

<success_criteria>
- All 7 in-scope files updated; zero out-of-scope files modified.
- `grep -rn "Central Region" backend/ docs/ README.md CLAUDE.md .planning/PROJECT.md data/scripts/` returns no matches.
- `python -m pytest backend/tests/ -q` reports 103 passed / 0 failed.
- `central-1`, `central-2`, `central-3` zone identifiers appear unchanged in the diff.
- ROADMAP.md has a new Backlog section documenting 999.2 as resolved on 2026-04-25 with option (a) noted as a future v2.0 possibility.
- No test files (`backend/tests/**`), historical phase artifacts (`.planning/phases/**`), or internal planning records (`.planning/REQUIREMENTS.md`, `.planning/codebase/`, `.planning/research/`) are modified.
</success_criteria>

<output>
After completion, create `.planning/quick/260425-vc6-rename-product-scope-from-central-region/260425-vc6-SUMMARY.md` documenting:
- Files changed (7) and the exact line-level renames applied
- Test result confirmation (103 passed)
- Confirmation that zone identifiers and historical artifacts were preserved
- Pointer to ROADMAP.md Backlog section as the resolution record for item 999.2
</output>
