---
phase: 260512-t7q-surface-origin-hub-id-in-fix-02-destinat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/nodes/planner.py
  - backend/tests/test_planner.py
autonomous: true
requirements:
  - OBS-FIX-02-HUBID
must_haves:
  truths:
    - "FIX-02 destination-less short-circuit trace entry surfaces origin_hub_id (no longer opaque to SSE consumers)"
    - "tool_input on the short-circuit entry is a dict with trigger and origin_hub_id keys (matches structured trace shape used elsewhere)"
    - "Existing D-10 pin test (test_planner_does_not_loop_on_destination_less_baseline_query) remains green"
    - "Full backend test suite goes from 358 -> 359 passed"
  artifacts:
    - path: "backend/agent/nodes/planner.py"
      provides: "Short-circuit return dict with structured tool_input on trace entry"
      contains: '"trigger": "fuel_data_present_no_destination"'
    - path: "backend/tests/test_planner.py"
      provides: "Regression test asserting structured tool_input shape on short-circuit trace"
      contains: "fuel_data_present_no_destination"
  key_links:
    - from: "backend/agent/nodes/planner.py:316"
      to: "trace consumers (SSE / Langfuse / frontend trace panel)"
      via: "reasoning_trace[0].tool_input dict payload"
      pattern: '"trigger":\s*"fuel_data_present_no_destination"'
---

<objective>
Surface `origin_hub_id` in the FIX-02 destination-less short-circuit trace entry by upgrading `tool_input` from a bare string to a structured dict (`{"trigger": ..., "origin_hub_id": ...}`).

Purpose: v1.1 milestone audit (.planning/milestones/v1.1-MILESTONE-AUDIT.md) flagged this as the sole cross-phase observability gap (Phase 9 x Phase 11 intersection). Functional state is correct — LangGraph's last-write-wins preserves `origin_hub_id` on partial dict returns — but the trace entry does not surface the hub identity, so SSE consumers reading the cold-start baseline-diesel response cannot see which hub the user chose. Closes the cold-start E2E acceptance criterion ("SSE stream emits `answer` within 30s with `origin_hub_id="hq-lat-krabang"` in trace").

Output: One commit covering the planner.py edit + one new regression test in test_planner.py. Test count 358 -> 359.
</objective>

<execution_context>
@/Users/pollot/Desktop/express-dynamic-workflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pollot/Desktop/express-dynamic-workflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@backend/agent/nodes/planner.py
@backend/tests/test_planner.py
@.planning/milestones/v1.1-MILESTONE-AUDIT.md
@.planning/phases/999.11-investigate-live-sse-hang-on-legit-baseline-diesel-price-query/999.11-SUMMARY.md

<interfaces>
<!-- The exact short-circuit block in planner.py (lines 296-326) — the executor only needs to change ONE line inside this block. -->
<!-- The string "fuel_data_present_no_destination" is unique in the entire backend/ tree (grep-confirmed at planning time): only 1 hit at planner.py:316. -->

Current short-circuit return dict (planner.py:308-326):
```python
return {
    "next_step": "respond",
    "user_intent": state.get("user_intent") or "news_query",
    "reasoning_trace": [
        {
            "step": prior_steps + 1,
            "agent": "planner",
            "tool": None,
            "tool_input": "fuel_data_present_no_destination",   # <-- line 316, the ONLY change
            "tool_output": None,
            "reasoning": (
                "Destination-less baseline query with cached fuel - "
                "short-circuit to respond"
            ),
            "timestamp": ts,
            "status": "ok",
        }
    ],
}
```

Existing D-10 pin test fixture pattern (test_planner.py:1318-1422) — the new test clones this shape:
- Uses `_user_state(...)` helper with `fuel_data` populated, `destination=None`, `shipping_type=None`, `weight_kg=None`, `origin_hub_id="hq-lat-krabang"`.
- Installs `MagicMock` factory via `monkeypatch.setattr(mod, "get_chat_model", mock_factory)` to detect any LLM leakage (short-circuit must fire BEFORE LLM invoke).
- Calls `planner_node(state)` directly.
- The existing D-10 test asserts: `out["next_step"] == "respond"`, `mock_factory.call_count == 0`, and trace entry at step=3 exists with `agent=="planner"` and `status=="ok"`. It does NOT assert the inner `tool_input` shape — so it passes unchanged after the planner.py edit (string -> dict doesn't affect any of its assertions).

Reasoning trace entry shape elsewhere in planner.py (for context — the new dict shape matches the structured trace convention used by other planner emissions):
```python
{"step": N, "agent": "planner", "tool": "...", "tool_input": {...dict...}, "tool_output": {...}, ...}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade FIX-02 short-circuit tool_input to structured dict + add regression test</name>
  <files>backend/agent/nodes/planner.py, backend/tests/test_planner.py</files>
  <behavior>
    Test (new): test_planner_short_circuit_trace_surfaces_origin_hub_id
    - GIVEN: planner_node called with fuel_data populated, destination=None, shipping_type=None, weight_kg=None, origin_hub_id="hq-lat-krabang" (clone D-10 fixture shape)
    - WHEN: planner_node returns
    - THEN:
      * out["next_step"] == "respond" (sanity — short-circuit still fires)
      * out["reasoning_trace"] has exactly one new entry (the short-circuit emission)
      * The new entry's `tool_input` is a `dict` (NOT a string)
      * `tool_input["trigger"] == "fuel_data_present_no_destination"`
      * `tool_input["origin_hub_id"] == "hq-lat-krabang"` (matches input state)
      * mock_factory.call_count == 0 (LLM not invoked — short-circuit must fire BEFORE LLM)

    Existing D-10 pin test (test_planner_does_not_loop_on_destination_less_baseline_query at line 1319): MUST remain green. It asserts on next_step, mock_factory.call_count, and trace entry agent+status — not on tool_input inner shape. Passes unchanged.

    Defense-in-depth: backend/tests/test_api_chat.py:197 reads SSE event stream structure (event types meta/trace/answer/done), not inner tool_input shape. Passes unchanged.
  </behavior>
  <action>
**Step 1: Confirm uniqueness (sanity grep before edit).**

Run from repo root:
```
grep -rn "fuel_data_present_no_destination" backend/ --include="*.py"
```
Expected: exactly ONE hit at `backend/agent/nodes/planner.py:316`. If more than one hit appears, STOP and reassess — the Edit assumption is broken.

**Step 2: Edit planner.py with Edit tool (replace_all: false, since string is unique).**

File: `backend/agent/nodes/planner.py`

old_string (exact, including 20-space indent — matches line 316 in the short-circuit block):
```
                    "tool_input": "fuel_data_present_no_destination",
```

new_string (exact, multi-line dict with state.get for origin_hub_id):
```
                    "tool_input": {
                        "trigger": "fuel_data_present_no_destination",
                        "origin_hub_id": state.get("origin_hub_id"),
                    },
```

Do NOT change any other field in the return dict (next_step, user_intent, step, agent, tool, tool_output, reasoning, timestamp, status all stay byte-identical). Do NOT touch the `_set_guard_refusal` helper or any other planner path. Do NOT change the `logger.info(...)` call at line 302-305.

**Step 3: Add regression test in backend/tests/test_planner.py.**

Place the new test immediately AFTER the existing `test_planner_does_not_loop_on_destination_less_baseline_query` function (which ends around line 1422, before the `# ---- Phase 11 / FIX-02 defense-in-depth ----` header at line 1425). Use Edit with `old_string` anchored on a unique snippet of that section divider header — for example:

old_string (use the exact divider, which is unique):
```
# ---------------------------------------------------------------------------
# Phase 11 / FIX-02 defense-in-depth — tool_call_count reducer invariant
# ---------------------------------------------------------------------------
```

new_string (insert new test BEFORE the divider; preserves the divider intact afterward):
```
def test_planner_short_circuit_trace_surfaces_origin_hub_id(monkeypatch):
    """v1.1 audit / observability gap — surface origin_hub_id in short-circuit trace.

    The FIX-02 destination-less short-circuit emits a single reasoning_trace
    entry to mark the routing decision. The trace entry's tool_input must be
    a structured dict containing the trigger AND origin_hub_id, so SSE
    consumers (frontend trace panel, Langfuse spans) can see which hub the
    user selected during a cold-start baseline-diesel query.

    Rationale: .planning/milestones/v1.1-MILESTONE-AUDIT.md flagged this as
    the sole cross-phase observability gap (Phase 9 hub selection x Phase 11
    short-circuit). Functional state is correct — LangGraph last-write-wins
    preserves origin_hub_id on partial dict returns — but the trace entry
    was opaque (bare string). This pins the structured shape.

    Mirrors the D-10 pin fixture at test_planner.py:1318 line-for-line; the
    only difference is the assertion target (tool_input inner shape vs.
    next_step + trace-entry presence).
    """
    mock_factory = MagicMock()
    mock_factory.return_value = _scripted_llm(
        '{"user_intent": "surcharge_query", '
        '"shipping_type": null, "weight_kg": null, '
        '"origin": null, "destination": null, '
        '"origin_hub_id": null, '
        '"missing_fields": [], '
        '"next_step": "fetch_fuel", '
        '"clarification_reason": null}'
    )
    monkeypatch.setattr(mod, "get_chat_model", mock_factory)

    state = _user_state(
        "What's the current diesel price in Bangkok?",
        fuel_data={
            "price": 39.95,
            "baseline": 36.31,
            "delta_pct": 0.1002,
            "date": "2026-05-11",
            "unit": "THB/L",
            "source": "eppo_cached_csv",
            "fetched_at": _now_iso_z(),
        },
        route_data=None,
        destination=None,
        origin=None,
        shipping_type=None,
        weight_kg=None,
        origin_hub_id="hq-lat-krabang",
        reasoning_trace=[
            {
                "step": 1,
                "agent": "planner",
                "tool": None,
                "tool_input": {},
                "tool_output": {"next_step": "fetch_fuel"},
                "reasoning": "Intent=surcharge_query; routing to fetch_fuel",
                "timestamp": _now_iso_z(),
                "status": "ok",
            },
            {
                "step": 2,
                "agent": "fuel_agent",
                "tool": "fetch_fuel_price",
                "tool_input": None,
                "tool_output": {"price": 39.95, "source": "eppo_cached_csv"},
                "reasoning": "Diesel above baseline",
                "timestamp": _now_iso_z(),
                "status": "ok",
            },
        ],
    )

    out = planner_node(state)

    # Sanity: short-circuit still fires (no regression vs. D-10 pin).
    assert out["next_step"] == "respond"
    assert mock_factory.call_count == 0, (
        "Short-circuit must run BEFORE the LLM is called"
    )

    # The short-circuit emits exactly one new trace entry at step=3.
    new_entries = [
        e for e in out.get("reasoning_trace", [])
        if isinstance(e, dict) and e.get("step") == 3
    ]
    assert len(new_entries) == 1, (
        f"expected exactly 1 short-circuit trace entry at step=3; "
        f"got {len(new_entries)}: {new_entries!r}"
    )
    entry = new_entries[0]

    # The load-bearing assertions — the structured tool_input shape.
    assert isinstance(entry["tool_input"], dict), (
        f"v1.1 audit regression: short-circuit tool_input must be a dict "
        f"(structured trace shape), got {type(entry['tool_input']).__name__}: "
        f"{entry['tool_input']!r}"
    )
    assert entry["tool_input"]["trigger"] == "fuel_data_present_no_destination", (
        f"trigger key must preserve original sentinel string for grep-ability; "
        f"got {entry['tool_input'].get('trigger')!r}"
    )
    assert entry["tool_input"]["origin_hub_id"] == "hq-lat-krabang", (
        f"v1.1 observability gap: origin_hub_id must surface in trace so SSE "
        f"consumers can see which hub the user chose; got "
        f"{entry['tool_input'].get('origin_hub_id')!r}"
    )


# ---------------------------------------------------------------------------
# Phase 11 / FIX-02 defense-in-depth — tool_call_count reducer invariant
# ---------------------------------------------------------------------------
```

Note: the new_string ends with the EXACT same three lines as the old_string (the divider header), so the divider is preserved and the new test is inserted immediately before it. Verify by inspection of the diff — there should be exactly one copy of that divider in the file after the edit.
  </action>
  <verify>
    <automated>cd /Users/pollot/Desktop/express-dynamic-workflow/backend &amp;&amp; .venv/bin/python -m pytest tests/test_planner.py -k "destination_less or short_circuit_trace" -v &amp;&amp; .venv/bin/python -m pytest tests/ 2>&amp;1 | tail -3</automated>
  </verify>
  <done>
    1. Grep `"fuel_data_present_no_destination"` in backend/agent/nodes/planner.py shows it INSIDE a dict literal as the value of a `"trigger"` key (no longer as a bare `tool_input` string value).
    2. New test `test_planner_short_circuit_trace_surfaces_origin_hub_id` exists in backend/tests/test_planner.py and passes.
    3. Existing test `test_planner_does_not_loop_on_destination_less_baseline_query` (D-10 pin) still passes.
    4. Defense-in-depth section divider (`# Phase 11 / FIX-02 defense-in-depth`) appears exactly once in test_planner.py.
    5. Full backend test suite: `359 passed` (was 358 baseline post-Phase 11).
    6. No other planner paths changed (`_set_guard_refusal`, cache-aware override, LLM invoke path all untouched — diff scope: planner.py = 1 logical line replaced with a 4-line dict literal; test_planner.py = 1 new function added).
  </done>
</task>

</tasks>

<verification>
- `grep -n "fuel_data_present_no_destination" backend/agent/nodes/planner.py` shows the string as a dict VALUE under a `"trigger":` key, not as a bare tool_input value.
- `grep -c "fuel_data_present_no_destination" backend/tests/test_planner.py` returns >= 1 (the new test references it as an assertion target).
- `cd backend && .venv/bin/python -m pytest tests/test_planner.py -k "destination_less or short_circuit_trace" -v` — both the D-10 pin and the new test pass.
- `cd backend && .venv/bin/python -m pytest tests/ 2>&1 | tail -3` — final summary line says `359 passed` (no skips/xfails introduced; was 358 baseline).
- No frontend changes (`git status` shows only `backend/agent/nodes/planner.py` and `backend/tests/test_planner.py` modified).
</verification>

<success_criteria>
- Backend test count: 358 -> 359 passed.
- `tool_input` on the FIX-02 short-circuit trace entry is a structured dict with `trigger` and `origin_hub_id` keys.
- D-10 pin test green (no regression on the FIX-02 fix itself).
- Single atomic commit: `feat(quick-260512-t7q): surface origin_hub_id in FIX-02 short-circuit trace (observability)`
- Closes the v1.1 milestone audit's cross-phase observability gap (Phase 9 x Phase 11).
</success_criteria>

<output>
After completion, create `.planning/quick/260512-t7q-surface-origin-hub-id-in-fix-02-destinat/260512-t7q-SUMMARY.md` capturing:
- Files changed (planner.py + test_planner.py)
- Diff summary (1 logical line in planner.py replaced; 1 new test function in test_planner.py)
- Test count delta (358 -> 359)
- Confirmation that D-10 pin test and SSE structure test passed unchanged
- Closure of v1.1 milestone audit observability gap
</output>
